# 工具交付邊界

本檔用來區分「應納入版本控管的工具碼」與「只作為本機案例、輸出或參考資料的檔案」。原則是：可重跑的程式、測試、preflight 與必要部署資產可以進 repo；大型工程案例、Office/PDF 輸出、暫存資料與本機快取不進 repo。

## 目前建議狀態

| 區域 | 建議 | 理由 |
|---|---|---|
| 所有直接呼叫 PowerShell 的 `.bat` 啟動器 | 納入 | 統一優先 `pwsh -NoProfile` 並保留 `powershell -NoProfile` 後備；既有 launcher smoke 會自動掃描所有批次檔，避免新增 wrapper 回退成單一路徑。各 wrapper 原有的固定參數、`%*` 透傳與 STA 邊界必須保持不變。 |
| `preflight-tools.ps1`、`run-preflight-tools.bat`、`run-preflight-tools-quick.bat`、`run-preflight-tools-ci.bat`、`run-preflight-tools-release.bat`、`結構工具箱/tools/release-preflight-lock.ps1`、`結構工具箱/tools/release-preflight-lock.test.js` | 納入 | 已成為跨工具交付前檢查入口；CI / quick / normal / release wrapper 需一起提交。CI 只跑 clean-checkout 可重現 gate，不得冒充完整 quick 或正式 release；release wrapper 固定強制慢檢查與平台 audit，且不得透傳 `%*` 任意參數或被 `-Quick` 覆蓋。正式 release 必須先取得依正規化工作區路徑衍生的 Windows 具名 Mutex；同工作區第二輪立即失敗關閉，程序終止後由 OS 釋放，不使用會殘留的鎖檔，不同工作區及 quick／CI 不互相阻擋。鎖 helper 與真實雙程序競爭／強制終止測試屬私人治理工具，不發布至 Pages；頁面專用 report-readiness / Pages artifact governance 需由 `pages-release-governance-contract` 納入同一輪 preflight 記錄。 |
| `.github/workflows/pr-validation.yml`、`pr-validation.contract.test.js` | 納入 | 送往 `master` 的 PR 使用 Windows runner 執行 `run-preflight-tools-ci.bat` clean-checkout gate，採唯讀權限、不依賴 secrets、限制執行時間並保留 summary / history artifact；ignored audit 狀態、本機 node_modules / Python 套件與本機工具檔仍由工作站 quick / release 補證，契約與 `pr-validation-contract` preflight record 必須同批提交。 |
| `audit-all.ps1`、`refresh-platform-status.ps1`、`platform-audit-preflight.ps1`、`run-audit-all.bat`、`run-audit-all-loop.bat` | 納入 | 平台 audit orchestration 與 preflight 重用判定入口；`audit-all.ps1` 子 audit 採 `ProcessStartInfo` 執行，避免 Windows `Path` / `PATH` 環境鍵重複時 `Start-Process` 失敗，也避免子系統 audit 狀態新鮮但整體摘要或 dashboard decision hash 未刷新。 |
| `鋼構工具/run-audit.bat`、`鋼筋混凝土/run-audit.bat`、`結構工具箱/audit-core.ps1`、`結構工具箱/run-audit-core.bat` | 納入 | 子系統 audit wrapper 與規範核心 audit 入口；preflight helper script 清冊會檢查這些入口存在並列入 staging 文件。 |
| `開挖擋土支撐/start_html_mode.ps1`、`開挖擋土支撐/stop_html_mode.ps1` | 納入 | 開挖擋土支撐本機服務邊界 smoke 需要的啟停入口；工程資料仍排除。 |
| `開挖擋土支撐/sign_receiver_request.ps1`、`開挖擋土支撐/sign_receiver_evidence_templates.ps1`、`開挖擋土支撐/簽署RVR身分請求.bat`、`開挖擋土支撐/簽署SEV身分請求.bat`、`開挖擋土支撐/簽署治理健康檢核點.bat`、`開挖擋土支撐/驗證治理健康檢核點.bat`、`開挖擋土支撐/建立治理檢核可信時間請求.bat`、`開挖擋土支撐/完成治理檢核可信時間證據包.bat`、`開挖擋土支撐/驗證治理檢核可信時間證據包.bat`、`開挖擋土支撐/簽署補充證據範本包.bat`、`開挖擋土支撐/verify_source_evidence_chain.ps1`、`開挖擋土支撐/驗證SEV證據鏈.bat`、`開挖擋土支撐/backup_receiver_trust_registry.ps1`、`開挖擋土支撐/check_receiver_trust_backup_health.ps1`、`開挖擋土支撐/備份與演練RVR信任清冊.bat`、`開挖擋土支撐/檢查RVR備份健康狀態.bat` | 納入 | RVR／SEV 身分、治理健康 GCR／GHC 與補充證據範本發布包共用的離線 Ed25519 簽署、ERH／RVR／SEV／GHC／可選公開 RTB 的可攜式獨立證據鏈驗證、本機公開信任清冊備份、隔離復原演練與每日健康監測入口。GHC 將完整 GHE 綁入獨立簽章 context，管理頁或獨立 GCV 驗證器可判斷目前 GHR 是相同、延伸、落後或分叉；只有受信任簽章且指定目前鏈相同或延伸可作為該歷程的錨點。GCV 不啟動服務、不讀專案資料庫或私人金鑰，且必須連同 GHC、RTB、GHE 來源檔保留，不是單獨證明。GAM／GTS／GTV 可選流程會以組織選定 TSA 與信任根建立並重驗 RFC 3161 SHA-256 時間戳；程式不自行連線 TSA、不查 OCSP／CRL，可信時間也不證明異地保存或工程核可。沒有 GTV 時 GHC／GCV 不具第三方時間戳；即使有 GTV，工具仍不證明檔案確已外部保存。範本發布簽章只固定來源與內容完整性，匯入後仍須本機核准；SCV 也不重新計算容量或構成工程核可。工程失敗不得因簽章升級，缺少雙方受信任身分時維持人工核對。RSR、SSR、GCR 與範本發布使用不同內容脈絡，私人金鑰只由離線簽署程序讀取；同一信任清冊負責公鑰登錄與撤銷狀態。詳細狀態與只追加的 RBH 健康轉換串鏈留在受控備份資料夾；相同狀態及問題代碼不重複新增。儀表板只讀 ignored `output/audit/rvr-backup-health-status.json` 與 `output/audit/rvr-backup-health-history.json` 的去識別本機摘要，不得含路徑、檔名、證據／事件指紋或清冊內容，也不得發布成 Pages 狀態資產。 |
| `開挖擋土支撐/receiver_governance_archive.ps1`、`開挖擋土支撐/建立治理可信時間外部歸檔請求.bat`、`開挖擋土支撐/完成治理可信時間外部歸檔證據包.bat`、`開挖擋土支撐/驗證治理可信時間外部歸檔證據包.bat`、`開挖擋土支撐/backend/receiver_governance_archive.py`、`開挖擋土支撐/backend/tests/test_receiver_governance_archive.py`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE.md`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_SCHEMA.json` | 納入 | GTV 的供應商中立外部 DMS／WORM 歸檔交換與獨立重驗入口。GAD 只接受封閉請求 JSON 與未加密、未壓縮、無子資料夾的單一 GAP ZIP；GAP 內完整 GTV 必須可重驗。GAR 只能由實際保存端在入庫後，以獨立 Ed25519 金鑰簽署相同 GAD／GAP、保存端／保存庫、物件與不可變版本 ID、入庫時間、模式、保存政策 ID、期限及 legal hold；draft 2020-12 JSON Schema 提供外部介接的封閉欄位契約，不能取代完整驗證器。案件端須以組織獨立核定的公開金鑰驗章，並把當時實際採用的核定證據檔 bytes 與 SHA-256 納入 GAV；驗證器不判讀該文件內容或自行建立信任。錯誤金鑰、期限不足、模式或政策不符、必要 legal hold 未啟用、增刪檔、重複可攜式檔名、壓縮／加密 ZIP、私人金鑰混入或 bytes 竄改均失敗關閉，且不得留下 GAV。GAV 是保存端簽章證言，不是工具即時查詢外部保存庫、供應者控制有效性、工程核可或正式附件資格；GAD／GAP／GAR／GAV 不得進入計算書、正式附件包或公開 Pages。 |
| `開挖擋土支撐/receiver_governance_archive_lifecycle.ps1`、`開挖擋土支撐/簽發外部歸檔週期狀態收據.bat`、`開挖擋土支撐/建立外部歸檔生命週期檢查點.bat`、`開挖擋土支撐/驗證外部歸檔生命週期檢查點.bat`、`開挖擋土支撐/backend/receiver_governance_archive_lifecycle.py`、`開挖擋土支撐/backend/tests/test_receiver_governance_archive_lifecycle.py`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE.md`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE_SCHEMA.json` | 納入 | GAV 的供應商中立長期保存週期治理。保存端必須先在自身保存庫實際重新觀察同一物件、不可變版本、GAP SHA-256／bytes、保留鎖、期限與 legal hold，才可簽發 GSR；簽發工具本身不查詢保存庫。案件端完整重驗原 GAV，以明確選取且另有實際核定證據的現行 Ed25519 公開金鑰驗證 GSR；允許金鑰輪替，但新金鑰必須重新核定。只有物件存在且內容相符、不可變狀態 active、模式與政策一致、期限涵蓋下一次重驗且必要 legal hold 仍 active 才建立封閉五檔 GSC。重驗會將歷史完整性與目前生命週期狀態分離為 current／review-due／blocked；週期或期限到達不改寫歷史證據，也不得把歷史有效冒充目前有效。錯誤金鑰、過舊觀察、負面保存狀態、期限不足、增刪檔、壓縮／加密 GAV ZIP、私人金鑰混入或 bytes 竄改均失敗關閉。GSR／GSC 不是獨立外部觀察、供應者控制證明、工程核可或正式附件，且不得進入計算書、正式附件包或公開 Pages。 |
| `開挖擋土支撐/receiver_governance_archive_lifecycle_portfolio.ps1`、`開挖擋土支撐/檢查多案件外部歸檔生命週期.bat`、`開挖擋土支撐/建立多案件外部歸檔生命週期總覽快照.bat`、`開挖擋土支撐/驗證多案件外部歸檔生命週期總覽快照.bat`、`開挖擋土支撐/backend/receiver_governance_archive_lifecycle_portfolio.py`、`開挖擋土支撐/backend/tests/test_receiver_governance_archive_lifecycle_portfolio.py`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE_PORTFOLIO.md`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE_PORTFOLIO_SCHEMA.json`、`.gitignore`、`結構工具箱/tools/build-pages-artifact.js`、`結構工具箱/tools/pages-live-smoke.js`、`pages-release-governance.contract.test.js` | 納入 | 多案件 GSC 只讀完整重驗與期限提醒。掃描只接受實體來源樹且不跟隨連結；每個候選 GSC 均完整重驗，再依保存端／保存庫／物件／不可變版本分鏈。同指紋複本去重，較舊檢查點保留為歷史；相同最新觀察時間衝突、同一版本不同 GAP、任何無效候選包、掃描上限或來源異動均阻擋，不能由有效舊包掩蓋。`upcoming` 只是在可設定期限內預先提醒，不改寫 GSC 的 current 狀態。封閉 GSP 快照只含決定性 JSON／HTML 並不覆寫；自我驗證只證明當時快照未改，不重新查詢來源 GSC 或外部保存庫，也不得冒充目前有效。發布目的地不得位於來源樹或工具程式庫內；`.gitignore`、Pages artifact 分類與線上負向探針另提供縱深隔離。GSP 不是工程核可、正式附件或法律保存完成證明，且不得進入計算書、正式附件包或公開 Pages。 |
| `開挖擋土支撐/receiver_governance_archive_lifecycle_monitor.ps1`、`開挖擋土支撐/manage_receiver_governance_archive_lifecycle_monitor_task.ps1`、`開挖擋土支撐/onboard_receiver_governance_archive_lifecycle_monitor.ps1`、`開挖擋土支撐/receiver_governance_archive_lifecycle_monitor_center.ps1`、`開挖擋土支撐/安裝多案件外部歸檔生命週期每日監測.bat`、`開挖擋土支撐/開啟多案件生命週期監控管理中心.bat`、`開挖擋土支撐/檢查多案件外部歸檔生命週期監測排程.bat`、`開挖擋土支撐/移除多案件外部歸檔生命週期每日監測.bat`、`開挖擋土支撐/backend/receiver_governance_archive_lifecycle_monitor.py`、`開挖擋土支撐/backend/tests/test_receiver_governance_archive_lifecycle_monitor.py`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE_MONITOR.md`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE_MONITOR_SCHEMA.json`、`開挖擋土支撐/GOVERNANCE_TRUSTED_ARCHIVE_LIFECYCLE_DASHBOARD_SCHEMA.json`、`結構工具箱/audit-dashboard.html`、`結構工具箱/tools/audit-dashboard.contract.test.js`、`結構工具箱/tools/audit-dashboard-browser-smoke.test.js`、`.gitignore`、`結構工具箱/tools/build-pages-artifact.js`、`結構工具箱/tools/pages-live-smoke.js`、`pages-release-governance.contract.test.js` | 納入 | GSP 之上的本機每日排程與狀態轉換監測。案件導入先建立不寫狀態、不註冊排程的草稿，並以唯讀完整預掃顯示去識別狀態計數；使用者須明確勾選核可，且 Install 必須攜帶同一組路徑、時間、提醒、掃描與輸出設定的 `GMI` 指紋，設定漂移即在寫入與註冊前阻擋。取消不建立排程；導入核可只同意本機維運設定，不是工程或附件核可。管理中心只列舉本工具固定描述且位於 Task Scheduler 根目錄的排程，逐一以不寫 dashboard 的 Status 重驗動作、觸發器、權限、路徑分離、最近結果與 GSM 新鮮度，區分正常、期限注意與設定漂移／停用／失敗／過期；新增與更新仍回到預覽指紋及核可方塊，移除須再次勾選且只刪排程。完整管理快照只在程序記憶體，含排程名稱與絕對路徑、可能含案件識別，敏感度高於去識別摘要。每輪完整重驗全部 GSC；`GMS` 排除評估時間與剩餘秒數，只綁定案件／GSC 選取、期限、狀態、原因、無效包與問題，因此相同訊號只更新 latest，不重複新增事件或通知。首次異常、注意內容或嚴重度改變及恢復才追加串鏈 `GME` 並限時通知；current 盤點變動會入鏈但不彈窗。狀態專用資料夾與來源樹及程式庫完全分離，封閉事件序號、前後訊號、狀態、時間與指紋；未知項目、連結、硬連結、競態或發布失敗均阻擋／回滾。工作排程採目前登入使用者、有限權限、漏跑補執行與禁止重疊；移除只刪排程不刪證據。狀態自驗只證明本機鏈與新鮮度，不重掃來源。每輪另原子更新 ignored 的去識別狀態、轉換歷程與排程摘要；封閉 schema 禁止案件名稱、路徑、排程名稱、保存端資料與證據指紋，失敗時狀態改為 untrusted 且清空舊計數。儀表板逾 36 小時即顯示過期，公開站缺檔固定顯示僅限本機。唯一 latest／事件檔名與本機管理腳本另由 Git ignore、Pages artifact 分類及線上負向探針防止誤公開。GSM／GME／導入草稿／管理中心／排程、通知與 dashboard 摘要不是工程核可、正式附件、外部時間戳或法律保存完成證明，不得進入計算書、正式附件包或 Pages。 |
| `開挖擋土支撐/excavation-traceability.catalog.json`、`開挖擋土支撐/excavation-traceability.contract.test.js`、`開挖擋土支撐/excavation-report.contract.test.js`、`開挖擋土支撐/receiver-evidence-templates.test.js`、`開挖擋土支撐/frontend/src/receiverEvidenceTemplates.ts`、`開挖擋土支撐/backend/app/receiver_evidence_template_package.py`、`開挖擋土支撐/backend/tests/test_receiver_evidence_template_package.py`、`開挖擋土支撐/backend/app/receiver_capacity.py`、`開挖擋土支撐/backend/tests/test_receiver_capacity.py`、`開挖擋土支撐/backend/app/pdf_render_evidence.py`、`開挖擋土支撐/backend/tests/release_report_artifacts.py` | 納入 | 開挖擋土支撐服務型工具的規範語意追蹤 catalog、服務治理契約、報告邊界契約、ERH 重撐 H 型鋼無偏心純軸壓受限實算與 RSC 證據、核可 PDF 逐頁像素／OCR／文字層證據及正式放行成品產生器；本機補充證據範本只重用五類查核的依據、文件編號、版次、日期與頁碼。schema v3 另保存內容版號、逐版變更欄位、本機審核人、核准時間、有效期限及簽章包匯入當下的發布來源。組織簽章包共用 RKE 信任清冊，僅證明來源與內容完整性；所有外部匯入仍為待本機核准，內容修訂會清除原發布來源。只有已核准且未過期的範本可用。範本禁止保存或套回檔名及 SHA-256，且不寫入 RVR、PDF、DOCX 或專案資料；本機核准與發布簽章都不構成工程核可。以上項目集中確認分析輸出匯入、支撐 / 橫擋 / 斜撐 / 大角撐、柱與基礎、特定承接容量、PDF / DOCX 報表、本機服務資料治理的來源、輸入、計算、報告、證據與人工複核邊界。 |
| `continuous-beam-regression.test.js`、`test-continuous-beam.ps1` | 納入 | 連續梁回歸、JSON 重播結果鏈與報告邊界檢查；確認雙跨混合載重案例由本地 JSON 重播後，採用輸入、反力、剪力、彎矩、撓度與計算書指紋完全一致，並在改動目前案例前拒絕未知 schema 或拓撲不完整的 JSON。另確認分析表格、圖面與 HTML 計算書仍有足量可讀文字、必要章節，且頁面專用閱讀狀態不混入計算書。案件欄位可留空並由主文承接；直接輸入 I 值或其他待人工確認條件仍如實列入工程內容。計算書預設內部審閱，勾選核可後成為正式附件。操作頁另共用直接列印封鎖邊界。 |
| `SRC工具/core/src-beam-core.js`、`SRC工具/src-beam.html`、`SRC工具/src-beam.css`、`SRC工具/src-beam.js`、`SRC工具/src-beam-core.test.js`、`SRC工具/src-beam.contract.test.js`、`SRC工具/src-beam-browser-smoke.test.js`、`SRC工具/src-traceability.catalog.json`、`SRC工具/README.md` | 正式工具納入、公開入口 `/src-beam` | 包覆型 SRC 梁採現行 100 年修正版規範，2024 研究修正草案不得冒充現行法規；官方教材回歸與獨立 oracle 鎖定撓曲、寬厚比、鋼骨／RC 剪力分擔、一般剪力與剪力摩擦。未知 schema、耐震、軸力、非完全包覆、未確認常重混凝土／整體澆置、剪力釘與關鍵細部均失敗封閉。正式操作頁已具案件 JSON 指紋重播、只含採用資料／公式／結果／結論的精簡計算書、空白案件欄位容許、內部審閱／正式附件核可、核可資料異動撤銷、直接列印封鎖及 Chromium PDF 證據；quick preflight 執行核心與報告契約，full／release preflight 另執行真實瀏覽器 smoke。首頁卡片、clean route、獨立基準與 release 當輪 PDF／來源 JSON／指紋／雜湊證據須一致；任一缺漏即不得宣稱完成正式放行。 |
| `SRC工具/src-column.html`、`SRC工具/src-column.js`、`SRC工具/src-column.css`、`SRC工具/core/src-column-core.js`、`SRC工具/core/src-column-h-section-catalog.js`、`SRC工具/core/src-column-oracle.js`、`SRC工具/core/src-column-rc-biaxial.js`、`SRC工具/core/src-column-shear.js`、`SRC工具/core/src-column-seismic-axial.js`、`SRC工具/core/src-column-seismic-detailing.js`、`SRC工具/core/src-column-weak-axis-shear-reference.js`、`SRC工具/src-column-h-section-catalog.test.js`、`SRC工具/src-column-rc-biaxial.test.js`、`SRC工具/src-column-shear.test.js`、`SRC工具/src-column-seismic-axial.test.js`、`SRC工具/src-column-seismic-detailing.test.js`、`SRC工具/src-column-core.test.js`、`SRC工具/src-column-oracle.test.js`、`SRC工具/src-column-page.contract.test.js`、`SRC工具/src-column-browser-smoke.test.js`、`SRC工具/src-column-traceability.catalog.json` | 正式工具納入、公開入口 `/src-column` | V1.0 支援完全包覆矩形 SRC 柱、置中雙對稱 H 型鋼骨、P-M／P-M-M、第 9.3 節及選定方向的第 8.4.2、9.6.1、9.6.2、9.6.3 節子檢核。單向案件 schema v1、雙向案件 schema v1；舊 V0.3～V0.8 案件經重算產生新指紋後遷移。Y 向鋼骨只有在專案明確採用 ANSI/AISC 360-22 G6 且確認無扭轉時由 production 依兩片翼板與 Cv2 計算；未採用的外部對照只留在 HTML。catalog schema v17 以 `attachmentResponsibility` 分開本構材附件、主文／專案引用輸入及全構架／接頭區／接合細部另案附件，並以 `materialProvenancePolicy` 分開官方教材斷面性質、專案指定設計材料、現況供貨與專案材證。計算書只列採用來源、公式、結果與結論；受控 catalog 斷面 `formalApprovalAllowed=true`，預設內部審閱，勾選核可後為正式附件；人工斷面可計算與列印但核可失敗封閉。正式清冊、獨立基準、rendered-delivery 當輪 PDF／案件 JSON／指紋／雜湊或 Pages 公開路由任一缺漏皆阻擋 release。 |
| `結構工具箱/tools/independent-engineering-adapters/src-beam.js` | 正式基準納入 | `/src-beam` 以 catalog v2 正式基準登錄；4 組案例、41 項斷言由 production 二分法結果與獨立二次方程 oracle 交叉核對。基準失敗會阻擋獨立工程基準 gate，且不得以 golden regression 或同核心重播替代。 |
| `結構工具箱/tools/independent-engineering-adapters/src-column.js` | 正式基準納入 | `/src-column` 以 1 組官方教材斷面案例、38 項斷言鎖定公開頁接線與 production 核心；獨立 oracle 不讀 production catalog 或 golden answer，改由矩形斷面幾何、材料彈性模數、剛度分配、寬厚比、受壓及壓彎互制的封閉式關係重算。 |
| `stone-feedback.contract.test.js` | 納入 | 石材 V2、舊版與 baseline capture feedback 合約；確認通知、確認、範本管理與錯誤提示不回退原生 dialog。 |
| `石材固定/stone-report.contract.test.js`、`石材固定/server_smoke_test.py`、`石材固定/石材計算書產生器_規範版V2.html` | 納入 | 石材報告邊界契約、離線匯出 smoke 與正式 V2 操作頁；固定檢查 Word/PDF 匯出稽核摘要、PDF / DOCX 文字抽檢、頁面專用閱讀狀態清理與 payload HTML 不一致時的降級判定，避免 page-only 閱讀狀態混入正式交付。V2 主頁共用 `結構工具箱/core/direct-print-boundary.css`，瀏覽器功能表直接列印只能顯示一頁邊界通知，既有 `#printout` 不得重新顯示；預覽列印、PDF 與 Word 維持獨立報表輸出。 |
| `石材固定/stone-traceability.catalog.json`、`石材固定/stone-traceability.contract.test.js`、`石材固定/js/code-profiles-registry.spec.js`、`石材固定/js/code-profiles-registry-smoke.test.js`、`石材固定/js/regression-smoke.test.js` | 納入 | 石材 V2 條文語意追蹤 catalog 與契約測試；集中確認風力 / 耐震需求、錨栓與連接件、石材板塊與孔位、使用性與交付稽核的規範來源、輸入、計算、報告、證據與人工複核邊界。active profile 切換另由 `PROFILE_INPUT_BINDINGS` / `buildProfileInputUpdates()` 同步仍沿用舊 profile 預設的 ap、Rp 與外牆 Ip 欄位，保留人工覆寫；registry smoke 與 calc-core regression 必須證明 profile 不只改 metadata，也改變代表案例的實際需求值。 |
| `struct-dx.contract.test.js` | 納入 | struct.dx 前端 feedback 合約；確認 diagnosis、verify engine、struct suite 三頁都有 action status outlet 與 helper，且公開頁只採本機系統字型堆疊，不依賴 Google Fonts 或其他執行期外部字型。 |
| `decking-tools.contract.test.js` | 納入 | 覆工板前端 feedback 與主頁列印邊界合約；確認套用、匯出與 reset 確認維持頁內狀態與受控確認流程，瀏覽器直接列印操作頁只能顯示邊界通知，只有「列印彙整計算書」可暫時啟用彙整報告列印模式。 |
| `覆工板/decking-result-replay.js`、`覆工板/decking-report.contract.test.js`、`覆工板/report/gen_report.py`、`覆工板/test-fixtures/report-smoke.json` | 納入 | 覆工板 JSON 重算與 Word / PDF 報告邊界契約、報表產生器及固定 smoke fixture；六組輸入會執行目前頁面計算核心並核對 31 項結果，確認 page-only 附件閱讀狀態與列印流程說明只留在頁面，專用 PDF 列印模式只顯示彙整報告，Python 產出的 `.docx` 經 `word/document.xml` 文字抽檢後保留案名/編號/日期、計算指紋與計算章節，但不混入頁面專用提醒。正式 release 會把當輪來源 JSON、DOCX 與結構摘要寫入 `decking-formal` 證據目錄。 |
| `螺栓檢討/anchor-report.contract.test.js`、`螺栓檢討/bolt-review-tool/src/documentApproval.ts`、`螺栓檢討/bolt-review-tool/src/reportHtmlSeal.ts`、`螺栓檢討/bolt-review-tool/tests/reportArtifacts.test.ts`、`結構工具箱/tools/anchor-html-seal-verifier.js` | 納入 | 錨栓報告邊界與正式產物契約；包裝 `bolt-review-tool` 內報告、附件狀態與正式產物測試，確認頁面專用的附件閱讀狀態與產報前檢查不混入計算書、列印 PDF 或 workbook/docx。正式核可只能在工作頁完成；計算內容或公司、案號、設計／校核人、發行日期、輸出模式、LOGO 等成品設定異動會撤銷舊核可，匯出留痕、衍生快照與儲存時間則不誤觸撤銷。匯出 HTML 為只讀附件身分，瀏覽器與獨立檢查器分別重算內容／核可封印，正文或核可資料竄改都會阻擋。release 當輪會把實際 HTML、DOCX、XLSX 寫入 `anchor-formal` 供總閘門重新解析。 |
| `開挖擋土支撐/excavation-report.contract.test.js`、`開挖擋土支撐/backend/app/pdf_render_evidence.py`、`開挖擋土支撐/backend/tests/test_pdf_render_evidence.py`、`開挖擋土支撐/backend/tests/test_report_delivery_api.py`、`開挖擋土支撐/backend/tests/test_reporting.py`、`開挖擋土支撐/backend/tests/release_report_artifacts.py` | 納入 | 開挖擋土支撐報告邊界契約、核可 PDF canonical evidence、單一 ZIP 來源套件、下載 API 邊界、Python 報表測試與正式放行成品產生器；確認 PDF 正式版與 Word 編修版共用同一套報表文字、下載邊界與 latest report 邏輯。核可 PDF 逐頁渲染並核對像素、OCR、文字層與檔案 SHA-256，再原子建立且回讀只含 PDF／證據的搬運 ZIP；證據只進附件包內部追溯區，ZIP 不是正式附件包。release 當輪另在 ignored 的 `excavation-formal` 保存 PDF、DOCX 與 latest download 副本，供總閘門重新解析並核對雜湊。 |
| `覆工板/decking-traceability.catalog.json`、`覆工板/decking-traceability.contract.test.js` | 納入 | 覆工板條文語意追蹤 catalog 與契約測試；集中確認 HS20 / PC400 / 吊車載重、覆工板面 / 小梁 / 大梁彎剪撓度、大梁 Pu、共構柱、握裹、樁基、JSON / Word 報表與施工臨設邊界的規範來源、輸入、計算、報告、證據與人工複核邊界。 |
| `section-tools.contract.test.js`、`dev_tools/section-properties-browser-smoke.js`、`結構工具箱/tools/analysis-section-tool-metadata.js`、`結構工具箱/core/ui/report.js` | 納入 | 根目錄斷面工具 feedback / 報告邊界合約、V2.1 真實瀏覽器重播 smoke 與共用計算書文件狀態；斷面性質與合成斷面的公開版本、案件 JSON 及內嵌計算引擎由共用 metadata 鎖定，計算書分列保留大寫 `V` 的「工具版本」及「計算引擎」。確認斷面性質、合成斷面與 RC 補強報表維持 inline status，並把 runtime HTML 轉成可讀文字檢查標題、主要章節、核可控制與 page-only wording 排除清單。斷面性質 V2.1 的實際入口需以本地 JSON 保存目前斷面、全部動態尺寸與結果選取，讀回後使用目前核心重算並核對計算指紋；結果選取也是附件內容，必須納入同一指紋。工作頁不得預先核可，計算書須先以可列印的內部審閱開啟，使用者只在預覽內明確核可後才成為正式附件；成品包含採用尺寸、計算結果、文件狀態、產出工具、版本、引擎、時間及同一指紋，且不得混入操作頁提示。下載的正式 HTML 須保存核可狀態與內容／核可雙封印；序列化前清除核可撤銷、驗證及下載等暫態視窗訊息，重新開啟時不得把它們當作附件正文。舊 `斷面性質計算.html` 只允許作為小於 5 KB 的相容導向，完整保留 query／hash 後進入 `index.html`，不得再保存第二套斷面公式；瀏覽器 smoke 必須以 `pickI` 舊書籤驗證導向與連續梁回填模式。合成斷面 V1.2 的版本化本地 JSON 需通過完整結果鏈重播，涵蓋輸入、A、Ix / Iy、Sx / Sy、Zx / Zy、rx / ry、Jopen / Jclosed 與計算書指紋。RC 補強斷面 V1.7 的 JSON 則需完整保存案件資料、目前頁籤及梁柱全部輸入，讀回後同時重算梁柱；實際結果與兩份計算書指紋由 RC 視覺 gate 鎖定。未知 schema、不完整欄位或不合法幾何必須在改動目前案例前拒絕。共用產生器預設輸出可列印的內部審閱，明確核可後成為正式附件；空白案件欄位可由主文繼承，NG 工程結果也不產生 DRAFT。操作頁直接列印仍只顯示一頁邊界通知，JSON 操作控制不進列印。 |
| `frame-analysis.contract.test.js`、`鋼架/平面剛架分析.html`、`結構工具箱/tools/analysis-section-tool-metadata.js` | 納入 | 平面剛架 JSON 重播結果鏈、報告邊界合約與頁面本體；公開工具版本及內嵌計算引擎由共用 metadata 供畫面、案件 JSON 與計算書使用，計算書分列「工具版本」及「計算引擎」。以雙案例載重門型剛架確認 JSON 重播後的模型、位移、反力、彈簧力、平衡結果、桿端力、M/V/N 圖與計算書指紋完全一致；另以懸臂端點力、簡支梁均佈載重、簡支梁跨中集中載重、固定－鉸支梁均佈載重、軸向桿加端彈簧、3–4–5 斜桿懸臂、固定柱腳門架側推及門架梁對稱均佈載重八條封閉式力學路徑，獨立核對位移、轉角、反力、端部釋放、彈簧分力、斜桿座標轉換、柱軸向變形、梁軸向約束與柱梁端矩。工作頁提供三案「標準驗證案例庫」，以相同 frozen reference 逐項顯示參考值、分析值與誤差；驗證面板以 page-only 邊界排除計算書、列印及附件。維持 V0.6、V0.5、V0.4、V0.3 及 V0.2 無 schema JSON 相容性，未知 schema 或重複節點編號必須在清空目前模型前拒絕。另確認附件閱讀狀態只留在頁面、`printReport()` 產出的 blob 計算書具備工具版本、計算引擎、輸出時間、計算指紋、必要分析章節與節點 / 桿件資料，且不帶 page-only wording。案件資料可留空，模型待確認項目仍如實呈現；文件預設內部審閱並可明確核可為正式附件。操作頁直接列印仍只顯示一頁邊界通知。 |
| `browser-dialogs.contract.test.js` | 納入 | 全專案瀏覽器原生 dialog 合約；掃描 HTML/JS/TS/TSX，要求正式工具不回退 `alert` / `prompt`，並讓 `confirm` 只能經審核路徑保留。 |
| `anchor/` | 納入部署資產 | GitHub Pages 倉庫子路徑與 Vercel `/anchor/` 共用此輸出；入口、service worker 與 `manifest.webmanifest` 必須使用 `./` 相對 base。hash asset 更新時舊檔刪除與新檔新增需一起提交，並同步 `anchor/deployment-manifest.json` 的 source fingerprint。 |
| `sync-anchor-deployment.ps1` | 納入 | 錨栓 React 工具的正式部署同步入口；以可攜式 `./` base build、受控清理 `anchor/assets`、複製 dist 並寫入部署 fingerprint manifest。來源 fingerprint 的相對路徑與檔案清單固定採 ordinal 規則，確保 Windows PowerShell 5.1 與目前 PowerShell 對中文路徑算出相同結果。 |
| `結構工具箱/tools/風力/*.html`、`結構工具箱/tools/風力/wind-shared-profile.js`、`結構工具箱/tools/wind-shared-profile.contract.test.js` | 納入 | 風力頁面共用報告路徑與耐風案件資料派送屬於平台可用性修正；`wind-shared-profile.v1` 只派送案件識別、場址條件及具明確工程對應的主結構幾何，標的物專用高度與尺寸不得由建築物總高誤填，並由 formal static contract 與瀏覽器 smoke 驗證真正跨頁預填。 |
| `鋼構工具/audit-tool.ps1`、`鋼構工具/steel-audit-browser-runner.js` | 納入 | 鋼構自巡檢改由單一 Edge CDP browser runner 執行 21 個實頁快照，減少 Playwright CLI 往返；runner 逐情境 timeout，且 abort 會落 `audit-status.json` 供 preflight 判斷。直接列印邊界檢查會在最多 5 秒內等待共享 stylesheet 實際套用並讓通知在螢幕隱藏後才判讀，避免 load event 與樣式計算的短暫競態；逾時後仍照原條件失敗，不放寬列印邊界。 |
| `石材固定/` | 納入程式碼與必要離線 vendor，排除參考資料與輸出 | 已有 self_check、quick smoke、報告邊界契約、版本治理、golden samples、條文語意追蹤與交付流程；納入正式 V2 工具、測試、治理文件、必要 vendor，PDF/XLS/Word 範例、圖片、專案報告與舊版 HTML 不進 repo。 |
| `開挖擋土支撐/` | 納入程式碼，排除工程資料 | Backend tests、frontend build、launcher 靜態 smoke、report boundary contract 與 traceability contract 已納入 preflight；只提交 `index.html`、backend/frontend 原始碼、requirements、設定、README、啟停腳本、traceability catalog / contract 與本目錄 `.gitignore`，不提交工程案例、Office/PDF、分析輸出、`app_data/`、`tmp/`、`frontend/dist/`。產生的 `.canonical-render.evidence.json` 與 `.formal-source.zip` 是案件內部追溯／搬運資料，不提交 repository，也不發布 Pages。 |
| `覆工板/` | 納入程式碼，排除工程資料與輸出 | 已有 `index.html`、Python 報告產生器、固定 smoke fixture、條文語意追蹤與 preflight Word 報告邊界契約；Excel、PDF、doc/docx、抽圖、dump 與吊車參考資料不進 repo。 |
| `鋼架/` | 可納入 | 目前以 `frame-analysis.contract.test.js` 治理 `平面剛架分析.html` 的頁面 / 計算書邊界與 runtime 產報 smoke；若後續納入更多剛架頁面，再補資料夾級 manifest 或 catalog。 |
| `結構工具箱/tools/foundation/` | 納入 | 基礎局部檢核為靜態快算工具，已拆 `foundation-local-core.js`、`foundation-local-golden-cases.js` 與 `foundation-local-core.test.js`，preflight 會跑 golden regression。注意不要改放到 `tools/基礎/`，會被根目錄 `基礎/` ignore 規則誤排除。 |
| `結構工具箱/tools/equipment/` | 納入 | 設備局部荷重為靜態快算工具，已拆 `equipment-load-core.js`、`equipment-load-golden-cases.js` 與 `equipment-load-core.test.js`；preflight 會跑平均分配及矩形四支點偏心反力、反力與力矩平衡、無拉力界線、接觸壓 / 分布壓及最大支承反力 regression。偏心模式限相同垂直剛度與剛性設備底座，負反力不得由本頁冒充錨栓拉力設計。 |
| `結構工具箱/tools/earth/` | 納入 | 擋土土壓局部快算為靜態初估工具，已拆 `earth-pressure-core.js`、`earth-pressure-golden-cases.js` 與 `earth-pressure-core.test.js`，preflight 會跑 Rankine／Coulomb、分層土、水壓與簡化穩定 regression。`earth-pressure-rc-bridge.js` 只接受 V0.6 懸臂式主動土壓 JSON，必須以同版核心重算並逐值核對 schema、logicSignature、合力、傾覆矩與被動抵抗後，才可把服務側向荷載送入 RC 基礎頁；靜止土壓、重力式牆、地震 M-O 或不相符結果一律在改動 RC 輸入前失敗封閉。RC 基礎頁會以自身幾何與永久重量重建線性基底反力，對簡單水平 Rankine 模型計算趾版底層及踵版頂層的 Mu、Vu、As、φMn、φVc；含地下水、分層回填、Coulomb 垂直分力或扶壁式時不得自動產生底版 OK。來源基底寬／垂直重量不取代 RC 幾何，計算書只列實際採用的土壓來源、荷載與簽章。 |
| `結構工具箱/tools/local-quick-tools.manifest.json`、`結構工具箱/tools/local-quick-tool-metadata.js`、`結構工具箱/core/ui/report.js`、`結構工具箱/core/direct-print-boundary.css` | 納入 | 高頻局部快算工具清冊、公開版本唯一來源、共用內部審閱／正式附件核可模型與工作頁直接列印邊界；公開工具版本與 `Core.version` 計算引擎分開追溯，頁面、案件 JSON 與計算書均由同一版本來源取值；清冊集中記錄路由、入口檔、核心檔、golden regression、smoke 標記、共用 stylesheet、body class、通知 class 與必要文字，避免新增工具時漏改契約、文件狀態、列印邊界與 preflight。 |
| `結構工具箱/tools/independent-engineering-benchmarks.catalog.json`、`結構工具箱/tools/independent-engineering-benchmarks.js`、`結構工具箱/tools/independent-engineering-benchmarks.test.js`、`結構工具箱/tools/independent-engineering-adapters/rc-column-pm.js`、`rc-beam-strength.js`、`rc-shear-wall-strength.js`、`rc-wall-strength.js`、`rc-retrofit-section.js`、`rc-foundation.js`、`rc-pile.js`、`steel-beam-asd.js`、`steel-column-asd.js`、`steel-plate-connection.js`、`steel-formal.js`、`decking.js`、`stone-fixing.js`、`rc-slab-strength.js`、`wind-force-mwfrs.js`、`wind-object-solid-table210.js`、`wind-cc.js`、`wind-parapet.js`、`wind-open-roof.js`、`wind-object-frame.js`、`wind-lattice-tower.js`、`wind-object-tower.js`、`wind-fence-sign.js`、`wind-sign-pole.js`、`seismic-force-static.js`、`seismic-appendage.js`、`seismic-misc.js`、`anchor-cast-in.js`、`src-beam.js`、`src-column.js` | 納入 | 獨立工程基準目錄、執行器、production adapter 與負向測試。預期值須由明列的封閉式力學關係獨立推導，不得讀取 production golden case 的預期答案；golden case 與同核心重播不等同獨立工程驗證。目前 33 / 33 個正式入口通過，代表設備荷重、土壓、基礎局部、RC 柱 P–M、RC 梁正負撓曲與耐震剪力、RC 特殊結構牆軸彎／剪力／特殊邊界構材／施工縫剪摩擦、RC 一般牆承重／非承重／地下室／結構牆厚度、Wall Pier、完整 P–M、簡式失敗關閉、面內剪力、配筋與地下室側壓、RC 梁柱補強斷面之 CFRP／鋼板補強、脫層、轉換斷面、剪力上限、圍束軸壓與 P–M、RC 獨立基腳強度、RC 單樁／群樁反力／樁帽強度、正式鋼梁 ASD、正式鋼柱 ASD、鋼構連接板 LRFD／ASD 板件強度與螺栓孔細部、正式風力 MWFRS、實體標示物表 2.10 雙 Cf 路線與斜風向作用、正式區域風壓 C&C 高度／內壓高度／屋面角隅控制分支、正式女兒牆 MWFRS／圖 3.4／圖 3.5 三條路線、正式開放式屋頂單斜／雙斜、有／無阻擋、面積分帶、坡度內插及高度切換、正式中空式／格子式表 2.11 構材分類、實體率分帶與底部作用、正式格構式高塔表 2.15 四段係數、構材／斜風向修正與分段力流、正式煙囪／水塔表 2.12 代表斷面、h/D 邊界、圓形 D√q(z) 分流、形狀修正、頂部附加物與分段力流、正式圍牆／標示物表 2.10 地面／高架雙路線、界限夾制、內插、專案 C_f 採用值、實體率修正與底部作用、正式獨立式招牌／燈桿表 2.10 面板、表 2.14 圓管門檻、表 2.13 角柱四個細長比區間、分段支柱力流及組合底部作用、正式地震力等值靜力、附屬構造物地震力三種控制分支、雜項工作物式(2-3)／式(5-1)／式(5-2)三條路徑、正式錨栓第 17 章拉剪互制、一向 RC 板板厚／正負彎矩／配筋／單向剪力、鋼構正式主工具連接板／拉力構件、覆工板面／大小梁／共構柱／握裹／樁基，石材固定背扣／插銷完整受力鏈，以及包覆型 SRC 梁撓曲、寬厚比、鋼骨／RC 剪力分擔與剪力上限，以及包覆型 SRC 柱斷面幾何、寬厚比、剛度分配、鋼骨受壓與壓彎互制共三十三條路由，不得擴張宣稱；鋼構連接板正式頁只涵蓋板件總斷面降伏、有效淨斷面斷裂、區塊剪力及螺栓孔配置細部，未涵蓋螺栓剪力與孔壁承壓強度。既定 P0／P1 基準路由已完成。分析摘要身分的 `/seismic-dynamic` 不計入正式附件入口覆蓋。RC 板基準必須走 `鋼筋混凝土/shared/slab-evaluator.js` 與 `flexure.js`，鎖定 `slab.html` 正式頁接線，並獨立核對一向板板厚、正負彎矩、溫度筋、撓曲強度及單向剪力；雙向簡化條帶法與無梁板衝剪不在本基準宣稱範圍。RC 梁基準必須走 `鋼筋混凝土/shared/flexure.js` 與 `beam-evaluator.js`，並獨立核對拉控撓曲、As,min、耐震 Ve 控制與 Vc 歸零；RC 剪力牆基準必須走 `鋼筋混凝土/shared/pmsection.js`、`wall-base.js` 與 `wall-evaluator.js`，並獨立核對 140 點 P–M 封包、耐震剪力 φ、雙層配筋、特殊邊界構材及施工縫剪摩擦；RC 一般牆基準必須走 `鋼筋混凝土/shared/pmsection.js`、`wall.js` 與 `wall-inplane-evaluator.js`，鎖定 `wall.html` 正式頁接線，並獨立核對六組厚度、Wall Pier、P–M、簡式容量、剪力、配筋及地下室側壓雙模型；RC 補強斷面基準必須直接執行 `RC補強斷面性質.html` 正式頁的純計算函式並鎖定梁柱頁面接線，獨立核對五組 CFRP／鋼板撓曲、轉換斷面、剪力與柱 P–M；錨固、接著面、延性及施工細節不在本基準宣稱範圍；RC 基礎頁的獨立基腳強度必須走 `鋼筋混凝土/shared/foundation-isolated.js`；單樁一般工程模式的軸向承載、基礎頁群樁反力及樁帽強度必須走 `鋼筋混凝土/shared/foundation-pile.js`。正式鋼梁與鋼柱基準必須走 `鋼構工具/core/materials/steel.js`；鋼梁獨立核對 F2 非彈性側向扭轉挫屈、G2 剪力及均布載重撓度，鋼柱獨立核對非細長 H 型斷面弱軸壓桿控制、傳統 ASD 容許壓應力及完整雙向壓彎互制；鋼構連接板基準必須走 `鋼構工具/calculator.js` 並以 `plate-check.html` 鎖定正式頁接線；鋼構正式主工具基準必須以 `index.html` 與 `app.js` 鎖定連接板／拉力構件分支及報告接線，並獨立核對規則孔群、螺栓剪力、孔壁承壓、區塊剪力、雙側縱向填角銲及全滲透開槽銲；偏心栓群、拉剪互制、prying action、混合銲道、耐震接頭與施工細節不在本基準宣稱範圍。覆工板基準必須直接執行 `覆工板/index.html` 正式頁的六段計算函式與 `覆工板/shared/h-section-table.js` 斷面表，並以不同實作獨立核對一般載重與長柱／未側撐重載兩案的三工況反力、彎矩、撓度、容許應力、柱壓彎互制、握裹及砂層樁基承載；吊車配置、非均質地層、施工階段及未登錄斷面不在本基準宣稱範圍。石材固定基準必須直接執行 `石材固定/js/constants.spec.js` 與 `calculator.spec.js`，並以正式頁載入標記鎖定計算路徑；獨立核對背扣／插銷兩案的自重、風震、有效固定點、錨栓服務值與群組邊距折減、5/3 拉剪互制、角鋼、石板局部、層間變位與熱伸縮；未登錄石材、固定件、錨栓產品與施工細節不在本基準宣稱範圍。正式風力與實體標示物基準必須走 `結構工具箱/core/loads/wind.js`；MWFRS 獨立核對剛性三層矩形建物之地況 C 速度壓、陣風因子、X/Y 雙方向逐層順風力、基底剪力、傾覆力矩、簡化橫風力與扭矩，實體標示物另須鎖定正式頁 `H_o`、`M/N`、`b_w` 分工，獨立核對表 2.10 雙內插路徑、規範與專案採用值分離、基底作用及 `0.3b_w` 斜風向偏心；區域風壓 C&C 須鎖定 `wind-cc.html` 正式頁對 `Wind.calcCC` 的接線，並獨立核對 h=18 m、h>18 m 部分封閉及高層屋面區域 3 三條控制分支；女兒牆須鎖定 `wind-parapet.html` 對 `calcMwfrsParapet`、`calcParapetCcCases` 及 `calcSingleRoofParapetCcCases` 的正式頁接線，並獨立核對式 2.3、圖 3.4 與圖 3.5；開放式屋頂須鎖定 `wind-open-roof.html` 的高度切換與 `calcOpenRoofCC` 正式頁接線，並獨立核對圖 3.3(a)／(b) 的四種屋頂／阻擋組合、三種有效面積帶及坡度內插；中空式／格子式風力須鎖定 `wind-object-frame.html` 對速度壓、`lookupPorousFrameCf`、等效寬度陣風因子及最終風力式的正式頁接線，並獨立核對表 2.11 圓形低／高 D√q(z) 與平邊構材三條路線、三種實體率帶及底部作用；格構式高塔須鎖定 `wind-lattice-tower.html` 對 `calcLatticeTowerWind`、最高段選取與 Kz 顯示的正式頁接線，並獨立核對表 2.15 四段基本係數、圓形構材／方形塔斜風向修正及逐段剪力、彎矩與合力高度；煙囪／水塔須鎖定 `wind-object-tower.html` 對 `calcTowerWind`、最高段選取與頂部高度 Kz 顯示的正式頁接線，並獨立核對表 2.12 六組代表路線、h/D 1～25 邊界、圓形 D√q(z) 自動分流、形狀修正、頂部附加面積沿用／指定 C_f 及逐段底部作用；圍牆／標示物須鎖定 `wind-fence-sign.html` 對 `lookupSignCf`、`calcGustRigid` 與最終風力式的正式頁接線，並獨立核對表 2.10 地面／高架路線、低高界夾制、內插、專案 C_f 覆寫、實體率修正及基底剪力／彎矩；獨立式招牌／燈桿須鎖定 `wind-sign-pole.html` 對面板、圓管、角柱及組合底部作用的正式頁接線，並獨立核對表 2.10、2.13、2.14、D√q(z)=1.70 分流、R 四區間與分段積分；正式地震力、附屬構造物與雜項工作物地震力基準必須走 `結構工具箱/core/loads/seismic.js`，等值靜力獨立核對地盤係數內插、設計週期、反應譜分段、VD／V*／VM 控制、頂部附加力與八層樓層力分配，附屬構造物鎖定 `seismic-appendage.html` 正式頁接線並獨立核對 Rpa、Cph 計算值／上下限控制及一般／近斷層 Fpv，雜項工作物鎖定 `seismic-misc.html` 委派共用核心並獨立核對式(2-3)、式(5-1)、式(5-2)及一般／近斷層垂直力；正式錨栓基準必須以專案既有 TypeScript 編譯器直接載入 `螺栓檢討/bolt-review-tool/src/calc.ts`，獨立核對鋼材拉剪、混凝土拉破／剪破、拉出、撬出及 17.8 拉剪互制。聯合基腳與樁帽配筋反算必須使用 `Mu_kgcm` 介面並取回 `.As`，非有限配筋需求須失敗關閉。基準結果屬內部品質證據，只進成熟度、preflight 與頁面診斷，不進計算書、列印或正式附件。 |
| `結構工具箱/tools/construction-stage-load-handoff.js`、`結構工具箱/tools/construction-stage-load-handoff.test.js`、`結構工具箱/tools/建立覆工板施工階段荷重交接檔.bat` | 納入 | 覆工板至開挖共構柱的施工階段控制軸力交接。產生器只接受完整覆工板 JSON，拒絕重複 JSON key，並以目前 `覆工板/index.html` 核心重算 `Pu1 / Pu2 / Pu3 / PuMax`、核對來源計算指紋後才產生 `construction-stage-decking-load-handoff` v1；交接檔以 `CSH-` 指紋封印來源工具／版本／計算指紋、tf 單位、控制軸力與控制工況。開挖前端以固定 `column_id` 逐柱明確匯入，每柱最多 20 個具唯一名稱、階段 ID 與來源指紋的階段，不得自動套用或沿用來源核可狀態；舊案單一荷重可遷移成一個 100% 階段。來源控制軸力保持不可改寫；同一交接指紋於所有啟用柱位的反力分配比例合計必須為 100%，多柱時逐柱分配依據必填且階段名稱一致，後端再以來源軸力乘比例作為各柱 Np。覆工板來源不含開挖柱座標方向；階段附加 X／Y 偏心只能由設計者在開挖端另行明確勾選、輸入帶正負號數值與採用依據，後端以 `ΔMx = Np × Δex`、`ΔMy = Np × Δey` 組合最終彎矩。分配不完整、未勾選卻夾帶偏心、勾選後零偏心或缺採用依據均失敗關閉。專案狀態保存原始交接記錄，Python 後端再獨立重算 `CSH-`，核對來源 Np、來源摘要、目標柱位、重複階段及重複來源。每階段連同零施工構台荷重基準案完整計算，柱互制、基礎壓入及拉拔分別取最不利控制階段。計算書只保留來源軸力、分配比例與依據、本柱採用軸力、附加偏心與依據、衍生／最終彎矩、三種控制階段及精簡來源追溯；匯入操作、警告及邊界說明留在頁面。比例合計檢查不證明比例的工程正確性；座標方向、支撐內力變化、二階效應及正式工程核可仍屬人工複核邊界。 |
| `結構工具箱/tools/local-quick-tools.run.js` | 納入 | manifest-driven 局部快算測試入口；由清冊呼叫 JSON 匯出 helper regression 與共同契約，preflight 以此作為局部快算總閘門。 |
| `結構工具箱/tools/local-quick-export.js` | 納入 | 高頻局部快算共用 JSON 匯出 helper；集中處理 payload 組裝、非有限數值序列化與下載動作，避免各頁匯出格式分歧。 |
| `結構工具箱/tools/local-quick-export.test.js` | 納入 | JSON 匯出 helper 的 Node stub regression；驗證 payload、預設檔名、MIME、click/remove/revoke 與 Infinity/NaN 序列化，不依賴瀏覽器下載事件。 |
| `結構工具箱/tools/project-storage.test.js` | 納入 | 共用專案欄位儲存 helper 的 Node regression；驗證 `未填` placeholder 會在 collect/apply/build payload 前被正規化為空字串，並確認專案名稱、編號、設計者欄位會先 trim 再進入跨工具 JSON / 報表 metadata。preflight JS 執行檔清冊需固定列入此測試，避免 project storage 與頁面報表的 placeholder 清理邏輯漂移。 |
| `結構工具箱/tools/project-meta-profile.js`、`結構工具箱/tools/project-meta-profile.test.js` | 納入 | 34 個具標準案件表頭的工作頁以 `tool-project-meta-profile-library.v1` 保存最多 20 個案件，並相容承接舊版單一 latest profile；有計畫編號時以正規化編號更新同案，否則以案名、最後才以設計者辨識，超過上限只移除最舊資料。案件可依三個表頭欄位搜尋，並依成功套用時間、再依儲存時間排序；搜尋與單純選案不得更新使用時間。封存必須可復原、預設隱藏，且封存 profile 在解除封存前不得套用；本機 `tool-project-meta-profile-view.v1` 只可記錄封存識別與最近使用時間，不得改寫 profile、工程輸入、附件核可或計算書。選案只切換待套用來源，不得改目前頁面；只交換計畫名稱、編號與設計者，永不交換尺寸、荷重、材料、計算參數或核可狀態。套用必須由使用者明確點選；目標頁已有不同的非空白值時，第一次點擊只能顯示衝突且維持零寫入，第二次針對同一來源與未變更目標明確確認後，才可覆寫並觸發既有 input/change 失效機制。確認前若來源或目標值改變，舊確認即失效；空白來源保留目標頁原值，三欄皆空白時不得取代既有清單。案件識別更新必須以所選 profile 與目前頁面三欄產生零寫入預覽，再次確認才可更名；須遷移封存與最近使用狀態。新識別已存在時必須顯示兩案差異，安全預設保留既有目標案件，並讓使用者明確選擇是否採用目前頁面表頭；確認後只保留一筆，清單或頁面在預覽後變更均須使確認失效，不得直接覆寫或留下重複案件。封存與刪除只作用於明確所選 profile，均不改目前頁面；刪除需同步清除該 profile 的本機管理標記。可攜備份固定使用封閉的 `tool-project-meta-profile-backup.v1` JSON、256 KiB 上限與精確欄位白名單；不得包含工程輸入、核可狀態、封存標記或最近使用時間，作用中與封存 profile 的三欄內容均可備份，匯入新增 profile 預設為作用中。匯入對本機缺少案件列為新增、同案相同內容列為略過、同案不同內容列為逐案差異選擇，預覽與第二次確認前維持零寫入；預覽後本機清單改變、重複識別、筆數不符或合併後超過 20 筆均失敗封閉，不得靜默淘汰本機資料。控制列為 page-only 且列印隱藏，表頭空白仍由主文承接，不降低附件狀態。 |
| 同上（來源辨識與永久刪除安全） | 納入 | HTML 控制列需顯示所選 profile 的儲存時間、來源工具及版本，但不得把這些管理資訊寫入計算書。永久刪除第一次點擊必須零寫入並明示不可復原及封存替代方案，第二次只可在所選 profile 與完整 library signature 均未改變時執行；選案、搜尋、顯示封存、封存／解除封存、套用、儲存、匯入、匯出或底層清單改變均須撤銷舊確認。刪除完成後只移除所選 profile 及其本機 view 標記，不得改目前頁面、工程輸入、附件核可或計算書。 |
| 同上（同案匯入差異解析） | 納入 | 備份與本機同一 profile identity 只比較允許的 `name`、`no`、`designer` 三欄；相同內容列為 identical，不得假造衝突。不同內容需在 page-only 面板逐欄顯示本機與備份值，逐案選擇 `local` 或 `backup`，預設固定為 local。任何選擇在整批確認前均須零寫入；確認時需重驗原本完整 library signature，已變更即失敗封閉。採用 backup 只可替換同 identity 的完整表頭 profile，不增加筆數、不更改本機封存／最近使用狀態，也不得觸及目前頁面、工程輸入、附件核可或計算書；未知案件或非法選項必須拒絕。 |
| `結構工具箱/tools/attachment-package-check.js`、`結構工具箱/tools/attachment-package-check.test.js`、`結構工具箱/tools/檢查附件組包.bat`、`結構工具箱/tools/attachment-package-build.js`、`結構工具箱/tools/attachment-package-build.test.js`、`結構工具箱/tools/建立正式附件包.bat`、`結構工具箱/tools/attachment-package-verify.js`、`結構工具箱/tools/attachment-package-verify.test.js`、`結構工具箱/tools/驗證正式附件包.bat` | 納入 | 本機案件附件組包一致性檢查、驗證後正式組包與事後完整性驗證。檢查器讀取 PDF、DOCX、XLSX、JSON、HTML 與文字附件，略過證據中間檔與系統雜項，未支援格式逐一列名為需人工確認。每份來源在內容解析前後需取得相同 SHA-256，檢查期間變更即 blocked；組包複製層還需讓檢查完成、複製前、複製後與目標檔案雜湊四者一致，避免通過核可檢查後被重新輸出或替換。來源資料夾本身或內容含符號連結、Windows junction 或其他特殊項目時需列名並 blocked，避免靜默遺漏或帶入資料夾外內容；組包複製層另逐層拒絕連結並以實際路徑確認仍在來源根目錄內。來源 JSON／計算書指紋不符時阻擋，多份同指紋來源或輸出要求人工確認；page-only 診斷文字混入交付物亦阻擋。`文件狀態：內部審閱` 阻擋正式組包；`文件狀態：正式附件` 還需同時抽取有效核可時間，缺少或日期無效一律 blocked。輸出時間無效時維持 review，核可早於輸出時 blocked，同一顯示秒允許；v3 事後驗證器需在指紋之外獨立重做相同時間檢查，並要求清單建立時間是具時區格式且不早於全部正式附件核可時間，完整時間鏈固定為「輸出 ≤ 核可 ≤ 組包」。計畫名稱、編號與設計人可由主文承接。缺少產出工具、版本、輸出時間或指紋仍維持 review。組包器只接受 `ready` 且至少一份明確正式附件，將交付文件放入 `01_正式附件/`，來源 JSON、SHA-256 清單與附件包指紋隔離於 `99_內部追溯_勿附入主報告/`；review / blocked 不建立輸出，既有輸出不覆寫。新建包採 v3 指紋，綁定檔案雜湊、建立時間、計畫編號、產出工具、版本、輸出時間、正式核可時間、計算指紋、檢查摘要與正式／內部分流邊界；既有 v1／v2 包仍依各自原規則驗證完整性，但固定回傳 review／CLI 1，只有 v3 可自動 ready／CLI 0，任一版本的完整性錯誤都以 blocked／CLI 2 優先。組包器在暫存區呼叫同一事後驗證器完成發布前自我驗證，完整吻合才原子更名；驗證失敗必須清除暫存且不得出現正式輸出。事後驗證器本身不修改附件包，逐檔核對大小、SHA-256、附件包指紋、README、路徑與清單外項目；清單內每一層 JSON 物件的欄位名都必須唯一，字面相同或以跳脫序列表示的同名欄位即使解析後指紋吻合仍固定 blocked，避免不同解析器採用不同值。頂層、檢查摘要、分流邊界與附件紀錄另採封閉白名單，任何未定義且未納入指紋的擴充欄位即使未影響指紋仍須 blocked。每個路徑另須採 NFC、避開 Windows 保留名稱與尾端空白／句點，再以不分大小寫的可攜式路徑鍵維持唯一；`A.pdf`／`a.pdf` 等碰撞即使重算出相符指紋仍固定 blocked。驗證入口的附件包根目錄本身不得是符號連結或 Windows junction，且需以原生檔案系統身分在結束前重驗，根目錄遭替換亦以 `package-changed-during-verification` 阻擋。清單、README、正式附件、追溯來源與目錄結構需在驗證結束前完成第二次快照，驗證期間任何新增、移除、替換或內容變更皆以同一狀態阻擋，避免雲端同步或重新輸出造成混合時間點結果。此完整性檢查用於偵測一般傳輸／整理變動，不取代數位簽章。三個 CLI 均採失敗封閉。 |
| `結構工具箱/tools/attachment-package-manager-worker.js`、`結構工具箱/tools/attachment-package-manager.ps1`、`結構工具箱/tools/啟動正式附件包管理器.bat`、`結構工具箱/tools/attachment-package-manager.contract.test.js` | 納入 | Windows 本機正式附件包操作介面。GUI 提供來源資料夾或 `.formal-source.zip` 選擇、選填計畫編號、可選輸出位置、狀態摘要、附件清單與問題明細；只有欄位原本空白且來源附件恰有一個一致的非空計畫編號時可建議帶入，來源空白仍可正式組包，衝突不得猜選，使用者輸入不得覆寫，建立前核心仍須重查。來源 ZIP 檢查 ready 且輸出欄位空白時可顯示原 ZIP 旁的不覆寫預計輸出位置，但不得建立資料夾；使用者輸出不得覆寫，來源切換只能清除仍等於舊自動建議的值。來源檢查與事後驗證必須以背景唯讀 worker 執行，UI 正常完成不得同步 `WaitForExit()`；進行中同一按鈕提供停止操作，取消、五分鐘逾時、路徑改變或關窗都需終止程序，依 worker PID 清理系統暫存區內受管 IPC 結果與來源 ZIP 隔離根目錄，且過期結果不得套用或形成組包權限。管理器須提供 `Ctrl+L` 聚焦目前模式路徑、`Enter` 只觸發來源檢查或事後驗證、`Esc` 停止唯讀 worker；Enter 在正式建立按鈕上也必須被攔截，組包仍要求明確點按。來源與驗證流程須有固定 Tab 順序、輔助名稱及權限描述。視窗須依啟動游標所在螢幕的 working area 限制尺寸並手動置中；800 × 640px 時，1040 × 784px 固定內容面必須透過獨立雙向捲動容器完整存取，狀態列固定於外層，`Ctrl+L` 聚焦時同步捲到路徑欄。來源區拖放只接受單一現有實體資料夾或精確 `.formal-source.zip`，驗證區只接受單一現有實體資料夾；多重路徑、一般檔案、來源 ZIP 誤放驗證區、連結與特殊項目必須保留原路徑並拒絕。合法拖入只能啟動對應背景唯讀 check / verify，不得啟動 build。正式建立須由獨立背景 worker 呼叫既有原子組包核心，UI thread 不得同步 `WaitForExit()`；開始前必須重驗一次性 ready grant，執行中鎖定全部輸入與其他動作、保持 message loop 可回應。核心僅能經系統暫存區受管 JSONL 回報準備來源、重新檢查、建立暫存包、發布前驗證、原子發布等真實階段；UI 只接受固定白名單並搭配實際經過時間，不得顯示 worker 任意文字或以無證據的百分比冒充進度。同時阻擋 Esc 取消及關窗。正式建立不可套用唯讀工作的取消或逾時路徑；worker 自然完成後才可套用核心結果、恢復 UI、清除一次性權限及結果／階段 IPC 與來源 ZIP 暫存。ZIP 只接受依序位於根目錄的同名 PDF／canonical evidence 兩檔，worker 由中央目錄先驗名稱、數量及逐檔大小，再將兩個指定內容寫入隔離暫存區，禁止路徑穿越、多檔、錯配與超限內容，且完成後一律清理。結構化 worker 必須直接呼叫既有 check / build / verify 核心，不得自行升級或降低 `ready / review / blocked`，且只有檢查結果 ready 時才在畫面啟用組包動作，組包核心仍會再次完整檢查。驗證既有正式附件包仍只接受資料夾；介面不啟動 HTTP 服務、不傳送案件資料、不寫入檢查或驗證結果至附件來源；只有使用者明確執行建立時，才由既有 v3 組包器建立新資料夾。四個檔案均屬私有本機工具，不發布至 Pages；PowerShell 檔須保留 UTF-8 BOM，`-Smoke` 只驗證 WinForms、Node 與 worker 可載入，不開啟視窗或變更案件資料；背景取消、鍵盤、viewport、drag-and-drop 及 build responsiveness smoke 必須在真實 WinForms message loop 證明程序與暫存清理、鍵盤／拖放不建立附件包、背景建立可回應、真實階段與經過時間持續更新但不可取消／關窗、完成後 UI 復原、結果／階段 IPC 清除，以及視窗在所選螢幕內、雙向捲動有效、右下內容及固定狀態列可見。 |
| `結構工具箱/tools/attachment-case-governance-viewer-worker.js`、`結構工具箱/tools/attachment-case-governance-viewer.ps1`、`結構工具箱/tools/啟動案件附件治理檢視器.bat`、`結構工具箱/tools/attachment-case-governance-viewer.contract.test.js` | 納入 | Windows 本機案件附件治理唯讀檢視器。單案模式必須直接呼叫 `attachment-case-governance-root.js`，多案模式必須直接呼叫 `attachment-case-governance-portfolio.js`，不得在 worker 或 GUI 另行升降 `ready / review / blocked`；P0／P1／P2、只顯示待處理案件與優先層級篩選只能改變畫面列數，不得改變整批狀態、退出碼或治理指紋。介面只顯示案件名稱、附件包／版本鏈狀態、待前進收據、既有問題與下一步，不建立、修改、核可、輸出或寫入案件資料，也不啟動 HTTP 服務。`ready` 只代表可進入內部歸檔複核，不是正式附件核可。四個檔案均屬私有本機治理資產，不發布至 Pages；PowerShell 檔須保留 UTF-8 BOM，`-Smoke` 不開啟視窗或存取案件。 |
| `結構工具箱/tools/attachment-package-upgrade-assistant-worker.js`、`結構工具箱/tools/attachment-package-upgrade-assistant.ps1`、`結構工具箱/tools/啟動舊版附件升級助手.bat`、`結構工具箱/tools/attachment-package-upgrade-assistant.contract.test.js` | 納入 | Windows 本機舊版正式附件兩步式升級助手。inspect 必須依輸入階段直接呼叫既有 `attachment-package-upgrade-assess.js` 或 `attachment-package-upgrade-workspace-check.js`，固定唯讀且不產生歷程收據；execute 必須先重做同一檢查，只有完整 v1／v2 舊包可另建安全工作區、完成度 ready 的工作區可另建 v3 包，並直接呼叫既有 `attachment-package-upgrade-flow.js`，不得在 worker 或 GUI 實作第二套複製、組包、核可或收據寫入。執行前須由同一路徑與計畫編號取得當次可執行授權並勾選明確確認；輸入或計畫編號變更立即撤銷。介面不提供原地升級、舊核可沿用、狀態人工提升、自訂不安全輸出或覆寫；輸出與外部內部歷程沿用核心安全預設，執行結果不是正式核可。四個檔案均屬私有本機治理資產，不發布至 Pages；PowerShell 檔須保留 UTF-8 BOM，`-Smoke` 不開啟視窗或寫入案件。 |
| 根目錄 `啟動案件附件工作台.bat`、`結構工具箱/tools/attachment-governance-hub-worker.js`、`結構工具箱/tools/attachment-governance-hub.ps1`、`結構工具箱/tools/啟動案件附件工作台.bat`、`結構工具箱/tools/attachment-governance-hub.contract.test.js` | 納入 | Windows 本機案件附件治理統一入口。根目錄捷徑只能把原始參數完整轉交既有受治理啟動器，不得直接呼叫 PowerShell、複製判定或建立較寬權限路徑。工作台可依使用目的手動啟動既有正式附件包管理器、案件附件治理檢視器或舊版附件升級助手，也可對使用者由啟動參數、對應選擇器或工作台內拖入的單一實體資料夾或 `.formal-source.zip` 執行唯讀工具建議；一般檔案、多重路徑、連結與特殊項目不得帶入，且自動建議不得啟動任何子工具。來源 ZIP 固定建議管理器 `source` 模式，由管理器既有安全解壓與來源檢查處理；hub 不得讀取 ZIP 內容、建立暫存區或自動組包。資料夾的新組包模式只接受報告型檔案或具有產出工具、版本、計畫編號、計算指紋等追溯訊號的附件作為正向辨識；一般 JSON、不支援檔案或特殊項目本身不得形成組包建議。worker 只能呼叫既有 `detectInputKind`、`assessUpgrade`、`scanCaseRoot`、`scanPortfolio` 與 `checkPackage` 唯讀核心，輸出建議工具及 `source / verify / case / portfolio / upgrade` 預填模式；不得另做治理狀態或權限判定，也不得呼叫組包、建立工作區、基準前進或任何寫入核心。只有建議仍對應目前路徑且使用者明確點擊建議工具時，才可傳入 `AutoInspect`，分別觸發管理器的 `check / verify`、檢視器的 `case / portfolio` 或升級助手的 `inspect`。三套 GUI 的自動檢查入口不得觸發 `build / execute`、確認勾選、建立、升級或核可；非建議工具仍只預填。介面必須固定揭露工程結果、附件完整性、治理 ready 與正式附件核可是不同層次。根目錄捷徑、四個工作台檔案與三套 GUI 均屬私有本機治理資產，不發布至 Pages；PowerShell 檔須保留 UTF-8 BOM，`-Smoke` 只驗證受治理入口與建議核心存在，不開啟視窗或存取案件。 |
| 根目錄 `serve-local.js`、`serve-local-browser-smoke.test.js`、`啟動斷面計算工具.bat`、`啟動螺栓檢討工具.bat` | 納入 | Windows 本機 Web 工具直接入口。`--route` 只接受 localhost 絕對路徑，啟動後仍由同一份 `vercel.json` rewrite / redirect 規則提供檔案；檔案型 clean route 會移除多餘尾斜線，真正資料夾目的地則保留尾斜線，避免相對 CSS、JavaScript 與頁內工具連結錯誤解析至根目錄。兩個批次入口分別固定前往 `/section` 與 `/anchor`，不直接以 `file://` 開啟模組化螺栓成品，也不要求先進入平台首頁。只有實體檔不存在時，部署清冊與三份 GSM 監控 JSON 的明列白名單才回傳帶 `X-Local-Optional-Resource: missing` 的 JSON `null`；任何其他缺檔仍回傳 404，不得用本機便利層掩蓋工具資源錯誤。瀏覽器 smoke 以桌機與手機實測 `/steel-formal/`、`/rc/`、`/section/`、`/anchor/` 與本機巡檢儀表板，實際由 RC 入口進入梁工具，並反向確認未知缺檔仍為 404。批次入口屬本機便利工具，依 Pages artifact 的 `.bat` 私有邊界不發布。 |
| `結構工具箱/tools/rendered-delivery-evidence.js`、`結構工具箱/tools/attachment-canonical-render-e2e.test.js` | 納入 | PDF 附件 canonical 可見性證據與實際 Edge 列印 E2E。`pdftotext` 僅可比對文字層 metadata，不得宣稱為可見內容或 OCR；自動放行只接受同次列印工作階段的 print-visible DOM（computed style、有效背景／對比、client rect）、PDF SHA-256、`pdftoppm` 逐頁像素／邊界指標，或真正由渲染頁面產生的 OCR／vision 證據。evidence 必須保存正規化 `visibleText.text`，由檢查器重算長度、SHA-256、文件 profile 與工程內容邊界，並與 PDF 的正式狀態、核可時間、產出工具、版本、輸出時間及計算指紋一致。Canonical PDF family summary 另須重讀實體成品與 evidence，保存成品 bytes、成品 SHA-256 及 evidence SHA-256；release 彙整再逐檔核對 evidence 與 summary 的雙重記錄。Canonical 正式報表的 33 組 PDF／evidence 必須由各產出 audit 先記錄 bytes 與 SHA-256，release 彙整層再以安全相對路徑、非連結及穩定讀取逐份核對，共 66 份全數通過；RC 正式與補強視覺 smoke 則另以 33 組 PDF／整頁 PNG、共 66 份成品獨立核驗；任一總閘門採用的代表 PDF 也必須能回連原始 audit，且同大小視覺檔替換的負向 fixture 必須失敗關閉。石材、錨栓、覆工板與開挖的混合格式產出端亦須先記錄實體 PDF／HTML／DOCX／XLSX／audit JSON／latest download 的 bytes 與 SHA-256，再由 release 彙整層經安全相對路徑、逐層非連結與穩定讀取檢查後核對；13 份成品必須全數通過，且同大小 Office 替換的負向 fixture 必須失敗關閉。Canonical 成品或 evidence 不得為連結，檔名必須是證據目錄直接子項。這些完整性欄位只屬私人 release 證據，不進計算書、正式附件內容或 Pages。HTML 明確隱藏、透明、零尺寸及同色前景／背景不得補足內容；不確定的裁切、頁外或對比狀態降 review。DOCX `w:vanish` 與 XLSX hidden／veryHidden 工作表、隱藏列、隱藏欄不計入；存在隱藏欄但 cell 缺 `r` 參照時降 review。Office 條件格式／物件疊放／字型替換、透明合成及掃描辨識仍可能需要人工複核；此證據不等同數位簽章或工程核可。 |
| `結構工具箱/tools/attachment-package-check.js` 的 HTML 靜態可見性判定 | 納入 | 同一元素命中的 simple selector 規則與 inline style 必須合併後判定，並考慮預設白底、繼承前景色及最近明示的祖先背景色。白字／預設白底、分拆規則形成同色前景與背景，以及 `width:0; height:0; overflow:hidden` 內容不得補足工程內容；明確白字／深色背景等有效對比不得誤判。外部 stylesheet、CSS `@import`、涉及可見性／配置／前景背景但無法解析的 selector、無法解析的 `color`／`background` 值，以及無法靜態確定的裁切、頁外或對比狀態均降為人工複核；純 typography 的複雜 selector 不會單獨觸發。 |
| `preflight-tools.ps1` 的 `attachment-package-check` key | 納入 | quick／CI 固定執行附件 Node 單元 gate，full／release 才在單元成功後追加實際 Edge canonical render E2E；不得因瀏覽器測試較慢而把整個 key 排除於 quick。Canonical profile 由可見標題重判 book／summary family；完整產出工具、版本、輸出時間與計算指紋會提升為 `traceable-*` 並用較嚴格 profile 重算，非 traceable 宣告不得降級，summary 宣告不得繞過 book 過程群組。`compiled-engineering-report` 僅作 book-family 相容宣告，實際內容仍使用重判 profile。 |
| `結構工具箱/tools/attachment-package-upgrade-assess.js`、`結構工具箱/tools/attachment-package-upgrade-assess.test.js`、`結構工具箱/tools/評估舊版附件包升級.bat` | 納入 | 舊版正式附件包的唯讀升級評估入口。必須先沿用事後驗證器確認完整性；blocked 包不得提供升級捷徑，v1／v2 完整包只回傳 review／CLI 1，依序要求保留舊包、由原始工具重新確認並輸出計算結果、重新核可、另建 v3 包。評估器不得原地改寫清單、補造 metadata、複製或推算核可時間，也不得自行產生正式附件；已通過的 v3 包回傳 ready／CLI 0，完整性異常回傳 blocked／CLI 2。逐份工作清單以產出工具、正規化版本與共享計算指紋配對正式附件及來源資料，列出舊輸出、指紋、來源與重新輸出／核可待辦；無配對來源時必須標示回外部可信來源，禁止從報告反推輸入。v3 與 blocked 包的工作清單固定為空。`--json` 與文字摘要使用同一份內部評估。工具、測試與批次入口屬私有治理資產，不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-workspace.js`、`結構工具箱/tools/attachment-package-upgrade-workspace.test.js`、`結構工具箱/tools/建立舊版附件升級工作區.bat` | 納入 | 舊版附件包安全升級工作區建立入口。只接受完整且狀態為 review 的 v1／v2 包，另建全新工作區並維持 review／CLI 1；blocked 與 v3 不建立工作區。工作區把 `00_內部升級工作說明_勿附入主報告/` 與 `01_新組包來源/` 實體分開，前者只含具 `WSP-` 指紋的 JSON／Markdown 逐份待辦，後者只含空白的新計算書與新來源資料夾。不得修改舊包、複製任何舊附件或來源、預填 metadata／核可時間，亦不得把工作區建立成功視為正式核可；新輸出完成後只可選取 `01_新組包來源/` 進入正式組包。輸出位置不得位於舊包內、包住舊包或覆寫既有資料夾，建立過程採暫存後原子發布，失敗需清除半成品。工具、測試與批次入口屬私有治理資產，不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-workspace-check.js`、`結構工具箱/tools/attachment-package-upgrade-workspace-check.test.js`、`結構工具箱/tools/檢查舊版附件升級工作區.bat`、`結構工具箱/tools/attachment-package-build.js` | 納入 | 升級工作區的唯讀完成度閘門及正式組包強制接線。先驗證工作清單指紋、JSON／Markdown 一致、固定兩區目錄與無額外控制檔，再沿用附件檢查器；每筆舊附件必須以產出工具與共享舊計算指紋唯一配對 1 份新正式計算書及 1 份新來源資料。新版本可不同，但新計算書與來源彼此仍須版本、案件與指紋一致；兩者輸出／儲存時間及正式核可時間不得早於工作區建立時間，避免複製舊包內容假裝完成。缺件、內部審閱、缺少新核可或舊時間維持 review／CLI 1；內容指紋不符、不安全連結、控制清單／邊界遭改寫為 blocked／CLI 2；只有逐份完成且附件檢查 ready 才回傳 ready／CLI 0。組包器辨識名為 `01_新組包來源` 的輸入後必須先通過同一完成度檢查，否則不建包；新 v3 輸出不得位於升級工作區內。檢查不得寫回核可或狀態。工具、測試與批次入口屬私有治理資產，不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-flow.js`、`結構工具箱/tools/attachment-package-upgrade-flow.test.js`、`結構工具箱/tools/舊版附件包升級流程.bat` | 納入 | 舊版正式附件的單一安全流程入口。只接受實體資料夾，辨識正式附件包、升級工作區或 `01_新組包來源`；附件包即使遺失清單，但仍具有正式／內部包邊界，也必須進完整性驗證並 blocked，不得降成未知輸入繞過。完整 v1／v2 只建立新工作區並回傳 review，v3 ready 不做變更，異常包不建立工作區；工作區 review／blocked 不建包，只有完成度 ready 才呼叫同一正式組包器產生 v3。直接選取新組包來源時必須回溯父工作區，不能跳過完成度與人工核可；統一流程本身不得寫入核可或改寫工作區。`--output` 依階段代表新工作區或新 v3 包；CLI 維持 ready 0、review 1、blocked 2、辨識／參數／執行錯誤 3。工具、測試與批次入口屬私有治理資產，不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-history.js`、`結構工具箱/tools/attachment-package-upgrade-history.test.js`、`結構工具箱/tools/attachment-package-upgrade-flow.js` | 納入 | 統一升級流程的外部原子歷程收據。每次 CLI／批次執行先確認歷程位置在舊包、工作區、來源區及預定輸出之外且可寫，再執行升級動作；不安全位置必須在任何受管產物變更前失敗。收據採 `formal-attachment-package-upgrade-history-record.v1`，只記錄階段、動作、狀態、受管產物是否改變、舊包／工作清單／新包指紋、完成度摘要與自我驗證狀態，不得記錄計算內容、輸入值或核可時間。每份 JSON 以唯一 receiptId、`HIS-` 指紋、暫存檔與原子更名發布，禁止覆寫；發布失敗需清除暫存。預設歷程資料夾為輸入旁的 `附件升級內部歷程_勿附入主報告/`，其內容不得附入計算書、主報告或正式附件包，也不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-history-index.js`、`結構工具箱/tools/attachment-package-upgrade-history-index.test.js`、`結構工具箱/tools/檢查附件升級內部歷程.bat` | 納入 | 外部升級歷程的唯讀完整性索引。只接受實體歷程資料夾，逐份檢查封閉欄位、重複 JSON key、檔名／時間／動作／receiptId、`HIS-` 指紋、動作／狀態／變更旗標、指紋格式、完成摘要與讀取前後雙快照；索引只輸出不含完整路徑的收據摘要及 `HIX-` 集合指紋，不寫回歷程、附件包或工作區。沒有基準只能驗證現況；資料夾外可信 `valid` 基準可偵測新增、遺失與同 ID 改變，新增為 review，遺失／改變／基準損壞為 blocked。基準與索引不得混入歷程資料夾、計算書、主報告或正式附件包；完整性通過不代表附件核可，亦非數位簽章。工具、測試與批次入口不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-history-baseline.js`、`結構工具箱/tools/attachment-package-upgrade-history-baseline.test.js`、`結構工具箱/tools/建立附件升級可信基準.bat` | 納入 | 外部升級歷程的可信基準發布器。只在同一唯讀索引結果為 valid 時，於歷程旁的專用外部資料夾或明確指定的新 JSON 路徑發布；空歷程、收據損壞，或相較既有基準有新增、遺失、改變時一律不產生基準。輸出不得位於歷程內或包住歷程，不得經連結重新導向，且禁止覆寫；採同目錄獨占暫存、fsync、以索引器自我驗證及原子更名，失敗需清除暫存與新建空資料夾。基準只保存 valid 索引的不具完整路徑摘要及 `HIX-` 指紋，不修改歷程、附件包、工作區或報告，也不代表附件正式核可或數位簽章。工具、測試與批次入口不發布至 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-history-baseline-advance.js`、`結構工具箱/tools/attachment-package-upgrade-history-baseline-advance.test.js`、`結構工具箱/tools/推進附件升級可信基準.bat` | 納入 | 可信基準的明確版本前進。必須指定既有 valid 外部基準；完全相符時零寫入，只有舊收據不變且純新增時可停在 review，並需 `--accept-additions`、內部複核人與複核依據才可發布。遺失、同 ID 改變、收據篡改、基準損壞或其他警告一律 blocked／不輸出。新版基準與內部核准紀錄組成單一外部前進包；核准紀錄以 `HAD-` 指紋綁定舊／新基準 SHA-256、`HIX-`、收據數及逐筆新增 ID／`HIS-`，兩檔需交叉驗證並與目前歷程完全相符後才原子發布整個目錄。舊基準、歷程、附件包、工作區與報告不得修改；前進包不得覆寫、落入或包住歷程，失敗需清除暫存。內部核准不是正式附件核可，整包不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-package-upgrade-history-baseline-chain.js`、`結構工具箱/tools/attachment-package-upgrade-history-baseline-chain.test.js`、`結構工具箱/tools/檢查附件升級可信基準版本鏈.bat` | 納入 | 可信基準版本鏈的唯讀驗證入口。掃描根目錄內初始 valid 基準與逐代前進包，以基準 SHA-256 及核准紀錄建立單根、單終端線性鏈；逐段驗證封閉欄位、重複 JSON key、`HAD-`、新舊 `HIX-`／SHA-256、純新增收據集合與時間先後，再以終端基準比對目前歷程。缺口、分叉、合流、重複基準、基準遭替換、缺少核准或目前歷程遺失／改變均 blocked；只有目前歷程的合法新增尚未前進時為 review，完全相符為 valid。入口不得建立、修改、改名或刪除版本鏈、歷程、附件包、工作區或報告；輸出只含相對名稱、摘要與 `HCX-` 指紋，不揭露複核人、依據或完整路徑。結果只證明內部版本鏈一致性，不是正式附件核可或數位簽章；工具、測試、批次入口、版本鏈及核准紀錄均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-overview.js`、`結構工具箱/tools/attachment-case-governance-overview.test.js`、`結構工具箱/tools/檢查案件附件治理總覽.bat` | 納入 | 正式附件包、外部升級歷程與可信基準版本鏈的案件級唯讀總覽。直接組合既有附件包完整性驗證與版本鏈驗證，不另行放寬或推算核可；三個輸入根目錄必須是完全分離的實體位置，並於整體檢查前後重驗附件包、歷程、版本鏈及外部初始基準快照。只有附件包 ready 且版本鏈 valid 才回報可進入內部歸檔複核；相容性提醒或合法新增待前進為 review；完整性錯誤、斷鏈、篡改、核准證據異常或競態變更為 blocked。輸出只保留資料夾名稱、封閉狀態摘要、下一步代碼及 `PKG-`／`HIX-`／`HCX-`／`GOV-` 指紋，不含完整路徑、計算內容、複核人或依據。總覽 ready 不是正式附件核可或數位簽章；工具、測試、批次入口與總覽結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-root.js`、`結構工具箱/tools/attachment-case-governance-root.test.js`、`結構工具箱/tools/檢查案件根目錄附件治理.bat` | 納入 | 單一案件根目錄的唯讀自動定位入口。只掃描直接子資料夾，以附件包清單、升級歷程收據 kind、標準歷程／基準名稱、可信索引 kind 及前進包固定兩檔結構辨識正式附件包、外部歷程與可信基準鏈；各類必須恰有一組才可呼叫同一案件治理總覽。缺少、多組、跨角色、連結項目、無法讀取或檢查期間候選／已選內容改變均 blocked，不依日期、名稱或排序猜選。入口與內層總覽均不得寫入案件根目錄；輸出只含直接子資料夾名稱、候選摘要、下一步及 `CAS-`／既有治理指紋，不含完整路徑、計算內容、複核人或依據。根目錄入口 ready 只表示可進入內部歸檔複核，不是正式附件核可或數位簽章；工具、測試、批次入口與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio.js`、`結構工具箱/tools/attachment-case-governance-portfolio.test.js`、`結構工具箱/tools/檢查多案件附件治理總覽.bat` | 納入 | 多案件附件治理的唯讀批次總覽。只掃描指定上層的直接子資料夾，以單一案件根目錄入口的封閉候選結構辨識案件，非案件資料夾明列忽略，不遞迴猜測。各案保持原判定；任一 blocked 使整批 blocked，否則任一 review 使整批 review，全部 ready 才為 ready。批次處置分群只能由既有問題與下一步代碼衍生：P0 為停止歸檔的來源異動、附件包、可信鏈或案件結構問題，P1 為合法新增待前進基準，P2 為相容性或其他人工確認；不得以分群變更個案狀態，同案多重原因可重複列群但優先案件數只能計一次。`--only-actionable` 與 `--priority P0|P1|P2` 只能建立顯示視圖，不得改變完整狀態、退出碼、總數或 `POR-` 指紋；視圖須標明 `fingerprintScope=all-cases`、保留完整摘要與所有上層問題，且不得讓被隱藏案件誤認為未受檢。檢查前後須重掃案件集合並重驗每案治理候選快照；上層連結、無案件或執行期間改變均 blocked。輸出只含案件名稱、狀態、封閉數量、優先群組、問題／下一步代碼及 `POR-`／既有治理指紋，不含完整路徑、計算內容、複核人或依據。總覽不得建立、修改或核可任何案件資料；結果只供內部跨案排序，不是正式附件核可或數位簽章，工具、測試、批次入口與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-compare.js`、`結構工具箱/tools/attachment-case-governance-portfolio-compare.test.js`、`結構工具箱/tools/比較多案件附件治理總覽.bat` | 納入 | 兩份完整多案件治理快照的唯讀比較入口。輸入必須是未篩選的完整 v1 總覽實體 JSON 檔，且需封閉驗證欄位、重複 JSON key、大小、摘要、案件唯一性、狀態、治理邊界與可重算分群；篩選視圖、連結、額外欄位、摘要矛盾或比較期間來源改變不得放行。依案件名稱分類新增、移除、改善、惡化、同狀態內容變更及未變，並以 `CMP-` 指紋綁定兩個 `POR-` 指紋與差異。比較狀態不得掩蓋目前批次健康：目前 blocked 必須維持 blocked，目前 review 至少維持 review；惡化或新增 blocked 亦 blocked，其餘任何差異至少 review。`--only-blocking` 只顯示目前 blocked 或惡化案件，`--change` 可重複指定差異類型；篩選輸出必須改用明確 comparison view，標示 `fingerprintScope=all-changes` 與完整／顯示／隱藏筆數，且只能縮減案件及對應下一步的顯示，不得改變完整狀態、摘要、退出碼或 `CMP-` 指紋。入口不得修改、核可、前進或重新掃描案件；輸出只含快照檔名、案件名稱、狀態／優先層級、差異代碼與指紋，不含完整路徑、計算內容、複核人或依據。比較結果不是正式附件核可或數位簽章，工具、測試、批次入口與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-snapshot.js`、`結構工具箱/tools/attachment-case-governance-portfolio-snapshot.test.js`、`結構工具箱/tools/保存多案件附件治理快照.bat` | 納入 | 完整多案件治理總覽的安全內部快照發布器。快照資料夾必須與案件上層雙向分離且為實體路徑；檔名由總覽時間與 `POR-` 組成，同名永不覆寫。完整 JSON 需先排他暫存、fsync 並由比較器封閉規則回讀，再以相同時間重建總覽；發布前後都需重驗案件集合與治理候選快照。原子硬連結若遇既有目標或外部競態必須失敗封閉，不得覆寫他人檔案；暫存驗證、來源穩定或發布後回讀失敗時需清除暫存並撤回新檔。來源於原總覽讀取期間改變時不得保存；其他穩定 ready／review／blocked 狀態均可保存供跨期比較，退出碼保持 0／1／2。命令結果只揭露快照資料夾名稱、檔名、SHA-256、案件數與 `POR-`，不含完整路徑、計算內容、複核人或依據。快照不是正式附件核可、版本前進或數位簽章，工具、測試、批次入口與快照均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-snapshot-index.js`、`結構工具箱/tools/attachment-case-governance-portfolio-snapshot-index.test.js`、`結構工具箱/tools/檢查多案件治理快照歷程.bat` | 納入 | 內部治理快照資料夾的唯讀索引與最新兩版比較入口。根目錄與每個項目都必須是實體項目；只接受安全發布器產生、檔名與內含時間／`POR-` 一致且 link count 為 1 的完整 JSON。連結、子目錄、非 JSON、破損／重複 key／額外欄位、改名、同群組同時間及索引期間內容變更均 blocked。索引依內含案件上層名稱分群，完整性狀態與各群組最新快照健康分開計算，整體不得低於任一最新健康；不足兩份為 review。最新兩版比較只接受單一群組的專用資料夾，混合群組必須 blocked 且要求分流，不得猜選；選定後沿用同一比較器與顯示篩選，並於比較結束再次重驗整個資料夾與所有檔案雜湊。`PSI-` 綁定全部有效快照、群組與索引問題；輸出只含資料夾名稱、案件上層名稱、快照檔名、時間、狀態、案件數、SHA-256、`POR-`／`CMP-`／`PSI-`，不含完整路徑、計算內容、複核人或依據。索引與比較固定唯讀，不是正式附件核可、版本前進或數位簽章，工具、測試、批次入口與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend.js`、`結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend.test.js`、`結構工具箱/tools/分析多案件治理快照趨勢.bat` | 納入 | 單一案件群組全部治理快照的唯讀跨版本趨勢入口。完整沿用快照索引的實體目錄、發布器檔名、封閉 JSON、`POR-`、單一硬連結與執行期間來源穩定驗證；混合群組不得猜選，少於兩份只回報 review 且不建立趨勢。分析依內含時間比較每一組相鄰快照，分開揭露目前健康、最新轉折、目前注意與歷史累計，整理批次狀態軌跡、案件狀態軌跡、跨期反覆問題及注意排序；注意排序只沿用目前狀態、既有 P0／P1／P2、最新惡化、目前移除與仍存在的反覆問題，不建立黑箱風險分數。整體狀態不得低於目前健康、最新轉折或目前注意；歷史舊惡化若已改善則保留軌跡但不永久把目前狀態降為 blocked。`TRD-` 綁定索引、全部快照、相鄰 `CMP-`、案件／問題趨勢及分析問題；分析後需重驗整個資料夾與所有來源雜湊。輸出不含完整路徑、計算內容、複核人或依據。趨勢固定唯讀，不是正式附件核可、版本前進、風險評分或數位簽章，工具、測試、批次入口與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend-disposition.js`、`結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend-disposition.test.js`、`結構工具箱/tools/檢查多案件治理趨勢處置.bat`、`結構工具箱/tools/記錄多案件治理趨勢處置.bat` | 納入 | 治理趨勢之預期移除與反覆提醒的獨立內部處置收據鏈。處置資料夾須與快照完全分離，只接受封閉實體 JSON、單一 link count、連續序號、單調時間、檔名一致及前後 `TRA-` 指紋相接；重複 key、額外項目、改名、缺口、斷鏈、硬連結或讀取競態均 blocked。`DTE-` 綁定案件本次持續移除的起點／最後出現時間，或問題本次連續出現起點、受影響案件集合與上層出現狀態；證據不變時收據可跨新快照適用，重新出現、再次移除或受影響集合改變時舊收據失效。唯讀組合結果完成前需再次重驗趨勢與收據鏈，任一側改變即 blocked。發布需使用上層排他鎖、排他暫存、fsync、封閉回讀、發布前後趨勢與收據鏈重驗及原子硬連結；同一有效目標不得重複確認，失敗不得留下本次收據。`TAI-` 綁定全鏈，`TDS-` 綁定目前適用狀態。收據保存複核人與依據，但外部結果不得揭露；確認只能解除對應歷史型 P2 注意，狀態仍不得低於目前健康、最新轉折、P0 或 P1。處置不是正式附件核可、版本前進、風險評分或數位簽章，工具、測試、批次入口、收據與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend-disposition-checkpoint.js`、`結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend-disposition-checkpoint.test.js`、`結構工具箱/tools/檢查多案件治理趨勢處置檢查點.bat`、`結構工具箱/tools/建立多案件治理趨勢處置檢查點.bat` | 納入 | 趨勢處置收據鏈的外部可信檢查點。檢查點資料夾須與治理快照及處置紀錄雙向分離；檔案只接受封閉實體 JSON、單一 link count、標準時間與內容一致檔名。`TAC-` 綁定當時全部收據的連續序號、檔名、時間、`TRA-`、每檔 SHA-256、終端指紋與 `TAI-`；前進檔另綁定前一檢查點的檔名、`TAC-` 與 SHA-256。指定鏈少於錨定數時，即使剩餘前綴仍鏈內自洽也必須以尾端截斷 blocked；既有前綴改動 blocked，純新增 review。初始與前進均需排他鎖、不可覆寫新檔、排他暫存、fsync、封閉回讀、原子硬連結，並在發布前後重驗快照、處置鏈與前一檢查點；失敗需撤回本次檔案，既有鎖不得誤刪。命令結果不得揭露完整路徑、複核人或依據，狀態不得低於目前趨勢處置狀態。使用者必須從受保護位置明確指定檢查點，不得自動猜選「最新」或以舊檔掩蓋缺失；檢查點只證明相對完整性，不是正式附件核可、工程狀態改善、防竄改儲存或數位簽章，工具、測試、批次入口、檢查點與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend-disposition-checkpoint-history.js`、`結構工具箱/tools/attachment-case-governance-portfolio-snapshot-trend-disposition-checkpoint-history.test.js`、`結構工具箱/tools/檢查多案件治理趨勢處置檢查點歷程.bat` | 納入 | 外部可信檢查點的指定終點歷程驗證。歷程資料夾須與快照及處置紀錄完全分離，指定終點須是其中直接實體檔案；工具不得自動猜選最新檔。資料夾只接受封閉實體 JSON、單一 link count 與內容一致檔名；全鏈恰有一份 initial，各 advance 必須以檔名、`TAC-`、SHA-256 唯一銜接前一檔，保持案件／資料夾範圍、完整收據前綴與單調時間，收據數及接受新增數須一致。缺檔、替換、分叉、循環、回退、範圍切換、非 JSON、連結、硬連結或讀取競態均 blocked；`TCH-` 綁定有序全鏈。指定終點位於唯一鏈尾時 ready，鏈上其後只有合法檢查點時 review 並要求人工改指新終點；不得改指舊檔掩蓋缺失。組合結果完成前需重驗治理快照、處置鏈、檢查點歷程及指定終點，狀態不得低於目前處置／工程狀態，結果不得含完整路徑、複核人或依據。歷程結果不是正式附件核可、防竄改儲存或數位簽章，工具、測試、批次入口、檢查點與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/attachment-case-governance-workspace.js`、`結構工具箱/tools/attachment-case-governance-workspace.test.js`、`結構工具箱/tools/檢查附件治理工作區.bat`、`結構工具箱/tools/建立附件治理工作區.bat` | 納入 | 單一設定檔的附件治理工作區。`TGW-` 綁定工作區名稱、相對於設定資料夾的三個正規化來源路徑、受信任終點檔名／`TAC-`／SHA-256、內部複核決定與前一設定身分；設定資料夾必須與快照、處置鏈及檢查點歷程完全分離。唯讀入口只需 `--config`，先封閉驗證實體 JSON、單一 link count、內容一致檔名與 `TGW-`，再解析固定來源、確認終點三重身分並呼叫同一指定終點歷程驗證；完成前重讀設定、終點與全部治理來源。初始及前進均使用輸出上層排他鎖、不可覆寫新檔、排他暫存、fsync、封閉回讀與原子硬連結，發布前後重驗來源；失敗撤回本次檔案且不得誤刪他人鎖。前進只能沿用前一設定的名稱與三個來源，新終點必須是舊終點後續檢查點，並綁定前一設定檔名、`TGW-` 與 SHA-256。設定改名、硬連結、終點替換、相對拓撲失效、來源切換或競態均 blocked；輸出不得揭露相對／完整路徑、複核人或依據，狀態不得低於目前治理／工程狀態。設定不是正式附件核可、防竄改儲存或數位簽章，工具、測試、批次入口、設定與結果均不得放入計算書、主報告、正式附件包或 Pages。 |
| `結構工具箱/tools/local-quick-output-consistency.test.js` | 納入 | 局部快算跨輸出一致性 regression；以 manifest + golden cases 驗證 core result、JSON payload、summary/checks、schema、provenance 與 HTML 匯出/report metadata 的工具身份一致。 |
| `結構工具箱/tools/local-quick-browser-smoke.test.js` | 納入 | 局部快算 Edge/CDP 瀏覽器 smoke；檢查乾淨路由、響應式版面、JSON round-trip、詳算式與簡易結果計算書、空白案件欄位省略、預設內部審閱、正式附件核可、輸入變更撤銷核可、page-only wording 排除、script / console error 與橫向溢出。正式放行另由來源 JSON 回讀後逐值比對全部輸入與結果，保存來源／重播雜湊，並核對詳細 PDF 沿用同一計算指紋及成品 SHA-256。 |
| `結構工具箱/tools/report-approval-invalidation.catalog.json`、`結構工具箱/tools/report-approval-invalidation.contract.test.js` | 納入 | 石材、錨栓、覆工板與開挖擋土支撐等獨立產報工具的共同核可失效門檻。清冊固定宣告核可控制、內容識別策略與負向證據；契約直接執行石材撤銷函式及覆工板輸入事件處理器，並要求錨栓計算內容鍵單元測試／React effect、開挖報告內容鍵／React effect／專用契約同時存在。任何報告相關異動都須撤銷正式附件、清除核可時間；有既有 PDF／Word／證據連結或文件狀態者亦須清除。契約由 `report-disclosure-contract` 在 quick、CI 與正式 release 共用。 |
| `鋼筋混凝土/tools/beam-report-visual.test.js`、`鋼筋混凝土/tools/beam-report-visual.contract.test.js` | 納入 | RC 梁報告瀏覽器成品與契約檢查；正式放行高負載下的報告 popup 最長等待 60 秒，避免把瀏覽器排程延遲誤判成計算或附件失敗，逾時仍須阻擋放行。 |
| `結構工具箱/tools/formal-tools.manifest.json`、`結構工具箱/core/ui/report.js`、`結構工具箱/core/wind-report.js`、`結構工具箱/core/direct-print-boundary.css` | 納入 | 風力 / 地震正式工具清冊、共用計算書核可模型與工作頁直接列印邊界；集中記錄 14 個正式頁的路由、報表、JSON、示意圖、pilot golden cases、regression 期望、內部審閱／正式附件狀態、輸入變更撤銷核可，以及來源 JSON 與計算書共用追溯指紋的契約。核可人與核可依據維持選填，留空不降低附件身分；填寫後只進入精簡頁尾及核可封印，輸入控制不進列印。正式核可後任一選填核可紀錄異動，須立即撤銷核可、清除舊核可時間並要求重新明確勾選。 |
| `結構工具箱/tools/formal-tools.run.js` | 納入 | 正式工具 manifest runner；由清冊呼叫共同契約與正式頁 Edge 瀏覽器 smoke。工具成熟度矩陣由 `preflight-tools.ps1` 在主檢查 summary 寫出後統一重產與檢查，避免新增 governance key 時讀到舊 summary。 |
| `結構工具箱/tools/formal-tools.contract.test.js` | 納入 | 正式工具共同契約測試；集中確認 14 個正式工具檔案、首頁入口、乾淨路由、文件邊界、報表分流、示意圖角色、HTML 彈窗型計算書可讀文字、正式追溯欄位與 golden case 欄位，並要求每個計算書 renderer 套用共用內部審閱／正式附件核可狀態、每個工作頁載入共用直接列印封鎖 CSS、body class、邊界通知及一致文字。案件 JSON 匯入另須共用完全相同的 schema／工具／版本驗證、重算指紋斷言與失敗回復，不得跨版本盡量套用。 |
| `結構工具箱/tools/formal-traceability.catalog.json`、`結構工具箱/tools/formal-traceability.contract.test.js` | 納入 | 風力 / 地震正式工具的條文語意追蹤 catalog 與契約測試；集中確認 14 個正式頁的規範來源、輸入、計算路線、報告落點、golden case 與人工複核邊界，並由 formal runner 與平台 preflight 直接執行。 |
| `結構工具箱/tools/calculation-book-content-boundary.json`、`結構工具箱/tools/calculation-book-content-boundary.js`、`結構工具箱/tools/report-disclosure.contract.test.js` | 納入 | 全工具計算書內容邊界的單一版本化清單、共用判讀器與跨家族契約；讀取 formal、RC、鋼構、錨栓、石材、覆工板與開挖擋土支撐 traceability catalog，要求每筆 trace 有人可讀報告落點、規範 / 專案依據、人工複核邊界，並讓瀏覽器報表、PDF 渲染、HTML / DOCX / XLSX 成品與附件組包共同排除頁面閱讀狀態、輸入 / 報表模式、操作流程、方法分級、適用邊界卡與規範覆蓋矩陣。契約另以正向內容群組要求採用輸入、計算／檢核過程、工程結果及至少兩個實際工程數值；章名、狀態、版本、日期、時間與指紋不計入數值證據。明確計算摘要可省略重複詳算，但保留相同數值門檻；原生工具再要求產出工具、版本、輸出時間與計算指紋，編製型工程報告及直接列印阻擋通知使用各自剖面。計算內容先於結論。由平台 preflight 的 `report-disclosure-contract`、附件組包與實際渲染 gate 直接執行，決策記錄於 `docs/adr/0003-calculation-book-content-boundary.md`。 |
| `結構工具箱/tools/delivery-artifacts.contract.test.js` | 納入 | 交付物一致性契約；鎖住石材 audit JSON / Word / PDF、錨栓 HTML / XLSX / DOCX 報告、覆工板 JSON / Word 報表與開挖擋土支撐 PDF / DOCX / latest download API 的 traceability、README、smoke fixture、報表產生器、前端狀態與下載邊界，並由平台 preflight 的 `delivery-artifacts-contract` 直接執行。 |
| `結構工具箱/tools/release-readiness.contract.test.js`、`結構工具箱/tools/rendered-delivery-evidence.js`、`結構工具箱/tools/rendered-delivery-evidence.contract.test.js`、`結構工具箱/tools/rendered-delivery-evidence.inventory.json` | 納入 | `release-readiness-contract` 正式放行與真實交付物渲染證據契約；inventory 對齊首頁 33 個 formal 入口，aggregate contract 在 release 當輪彙整 RC、鋼構、風 / 震、局部快算、錨栓、石材、覆工板與 SRC 梁與 SRC 柱證據，並把動力分析摘要 PDF / evidence JSON 另列為 `seismic-report`、開挖本機服務 PDF / DOCX / latest download 副本另列為 `excavation-formal`，使首頁 `33/33` 與補充報告 / 服務成品 `2/2` 分開揭露。錨栓需在當輪 `anchor-formal` 目錄保存 HTML、DOCX、XLSX，aggregate contract 重新解析 HTML、`word/document.xml` 與 XLSX workbook / worksheet XML，核對案名、章節、工作表、檔案尺寸及 page-only 排除清單，不得只把 `anchor-report-contract` 通過日誌當成渲染成品；石材 `auto_word` 正式路徑則在 `stone-formal` 保存 PDF、DOCX 與 audit JSON 並重新核驗；覆工板需在當輪 `decking-formal` 保存 DOCX 與結構摘要，aggregate contract 重新解析 `word/document.xml`，核對案名、編號、日期、主要章節、段落、表格、尺寸及 page-only 排除清單，不得依賴固定共用輸出路徑或測試日誌；動力分析摘要需重新解析 PDF 並核對 evidence JSON 的 renderer、表格、頁數、文字、孤立標題與續頁脈絡；開挖成品則重新解析 PDF 與 `word/document.xml`，並核對原始報告與最新下載副本雜湊相同。release wrapper 固定使用 `ForceSlowChecks` 與 `ForcePlatformAudit` 強制慢測與平台 audit、不透傳 `%*` 任意參數，且 preflight 本體拒絕 `Quick` 與 release force flags 同時使用。瀏覽器報表以 CDP `Page.printToPDF` 產出 A4 PDF，再由 `pdfinfo`、`pdftotext`、`pdftoppm` 檢查頁數、可讀文字、非空白頁、稀疏末頁、頁邊截切、表格標題、閱讀順序、頁尾與內容不得混列、頁尾孤立章節標題、續頁上下文及 page-only 文字排除；續頁若不是從章節 / 步驟 / 重複表頭開始，而是孤立公式、單一資料列或無標籤片段，`uncontextualPageStartCount` 必須阻擋 release。稀疏末頁判定同時使用文字量與渲染墨量，避免誤判以圖表為主的合法頁面。共用報告列印頁尾必須跟隨文件流，不得固定覆蓋分頁內容；章節標題需跟隨後續內容、表格列不得拆頁，跨頁表格需重複表頭；短輸入群組只允許個別 `keepTogether`，不得全面鎖住所有輸入表。RC 視覺 PDF 亦呼叫相同的分頁成品檢查。證據寫入當輪 `PREFLIGHT_RUN_DIR/rendered-delivery-evidence/`。preflight summary / history / homepage status 保留 force flags、慢測重用與平台 audit 重用資訊，並讓儀表板把正式放行 run 標示為 Release。 |
| `結構工具箱/tools/formal-browser-smoke.test.js` | 納入 | 風力 / 地震正式頁 Edge/CDP 瀏覽器 smoke；覆蓋 14 個正式 / 報表頁的乾淨路由、響應式版面、報告模式、JSON、計算書與示意圖。列為 `reportTraceRequired` 的工具必須輸出工具、版本、時間與指紋，且來源 JSON 與計算書指紋相同。每個 popup 實測空白案件欄位省略、預設內部審閱、勾選核可變正式附件、核可紀錄異動後撤銷並重新明確核可、目前版本 HTML 序列化、分頁／PDF 預設標題，以及計算輸入變更後撤銷核可；下載內容必須保存唯一且靜態可讀的文件狀態列、核可時間、穩定報告名稱與指紋，移除互動控制並於重新開啟時沿用該狀態列。附件組包檢查器必須能在不執行 JavaScript 下辨識正式附件。分頁標題須隨核可狀態更新且不得重複串接。工程 review / blocked 與文件狀態分離。另把 14 個工作頁直接列印成一頁封鎖 PDF，確認不會繞過計算書流程。 |
| `鋼筋混凝土/shared/direct-print-boundary.css`、`鋼筋混凝土/shared/beam-evaluator.js`、`鋼筋混凝土/shared/beam-evaluator.test.js`、`鋼筋混凝土/shared/column-evaluator.js`、`鋼筋混凝土/shared/column-evaluator.test.js`、`鋼筋混凝土/shared/wall-base.js`、`鋼筋混凝土/shared/wall-evaluator.js`、`鋼筋混凝土/shared/wall-inplane-evaluator.js`、`鋼筋混凝土/shared/wall-inplane-evaluator.test.js`、`鋼筋混凝土/tools/rc-index-menu-browser-smoke.test.js`、`鋼筋混凝土/tools/rc-traceability.catalog.json`、`鋼筋混凝土/tools/rc-traceability.contract.test.js`、`鋼筋混凝土/tools/report-screenshot-quality.js`、`鋼筋混凝土/tools/beam-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-beam.ps1`、`鋼筋混凝土/tools/column-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-column.ps1`、`鋼筋混凝土/tools/slab-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-slab.ps1`、`鋼筋混凝土/tools/wall-regression.test.js`、`鋼筋混凝土/tools/wall-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-wall.ps1`、`鋼筋混凝土/tools/shear-wall-report-visual.test.js`、`鋼筋混凝土/tools/shear-wall-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-shear-wall-report.ps1`、`鋼筋混凝土/tools/foundation-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-foundation.ps1`、`鋼筋混凝土/tools/single-pile-report-visual.contract.test.js`、`鋼筋混凝土/tools/test-single-pile.ps1`、`鋼筋混凝土/tools/retrofit-report-visual.test.js`、`鋼筋混凝土/tools/test-retrofit-report.ps1` | 納入 | RC 梁、柱、板、牆、剪力牆、基礎、單樁與補強斷面的條文語意追蹤、報告視覺 smoke 與 wrapper。RC 梁以 DOM-free `BeamEvaluator` 逐完整 tuple 計算正／負彎矩利用率，並依 Pu 重算 φVn、耐震 Vc 歸零及剪力利用率；Tu 達門檻時因未建立 φTn／V-T 互制而失敗關閉，不得假造扭力容量。RC 柱以 `ColumnEvaluator` 逐完整 tuple 依 Pu 重算 δns／δs、放大彎矩與雙軸 P-M 容量面利用率。一般 RC 牆以 DOM-free `WallInplaneEvaluator` 依均布縱筋、輸入保護層與可選的兩端附加集中縱筋建立含軸壓與軸拉的應變相容 P-M 設計封包，逐完整 tuple 計算 `|Mu|/φMn @ Pu` 與 `|Vu|/φVn`，並在正式計算書輸出配筋斷面、P-M 設計互制圖及需求點；特殊結構牆另納入單段剪力上限，軸力超出設計封包時失敗關閉。Mu 留空時以 `Pu·e` 推導，`e ≤ h/6` 簡式只在 HTML 對照。剪力牆沿用 `WallEvaluator` 重算 P-M 彎矩與剪力容量利用率。軸力越界、二階不穩定或容量無法建立時須失敗關閉並保留該列為控制候選。頁面診斷明細與容量候選仍維持 page-only；RC 柱的方法分級、適用邊界與條文覆蓋矩陣只保留於 HTML 操作頁及內部診斷資料，計算書僅輸出採用資料、計算式、工程檢核、結論與已完成的可追溯複核紀錄。8 個 RC 計算書預設為可列印的內部審閱，勾選核可後為正式附件。空白案名／案號／設計人省略並由主文承接，NG 結果仍可如實納入附件；工程狀態不決定文件 DRAFT。專案 JSON 仍驗證 schema、工具、版本與來源指紋；RC 柱人工複核仍保存依據、文件編號、複核人、時間與 context key。視覺 smoke 實測內部審閱頁尾、正式核可、核可時間、無 DRAFT、空白欄位省略、NG 正式附件、可讀文字、版面溢出與 page-only 排除。工作頁直接列印一律由共用 CSS 封鎖。 |
| `鋼筋混凝土/tools/report-portable-html-check.js`、`鋼筋混凝土/tools/retrofit-report-visual.contract.test.js` | 納入 | RC 八個報告視覺 smoke 共用的正式附件 HTML 下載門檻，以及 RC 補強斷面的專用靜態契約；RC 補強 V1.7 需以頁面正式 JSON 蒐集／套用函式保存案件、頁籤及全部梁柱欄位，改動目前案例後讀回並同時重算，要求梁柱完整結果與兩份計算書指紋分別一致；未知 schema 或不合法幾何在套用前拒絕。報告部分須實際核可並點擊下載，核對檔名、靜態文件狀態、核可時間、計算指紋與暫態控制排除，補強 NG 結果亦以如實正式附件路徑驗證。八類 RC evidence 皆以 HTML 與 PDF 分流，HTML 檔名及指紋寫回各報告 audit JSON；release 清冊固定宣告梁 4、柱 6、板 5、牆 4、剪力牆 2、基礎 6、單樁 3、補強 2，共 32 份。彙整器依工具別逐份重新讀取實體 HTML，驗證正式狀態與指紋並計算檔案及集合 SHA-256，不以資料夾檔案數代替內容檢查。真實檔名只保留在私人放行證據。 |
| `dev_tools/html-attachment-integrity.js`、`結構工具箱/tools/html-attachment-integrity.test.js`、`dev_tools/attachment-integrity-diagnostic.js`、`結構工具箱/tools/attachment-integrity-diagnostic.test.js` | 納入 | RC 正式附件渲染時即保存實體 HTML 的 bytes 與 SHA-256，release gate 必須拿同一份 audit／summary 內的原始值重新比對檔案，不得只替異動後內容重新計算新 hash。負向契約以暫存 fixture 證明檔案刪除、截短、維持相同位元組數的內容改寫與清冊多出附件都會失敗關閉。正式 release 不論總閘門成功或失敗，皆另彙整八類 RC 的預期、實際、已驗證、匿名 hash 與異常代碼至當輪證據及 git ignored 的 `output/preflight/attachment-integrity-latest.json`；本機儀表板只有在最新 preflight 是失敗 release 且 runId 相符時才採用此診斷，公開 Pages 仍保留最近一次成功放行快照。顯示層以匿名附件序號把代碼翻成檔案缺失、SHA-256 不符、位元組數不符、清冊缺項或清冊多出附件等可處置原因及建議處置，不得顯示實體檔名；相符的本機失敗診斷可複製只含工具名稱、匿名附件序號、原因及處置的清單，不含檔名、路徑、hash 或 bytes，並只從固定八類 RC 白名單產生「開啟來源工具」捷徑。清單與捷徑都以 `screen-only` 排除列印，公開成功狀態也不顯示捷徑。最近 20 筆正式 release 的失敗只在具同輪診斷時才可與其後第一筆整體通過且附件全數驗證的 release 配對為本機關閉紀錄；缺少診斷的舊失敗不得推測。關閉歷程最多顯示五筆匿名摘要，以 `screen-only` 排除列印，不發布 Pages。hash／bytes 不符時不得以更新清冊取代原始輸出，清冊多出附件時也不得手動刪項湊數；兩者都必須由原始計算重新產生附件。Node-only 驗證與診斷模組置於 `dev_tools/`，測試及本機診斷都不進計算書、正式附件、公開狀態或 Pages。 |
| `鋼筋混凝土/shared/report.js`、`鋼筋混凝土/tools/beam-report-visual.test.js` | 納入 | RC 共用計算書的目前版本 HTML 保存與預設檔名邊界。內部審閱與正式附件皆可直接下載；勾選核可時須把文件狀態與核可時間同步回可序列化來源，核可後修改核可人或核可依據則立即撤銷正式狀態、清除舊核可時間並要求重新勾選。下載內容保留穩定報告名稱、唯一且靜態可讀的文件狀態列、核可時間與計算指紋，移除互動控制，並在重新開啟時沿用該狀態列而不重複建立。分頁標題同步使用「報告名稱＋文件狀態＋指紋」，供瀏覽器列印／存 PDF 採用可辨識的預設檔名，切換或重新開啟不得重複串接。下載控制仍只屬螢幕預覽工具列，列印時隱藏；視覺 smoke 需實測正式附件序列化、靜態辨識、核可紀錄異動撤銷與標題結果，而不只檢查按鈕文字。 |
| `鋼筋混凝土/shared/wall-rebar-designer.js`、`鋼筋混凝土/shared/wall-rebar-designer.test.js` | 納入 | 一般 RC 牆設計模式的 DOM-free 容量式配筋搜尋；使用與正式檢核相同的 `WallInplaneEvaluator`，同時篩選 P-M、剪力、最小配筋、筋距、層數及牆厚可配置性，並依總鋼筋量、施工擁擠程度與利用率排序均布縱筋、兩端附加筋及水平筋候選。候選表與排序理由只留在 HTML 工作頁；一鍵套用後必須切回檢核模式重算，計算書只輸出實際採用配筋及正式結果。 |
| `鋼筋混凝土/shared/beam-rebar-designer.js`、`鋼筋混凝土/shared/beam-rebar-designer.test.js` | 納入 | RC 梁設計模式的 DOM-free 容量式配筋搜尋；共用正式 `Flexure` 應變相容核心與 `BeamEvaluator` 剪力核心，跨底／頂主筋號數與排數、箍筋號數、肢數及間距篩選正負彎矩、1.2Mcr、ρmax、As,min、裂縫／排間／排內間距、剪力強度與耐震加嚴。Tu 達自動判定邊界時失敗關閉，不代替 22.7 詳算。候選與排序只留在 HTML 工作頁；一鍵套用後切至檢核模式重算，計算書只輸出實際採用配筋與正式結果。 |
| `鋼筋混凝土/shared/column-rebar-designer.js`、`鋼筋混凝土/shared/column-rebar-designer.test.js`、`鋼筋混凝土/shared/column-transverse-designer.js`、`鋼筋混凝土/shared/column-transverse-designer.test.js` | 納入 | RC 矩形柱、圓形箍筋柱與螺旋柱設計模式的 DOM-free 整體容量式配筋搜尋；跨 #6～#11 主筋、#3～#5 橫向筋與 2.5～15 cm 間距，矩形柱另枚舉每邊位置數／束筋並篩選 25.7.2 排內配置與側撐，圓柱另枚舉圓周均布根數並篩選弦向淨距。圓柱幾何只提供直徑 D，頁面、專案檔與指紋正規化為 h=D；有效舊專案的多餘 h 先驗證原指紋再遷移。所有柱型皆呼叫正式 `PMSection`、`ColumnEvaluator` 與候選配置 Mpr／Ve，篩選雙軸 P-M、配筋率、兩向剪力、Av,min、斷面上限、一般／耐震間距及耐震圍束量。候選預測與套用後正式數值由瀏覽器 parity gate 鎖定。候選只留在 HTML 工作頁；套用後切至檢核模式重算，錨定、搭接、首支箍筋、ℓo 外間距與施工圖仍由正式／人工複核處理，計算書只輸出實際採用配置與正式結果。 |
| `preflight-tools.ps1` 的 `excavation-backend` timeout | 納入 | 完整開挖擋土支撐 Python 測試套件的專屬執行上限固定為 `timeoutSeconds = 300`，容納完整測試與程序收尾；仍保留逾時失敗與程序樹終止，不得重用、跳過或縮減測試。 |
| `preflight-tools.ps1` 的 `rc-column-report-contract` timeout | 納入 | RC 柱報告閘門包含三個完整視覺案例與人工複核完成／正式附件核可案例，專屬執行上限固定為 `timeoutSeconds = 600`，避免 release 全平台負載下的正常瀏覽器證據被誤判為失敗。 |
| `preflight-tools.ps1` 的 `formal-browser-smoke` timeout | 納入 | release 模式會重跑 14 個風力／地震正式工具的桌面與行動版報表及正式渲染證據；專屬執行上限固定為 `timeoutSeconds = 600`，避免平台稽核後的正常瀏覽器啟動時間被誤判為失敗。 |
| `audit-all.ps1` 的 RC timeout、`preflight-tools.ps1` 的 `platform-audit` timeout、`鋼筋混凝土/audit-tool.ps1` 的暫態重試 | 納入 | 完整 RC audit 內層固定為 `timeoutSeconds = 900`，強制平台 audit 外層固定為 `timeoutSeconds = 1200`；保留逾時失敗與程序樹終止，不得以增加上限為由重用、跳過或縮減 RC、鋼構及核心檢查。RC 子 gate 只在 log 精確命中 `net::ERR_NO_BUFFER_SPACE` 時保存首次失敗紀錄、冷卻 60 秒並完整重跑該 gate 一次；其他錯誤、逾時或第二次失敗仍封閉阻擋。 |
| `結構工具箱/tools/tool-maturity-matrix.js` | 納入 | 工具成熟度矩陣產生器；合併正式工具與局部快算 manifest，輸出治理覆蓋率與下一步品質欄位 JSON / Markdown，並揭露報告可讀文字抽檢及 `documentState` coverage，讓內部審閱／正式附件核可證據可在頁面治理層被檢視但不把頁面診斷明細混入計算書或列印 PDF。它也會將最新 `output/` 結果精簡成首頁公開狀態快照，保留 `sourceCommitSha`、`sourceBranch`、`sourceDirty` 以辨識實際受測來源；每輪第一次矩陣 refresh 固定以 `--preserve-homepage-status` 只驗證矩陣，避免後置檢查完成前把新指標掛到舊 runId。完整／正式放行在 summary 寫出後先重產矩陣，再執行 `audit-dashboard-contract-final` 等後置檢查；quick preflight 的最後 refresh 仍保留 tracked 正式快照，完整／正式放行則在 summary 與後置檢查完成後才一次刷新首頁並執行最終矩陣驗證，且正式放行要求來源 commit 可辨識、啟動時工作樹乾淨。 |
| `結構工具箱/tools/public-status-claims.contract.test.js`、`結構工具箱/tools/formal-tool-metadata.js`、`結構工具箱/tools/analysis-section-tool-metadata.js`、`鋼筋混凝土/index.html`、`鋼構工具/tool-metadata.js`、`鋼構工具/app.js`、`石材固定/js/version-sync.js`、`螺栓檢討/bolt-review-tool/src/appMeta.ts`、`覆工板/index.html`、`開挖擋土支撐/index.html`、`結構工具箱/index-classic.html` | 納入 | 公開狀態宣告一致性治理。首頁工具 metadata 是唯一公開工具清冊；RC 家族入口只能重述與首頁一致的 8 個工具版本及 formal 狀態。鋼構四個正式入口由共用 metadata 對齊首頁；風力／地震 17 個入口共用 `FormalToolMetadata`，14 個報告以公開語意版本作「工具版本」，內部 `TOOL_VERSION` 只作案件相容與計算核心識別並另列「計算引擎」；連續梁、平面剛架、斷面性質與合成斷面共用 `AnalysisSectionToolMetadata` 對齊首頁、工具頁、案件 JSON 及計算書版本，內嵌公式核心另列「計算引擎」；石材頁面、核心與報告共用 `StonePublicMetadata`，不得殘留舊 `V2`／`V2.1` 宣告。錨栓同樣將公開語意版本與 build-derived 計算引擎分開呈現與輸出；覆工板的頁面、JSON、列印報告共用一份 metadata；開挖維持服務型定位。公開入口預設只讀 tracked 快照，本機私有 audit 必須明確選用。RC、分析／斷面、鋼構、風力／地震、石材、覆工板、開挖及舊網址入口的直接列印只能顯示封鎖通知。契約沿用 canonical public-evidence schema，並以 `public-status-claims-contract` 納入 preflight、成熟度矩陣及 Pages HTTP／Playwright 成品驗證。 |
| `結構工具箱/assets/status/public-evidence-schema.js`、`結構工具箱/tools/public-evidence-schema.test.js`、`結構工具箱/tools/public-release-change-governance.test.js`、`結構工具箱/tools/public-release-change-assistant.js`、`結構工具箱/tools/public-release-change-assistant.test.js`、`.github/public-release-reduction-authorization.json` | 納入 | `public-evidence-schema-contract` 與 `public-release-change-governance` 的版本化公開證據契約。Schema v3 模組是 dashboard 必需的公開 runtime，同時由快照產生器、Pages manifest builder、live smoke 與唯讀門檻助手共用；三份 tracked status 必須明列相同 `publicEvidenceSchemaVersion`。歷程 schema v2 封閉保存最多 8 次、按時間遞增且以目前 release 為終點的公開摘要鏈，逐次只列受測 commit、正式／後置門檻、四個通過面向、十組完成數，以及由 12 個 required counter 精確推導的基準／維持／提升／縮減／混合分類。助手預設只比較目前正式輸出與公開基準，維持或提升不得建立核准；只有實際縮減、候選 release 較新且提供 12～120 字可公開理由時，才可將精確差異寫入一次性授權。授權未使用、沿用到下一輪、含額外欄位、私密路徑或雜湊均失敗關閉，也不得覆寫另一份 active 核准或寫到 repository 外；只有 tracked 公開歷程證明完全相同縮減已發布後，助手才可安全重設 inactive。授權檔本身不發布，僅將縮減理由帶入公開歷程。Git 回填接受可重驗的成套舊快照並對重複 carrier 去重。未知版本、缺欄、錯誤型別、跨 release 混用、歷史缺漏／倒序／衝突／未對齊、偽造變化、未完成面向、未宣告欄位或絕對私密來源路徑均失敗關閉；不得發布案件、輸入、逐檔清冊、來源路徑或私密雜湊。manifest 另封閉保存四個通過面向、歷史範圍與最新變化分類，dashboard 不得另行推定。助手、候選檔、測試與授權檔屬私人治理，不發布至 Pages。 |
| `結構工具箱/tools/public-release-decision-receipt.js`、`結構工具箱/tools/public-release-decision-receipt.test.js`、`結構工具箱/tools/public-release-decision-backup.js`、`結構工具箱/tools/public-release-decision-backup.test.js`、`結構工具箱/tools/public-release-decision-backup-health.js`、`結構工具箱/tools/public-release-decision-backup-health.test.js`、`結構工具箱/tools/public-release-decision-restore-drill.js`、`結構工具箱/tools/public-release-decision-restore-drill.test.js`、`結構工具箱/tools/public-release-decision-restore-drill-health.js`、`結構工具箱/tools/public-release-decision-restore-drill-health.test.js`、`.github/public-release-decision-anchor.json`、`output/preflight/history/<runId>/public-release-*-receipt.json`、`output/private-backups/public-release-decisions/*.json`、`output/private-backups/public-release-decisions/restore-drills/*.json`、`output/audit/public-release-decision-backup-health.json`、`output/audit/public-release-decision-restore-drill.json`、`output/audit/public-release-decision-restore-drill-anchor.json`、`output/audit/public-release-decision-restore-drill-health.json` | 納入 | 正式 release 的私人追加式決策鏈。只有 `quick=false`、兩個 force flag、乾淨來源、正式檢查與 post-check 全通過且公開快照完成後，preflight 才能新增封閉 schema v1 `PRD-` 決策收據；保存受測 commit、12 個公開 required counter 的變化、核准使用狀態及公開 bundle／preflight／實際渲染證據雜湊，不保存案件輸入或逐檔附件。每份收據以內容指紋鏈回前一份；Git-tracked 鏈尖錨點只保存最新 runId、收據 ID 與 canonical SHA-256，使 ignored 私人鏈遭整批刪除仍會失敗關閉。同 run 只允許完全相同收據，額外欄位、竄改、倒序、斷鏈、錨點不符或未結清前輪核准均失敗關閉。使用縮減核准時，決策收據固定為 reset pending；助手安全重設 inactive 時另寫 `PRA-` 重設收據，精確鏈回決策與核准內容，收據寫入失敗則回復原 active 設定。收據完成後自動建立封閉 `PRB-` 可攜備份；設定外部位置時，同一 PRB 必須以單一交易建立本機與外部雙副本，任一失敗回滾當輪全部新增檔。還原預設只預覽，只接受同鏈、缺少 reset 或向前延伸；只有 Git 錨點的新工作站必須由相同鏈尖備份啟動，舊鏈、分叉、竄改、覆寫與失敗交易均封閉拒絕或回滾。健康工具在每次正式匯出後核對兩地目前鏈副本、相同 PRB 配對、損毀與保存期限；同路徑不得冒充冗餘，過期只提醒不自動刪除。健康通過後，隔離演練必須從成對外部副本建立只有 tracked 錨點的暫存工作區，實際套用完整 `tracked-anchor-bootstrap`、重驗收據鏈與錨點、證明來源決策檔未變並安全清除暫存區。既有 schema v1 PDR 原樣保留；新 schema v2 PDR 必須以連續序號與前筆 ID／canonical SHA-256 鏈回既有鏈尖，本機、外部及 ignored 私有鏈尾錨點以同一交易更新後還要重驗整條兩地歷史；任一步失敗都回滾兩地新收據與原錨點。去識別狀態保存單調筆數，使兩地同時刪除鏈尾也能判定回退；獨立健康工具再檢查鏈內刪除、兩地差異、私有鏈尾錨點、目前 PRD 鏈尖及預設 8 日演練時效。三份 ignored 摘要都不得含路徑、PRB／PDR ID、雜湊或收據內容；私有鏈尾錨點保存的 ID／雜湊不得進入摘要。決策鏈、備份、演練收據及狀態只位於私人位置；錨點、產生器、測試、收據與備份都不得發布至 Pages、dashboard、計算書、主報告或正式附件，也不宣稱個人身分簽章、第三方保存或雲端服務端同步已完成。 |
| `結構工具箱/tools/public-release-decision-backup-task.test.js`、`結構工具箱/tools/run-public-release-decision-backup-health.ps1`、`結構工具箱/tools/manage-public-release-decision-backup-health-task.ps1` | 納入 | 私人決策備份每日健康排程。Install 必須先執行一次要求外部鏡像的 PRB 健康、PDR 兩地串鏈、單調回退、8 日演練時效與目前 PDC provider 往返真實檢查，成功後才以目前登入者、Interactive、Limited、隱藏視窗、每日 trigger、禁止重疊與漏跑補執行註冊固定 task；不得使用 SYSTEM 或最高權限。Status 需核對執行檔、精確 runner、每日 trigger、非 Disabled 狀態與最近結果；Remove 僅刪固定 task，不刪收據、備份、檢查點或摘要。runner 從目前使用者環境取得私人外部目錄，不把設定寫入 Git。排程、完整狀態、外部位置與健康明細不得發布至 Pages、dashboard、計算書或正式附件；成功只證明最近一次 provider API 觀察與回同步證據仍新鮮有效，不把掛載目錄可讀冒充新的服務端查詢。 |
| `結構工具箱/tools/public-release-decision-restore-drill-task.test.js`、`結構工具箱/tools/run-public-release-decision-restore-drill.ps1`、`結構工具箱/tools/manage-public-release-decision-restore-drill-task.ps1` | 納入 | 私人決策備份每週隔離還原排程。Install 必須先從受控外部鏡像完成一次真實還原演練、PDR 串鏈及目前 PDC provider 往返健康檢查，成功後才以目前登入者、Interactive、Limited、隱藏視窗、每週日 09:15 trigger、禁止重疊與漏跑補執行註冊固定 task；不得使用 SYSTEM 或最高權限。Status 要核對唯一 action、精確 runner、weekly trigger、星期、時間、身分、非 Disabled 狀態與最近結果；Remove 只刪固定 task，不刪 PRB、PDR、PDC 或 ignored 狀態。後續同 release 的 PDR 追加仍可驗證既有 PDC 所證明的歷史前綴；新 PRD／PRB 必須重新取得 provider API 證據。 |
| `結構工具箱/tools/public-release-decision-cloud-checkpoint.js`、`結構工具箱/tools/public-release-decision-cloud-checkpoint.test.js`、`output/audit/public-release-decision-cloud-checkpoint-staging/`、`output/audit/public-release-decision-cloud-verifications/`、`output/audit/public-release-decision-cloud-checkpoint-anchor.json`、`output/audit/public-release-decision-cloud-checkpoint-health.json`、外部私人 `cloud-checkpoints/*.json` | 納入 | Provider-aware 私人服務端證據。封閉 `PCO-` 觀察必須來自 Google Drive API 對目前 PRB 與 PDR 的檔名、bytes、雲端時間及 provider file ID；產生器再以工作站重驗的 PRD／PRB／PDR 鏈尖核對該觀察，建立含連續序號、前筆 canonical SHA-256 與內容指紋的 `PDC-`。目前 Drive 連線採唯讀授權，因此 PDC 經 DriveFS 寫入私人目錄後，必須由 provider API 下載服務端原始檔；`--confirm` 強制讀取獨立於外部掛載目錄的 `--provider-content-file`，由 CLI 自行核對 bytes 並重算 SHA-256。只有含 `contentReadback=provider-download`、檔名、內容雜湊與 provider 中繼資料的 schema v3 `PCV-` 才可標成往返完成或推進單調鏈尾；schema v1 中繼資料與 v2 宣告雜湊只供歷史讀取。健康工具重驗完整 PDC 串鏈、目前 PRD／PRB、已證明 PDR 前綴、內容級 PCV 預設 8 日時效與 ignored 私有鏈尾錨點。新 release、確認過期、鏈刪除／竄改、錨點落後或未完成 DriveFS→API 原始內容回讀都失敗提醒。去識別健康摘要不得含路徑、檔名、provider ID、PRD／PRB／PDR／PCO／PCV／PDC ID 或雜湊；PDC、觀察、確認紀錄、provider 原始下載檔、staging、錨點、工具與測試均不得發布至 Pages、dashboard、計算書或附件。證據代表特定時間由 provider API 下載、並由 CLI 重算雜湊的原始檔，不等同 Google 提供簽章雜湊、帳號不可刪除、WORM／第三方保管、身分簽章或帳號安全保證。 |
| `結構工具箱/tools/pages-live-smoke.js`、`結構工具箱/tools/pages-live-browser-smoke.js`、`結構工具箱/tools/run-pages-browser-smoke.sh`、`結構工具箱/tools/build-pages-clean-routes.js`、`結構工具箱/tools/build-pages-deployment-manifest.js`、`結構工具箱/tools/verify-pages-release-lineage.js`、`.github/workflows/pages-deploy.yml`、`.github/pages-smoke/package.json`、`.github/pages-smoke/package-lock.json`、`.github/pages-smoke/performance-budget.json`、`.github/pages-smoke/write-ci-summary.js`、`.github/pages-smoke/build-performance-trend.js`、`.github/pages-smoke/build-performance-trend.test.js`、`run-pages-artifact-smoke.ps1` | 納入 | GitHub Pages Actions 部署、clean route 相容層、部署 provenance、上線前阻擋與上線後 smoke；正式 lineage gate 要求部署 HEAD 的直接父提交等於公開 preflight 的 `sourceCommitSha`，且兩者差異恰為三個公開狀態 JSON 與一個不發布的私人決策鏈尖錨點，任何未經 release 測試的 HTML、JS 或計算核心夾帶變更皆在 push 與 Actions staging 前阻擋。workflow 以 `fetch-depth: 2` 取得父提交，再由 route builder 依 `vercel.json` 為公開短網址建立專案子路徑安全的轉向頁。deployment manifest builder 必須以固定 ordinal 路徑順序列出每個非隱藏發布檔案的相對路徑、位元組數與 SHA-256，從這份封閉 v2 清冊重算 `sha256-tree-v1` digest、fileCount 與 totalBytes，並於 staged root 寫入 `pages-deployment.json` 的 commit SHA 與 Actions runId / attempt；manifest、lineage verifier與 builder source 不進 tree digest，也不得發布。archive / upload / deploy 前必須以短暫 HTTP server 直接服務 staged `_site`，HTTP smoke 除了從公開 `home.js` 擷取並驗證全部 43 個首頁 route、狀態快照、直接列印邊界、本機路徑洩漏與私有路徑，也要確認 deployment manifest 對齊當次 commit/runId，以最多 8 個並行請求逐檔下載整個公開 artifact，核對 HTTP 200、位元組數及 SHA-256，再以共用 Playwright CLI runner 執行桌機 1280 × 800 與手機 390 × 844 共 92 組頁面；任一缺檔、陳舊檔、內容替換或其他失敗皆阻擋發布。build、live-smoke 與本機預演必須先從同一 v3 lockfile 以 `npm ci --ignore-scripts` 重建 `node_modules`，逐一核對 `@playwright/cli`、其 Playwright runtime、Terser 與遞迴依賴的 registry integrity；runner 只能直接呼叫安裝樹內的 JS entry，不得用 `npx --yes` 動態解析。兩個 Actions job 各以精確、無 restore key 的 `actions/cache@v5` 分別快取 `~/.npm` 與 `~/.cache/ms-playwright`，兩種 key 都綁定 OS、架構及完整 lockfile digest，瀏覽器 key 另綁定 Chromium 家族；不得快取 `node_modules`。cache hit 後仍執行 lockfile 安裝與 `install-browser chromium`，只省略重複 tarball／瀏覽器下載，不省略版本、integrity、存在性與瀏覽器補檢。兩個 job 必須先以 `if: always()` 將來源 commit／run、lock digest、兩種 cache、runtime 安裝、HTTP 與 browser 結果組成封閉 v1 JSON，各以唯一 artifact 名稱保存 14 天；上傳失敗不得忽略。最後一個 summary step 從同一收據顯示 job 結果與上述證據，因此 job status 涵蓋證據上傳。效能基準只接受 schema v1、`warning-only`、至少三輪同 commit 暖快取樣本及正整數門檻；目前 runtime 8 秒、HTTP 90 秒、browser 180 秒，超過時輸出 `::warning` 並列入收據／摘要，但不得改變工程或部署成功判定。獨立 `performance-trend` job 需等 build 與 live-smoke 完成，當輪收據必須成對、來源／run／lock 一致、各自 attempt 為正整數且 smoke 完整成功；只重跑失敗 job 時可保留較早 attempt 的成功 build 收據並採較新的 live 收據，趨勢輪次以兩者較大的 attempt 表示。再從成功 Actions run 尋找同樣成對收據。只有 build／live 的 npm 與 browser 都為 exact hit 才納入同 lock digest 最近最多 20 輪統計；冷快取當輪必須明確列出排除原因而非阻擋正常部署，lock digest 改變則開始新序列。封閉 v1 trend 保存每輪六個耗時觀測值，驗證器需以 nearest-rank 從觀測值重算 minimum／P50／P95／maximum；少於 3 輪為 `collecting`，至少 3 輪才為 `ready`。趨勢格式錯誤、缺當輪或半邊收據、統計不可重算、artifact 缺失或上傳失敗均阻擋；效能超標本身仍只警示。基準、summary source、單輪結果／收據、trend source／test／JSON 與摘要均不得進入 Pages、計算書或正式附件。deploy 後需對正式網址重跑相同身分、逐檔完整性與頁面檢查，確認實際站台與 staged artifact 一致。browser smoke 攔截 console、pageerror、requestfailed、HTTP 錯誤與整頁橫向溢位，並專項驗證 RC 單樁寬表、風壓參考圖與石材 A4 預覽局部捲動。staged gate 與本機預演不允許重試；只有正式 live 的 HTTP smoke 與 browser smoke 遇到 HTTP 5xx 或明列的 `net::ERR_*`／Node 網路暫態錯誤時，才會各自在 5 秒後完整重跑最多一次，非暫態與第二次持續失敗仍直接阻擋。HTTP smoke 的逐檔清冊、公開頁、資產、舊輸出與私有邊界請求統一先拒絕 5xx，避免把 503 誤當成「私有路徑非 200」的成功證據；404、大小或雜湊不符固定為實質失敗。CLI 回傳必須檢查 JSON `isError`，不得以固定為 0 的程序碼誤判。Pages artifact 需排除兩支 smoke source、browser runner、route builder、deployment manifest builder、lineage verifier、私有 smoke package／lockfile／performance budget／summary／trend source 與 test、`output/`、Markdown（含 `CONTEXT.md`、`docs/adr/`）、PowerShell / batch、測試、合約、`dev_tools/`、Python / TypeScript source、backend 與 package manifest；`--check-private-boundary` 固定驗證代表性非公開路徑不是 HTTP 200。`run-pages-artifact-smoke.ps1` 會先用同一 lockfile 重建本機 runtime，再在 temp 建立同型 artifact 與 local provenance、排除 `.claude`、`node_modules`、testdeps、`app_data` 與 tmp，啟動隱藏的短暫 HTTP server，並執行相同 HTTP 與 browser smoke；只在 127.0.0.1 artifact 預演時，瀏覽器腳本精確忽略鋼構本機 audit JSON 與石材 8765 狀態端點，其他失敗仍阻擋。若只是在 repo root 臨時預覽，可對 HTTP smoke 加 `--allow-local-output` 跳過本機 `/output/` 假陽性，但不得用在公開站 smoke。`deploy-pages@v5` 需保留 `actions: read` 權限，並保留官方 600,000 ms（10 分鐘）部署硬上限；逾時仍會取消並判定失敗，不得在 workflow 內跳過 deploy 或 live smoke。 |
| `push-pages-release.ps1`、`push-pages-release.bat` | 納入 | 正式 Pages 安全推送與事後驗證入口。批次入口優先 `pwsh -NoProfile`，未安裝時回退 `powershell -NoProfile`；PowerShell 腳本不得硬編碼非 ASCII 的受治理腳本路徑，須以唯一檔名解析，使 Windows PowerShell 5.1 在 UTF-8 無 BOM 來源下仍可安全執行。入口先拒絕 dirty、錯誤分支、遠端分歧與不符合受測來源 lineage 的 HEAD，只允許非 force push；推送後等待同一 SHA 的 push workflow，逾時且沒有可用既有 run 才使用 `workflow_dispatch` 後備入口。完成必須以 build／deploy／live-smoke／performance-trend 四個 job 全部成功、公開 `pages-deployment.json` 的 commit、runId 與 `sourceDirty=false` 一致，加上執行安全推送的工作站再次呼叫 `pages-live-smoke.js`，從正式網址獨立完成 v2 清冊重算、全部公開檔案大小／SHA-256、43 條 route、公開狀態及私有邊界檢查為準；工作站預設最多 3 次、間隔 10 秒完整重驗，且只對 smoke 分類的 5xx 或網路暫態錯誤重試，非暫態錯誤立即失敗。一般推送、既有同 SHA 部署與 `-VerifyOnly` 都走相同共同完成路徑，成功結果固定回傳 `publicArtifactVerified=true`、重試上限／間隔與 manifest 的 schemaVersion、fileCount、totalBytes、artifactDigest。GitHub Actions 頂層 run 狀態延遲只能標示 `aggregateStatusStale=true`。個別 job 聚合延遲也只可在 run 已 completed/success 且該 job 每一個 step 都 completed/success 時標示 `aggregateJobStatusStale=true`，只有 deploy failed log 同時命中 `deployment_queued`／`deployment_in_progress`、`Timeout reached, aborting!` 與取消動作，且同一 SHA 的 Pages API 在 180 秒內轉為 `succeed` 時，安全推送入口才可對同一 run 執行 `gh run rerun --failed`，等待 deploy、live-smoke 與 performance-trend 補成全綠並回傳 `deploymentRecoveryUsed=true`；其他 job、step、後端未完成或工作站逐檔複驗在有上限的暫態重試後仍失敗都不得以新 dispatch 掩蓋。dirty 例外僅限 `-VerifyOnly -AllowDirtyVerification` 的唯讀複驗，永遠不能授權 push 或 dispatch。 |
| `結構工具箱/tools/build-pages-artifact.js` | 納入 | Pages 唯一發布清冊與跨平台 staging builder；Actions 與 `run-pages-artifact-smoke.ps1` 都必須由 Git tracked 加上未 ignored 的工作中新增檔產生 artifact，集中排除 hidden path、`output/`、文件、測試、治理腳本、`dev_tools/`、backend / frontend source 與 package source。builder 使用獨立暫存 Git index 套用 clean filter，且 checkout 固定 `core.autocrlf=false` / `core.eol=lf`，使 Windows 本機預演與 Linux Actions 對同一來源得到相同 fileCount、totalBytes 與 tree digest；不得再在 workflow 或 PowerShell wrapper 維護第二份 `rsync` / `robocopy` 排除規則。 |
| `pages-release-governance.contract.test.js` | 納入 | A0 專用契約；只鎖頁面專用閱讀狀態、Pages artifact 邊界、公開狀態快照、release force flags 與 report-readiness / preflight runId 對齊，避免為了提交 A0 而整檔 staging 大型入口契約。正式 Pages provenance 要求 workflow 在 staging 前證明 checkout 無 tracked / untracked 變更，並在 `pages-deployment.json` 記錄及驗證 `sourceDirty: false`；本機 artifact 預演若尚有修改，必須如實記錄 `sourceDirty: true`，不得以 HEAD commit 冒充 clean deployment。錨栓代表頁必須逐一請求 HTML 引用的同源 script、stylesheet、modulepreload、icon 與 manifest，避免入口 200 掩蓋應用程式資產 404。 |
| `結構工具箱/tools/local-quick-tools.contract.test.js`、`結構工具箱/tools/local-quick-browser-smoke.test.js` | 納入 | 高頻局部快算共同契約與瀏覽器 smoke；集中確認工具檔案、首頁入口、乾淨路由、README、邊界文件、staging 建議、頁面責任邊界與各工具 golden regression。基礎局部檢核、設備局部荷重與擋土土壓工作頁必須載入共用直接列印封鎖 CSS；smoke 會在螢幕與列印媒體確認通知顯示狀態、其他 body 子元素全部不可列印，並把 3 個工作頁各自輸出成一頁 PDF，以 Poppler 抽檢通知文字及排除工具標題、案件資料與 DRAFT。鋼梁 / 鋼柱既有案件頁則必須在用途確認後才輸出舊案延續計算記錄，且頁面本體的直接列印只能顯示非附件邊界提示，不得列印計算結果；同一 smoke 另以桌機與手機實測載重組合控制建議、完整 tuple 選取、需求欄映射及建議文字不進報告。 |
| `結構工具箱/core/loads/loadcombo.js`、`結構工具箱/tools/loadcombo-v2.test.js`、`結構工具箱/tools/force-picker.html`、`結構工具箱/core/ui/force-picker.js` | 納入 | 多內力載重組合採完整有號 tuple：工作頁可比較各分量包絡，並依正／負向、絕對值、正規化多分量需求或構件專用 scorer 列出極限狀態控制候選。沒有逐工況容量 evaluator 的 P-M／P-V 候選只作需求向量篩選，不得宣稱為容量互制；RC 梁可逐列計算正／負彎矩容量利用率並依 Pu 重算 φVn、耐震 Vc 歸零與剪力利用率，Tu 達門檻時因未建立 φTn／V-T 互制而失敗關閉；RC 柱可逐列依 Pu 重算 δns／δs、放大彎矩與雙軸 P-M 容量面利用率；一般 RC 牆可逐列計算含軸壓／軸拉的 `|Mu|/φMn @ Pu`、剪力及特殊結構牆單段剪力上限利用率；剪力牆可逐列重算 `|Mu|/φMn @ Pu` 與 `Ve/φVn`。各工具都以可讀狀態顯示軸力越界、二階不穩定、扭力詳算或容量待確認，不得以無限值或假利用率冒充結果。候選、容量分數、平手與篩選準則屬 page-only；來源、跨工具追溯及計算書只能使用一個實際組合列，不得拼接不同組合的 `|P|`、`|M|`、`|V|` 或 `|T|`。寫入設計需求欄時必須依構件語意明確映射：RC 梁將 signed M 分流至 `+Mu / −Mu` magnitude 欄，V/T 寫入 magnitude；RC 柱及牆類的方向無關 M/V 需求欄寫入 magnitude，P 保留規定符號。來源 signed tuple 不因映射而改寫。Force Picker 屬內部流程，未明確選定組合時不得建立或送出 payload；`meta.combination` 保存名稱、方法、係數、來源完整有號 values 與 `tuplePreserved`，報告同時列來源 tuple 與採用需求。RC 梁／柱／牆／剪力牆及舊案鋼梁／鋼柱使用 tuple 選取，單一 `wu` 的 RC 板保留既有單分量流程；反例測試須證明不會合成不存在向量、不會把負需求靜默寫入 magnitude 欄而低估，容量 scorer 還須證明越界／未解析狀態失敗關閉與瀏覽器實際選取同一完整 tuple。 |
| `鋼構工具/steel-traceability.catalog.json`、`鋼構工具/steel-traceability.contract.test.js` | 納入 | 鋼構條文語意追蹤 catalog 與契約測試；集中確認鋼構正式入口、連接板、拉力構件、鋼梁與鋼柱的規範來源、輸入、計算、報告、證據與人工複核邊界，並由鋼構 audit 與平台 preflight 直接執行。 |
| `鋼構工具/app.js`、`結構工具箱/assets/status/platform-status.json` | 納入 | 連接板／拉力構件頁的巡檢狀態來源邊界；本機與私人區網保留鋼構細部 `output/audit/audit-status.json`，公開部署必須改讀已發布的平台狀態快照並明確標示涵蓋鋼構、RC 與風震核心，不得在 Pages 請求私有 `output/` 路徑造成 404，也不得把平台整體狀態誤稱為單一鋼構工具的正式核可。 |
| `鋼構工具/tool-metadata.js`、`鋼構工具/steel-formal.regression-test.js`、`結構工具箱/core/direct-print-boundary.css` | 納入 | `tool-metadata.js` 是連接板、拉力構件、鋼梁與鋼柱正式頁的 canonical 工具名稱／版號來源，必須與引用它的 HTML、`app.js`、鋼構 browser runner、共用直接列印封鎖 CSS 及 regression 一起 staging，並隨 Pages 靜態資產發布；頁首與結果區輸出按鈕必須統一走正式報表 builder，不得直接列印工作頁。四類正式核算均由各自同一份 report config 建立來源 JSON 與正式計算書指紋；重新匯入時先驗證 schema、工具種類、版本與快照指紋，再以同一計算流程重算，只有相同指紋才保留套用結果，否則回復原輸入。來源 JSON 保留在附件追溯資料，不進入計算書內容。四個鋼構正式工作頁的瀏覽器直接列印只能輸出一頁可抽取文字的邊界通知，不得輸出表單、案件資料、結果或 `DRAFT`；正式報表彈窗維持可列印。regression 集中確認 title / H1 / 報告版本一致、四項輸出追溯欄位、來源匯入／失敗回復、功能借鏡排除、共用報表 helper、HSS 斷面、單位模式、page-only 產報前檢查，以及計算書不含輸入模式、換算、流程設定、首頁摘要卡、符號教學或長篇條文解釋；browser runner 實際以檔案介面重現連接板／拉力構件／鋼梁／鋼柱來源、拒絕錯誤版本、比對來源 JSON 與計算書指紋、抽檢正式報表 HTML / PDF 的可讀文字與「計算內容在前、結論在後」順序，另以 Edge CDP、PDF 與 Poppler 驗證四個工作頁的直接列印封鎖，由平台 preflight 的 `steel-formal-regression` 直接執行。 |
| `螺栓檢討/bolt-review-tool/src/anchor-traceability.catalog.json`、`螺栓檢討/bolt-review-tool/src/anchorTraceabilityCatalog.test.ts`、`螺栓檢討/bolt-review-tool/src/backup.ts`、`螺栓檢討/bolt-review-tool/src/backup.test.ts`、`螺栓檢討/bolt-review-tool/src/useProjectLibrary.ts`、`螺栓檢討/bolt-review-tool/src/attachmentReadiness.ts`、`螺栓檢討/bolt-review-tool/src/documentApproval.ts`、`螺栓檢討/bolt-review-tool/src/documentApproval.test.ts`、`螺栓檢討/bolt-review-tool/src/reportDocumentState.ts`、`螺栓檢討/bolt-review-tool/src/reportDocumentState.test.ts`、`螺栓檢討/bolt-review-tool/tests/reportArtifacts.test.ts`、`螺栓檢討/anchor-traceability.contract.test.js`、`螺栓檢討/anchor-report.contract.test.js` | 納入 | 錨栓條文語意追蹤、工作區 JSON v2 計算重現、正式核可指紋、報告邊界與正式產物測試。v2 匯入需在合併前重算案例並比對產品、計算版本與 SHA-256 指紋；案例合併、計算內容或會改變附件內容／身分的報告設定變更會撤銷文件核可，純留痕與快照異動不會誤撤銷。頁面附件閱讀明細只留在工作頁；HTML / DOCX / XLSX 預設內部審閱，明確核可後為正式附件並記錄時間。空白案件識別由主文承接，NG 可如實形成正式附件。release 當輪保存並解析三種實體證據。 |
| `toolbox-entrypoints.contract.test.js` | 納入 | 工具箱入口合約；集中確認首頁入口、`routeFileMap`、`vercel.json`、`formal-tools.manifest.json`、`local-quick-tools.manifest.json`、canonical family metadata 與實際 HTML 存在性。固定治理名稱包括首頁正式狀態治理、首頁版本治理、公開狀態宣告一致性、非 formal 責任邊界、Pages deploy / live smoke、preflight contract 文件化、preflight JS 執行檔清冊、preflight helper script 清冊、staging 指引可執行性及目前工作樹覆蓋率；實作來源則核對 `governanceSources`、`tool-maturity-matrix.js`、`stateBoundaryRules`、`HOME_TOOL_UPDATES`、`APP_VERSION` 與 `TOOL_VERSION`。風力／地震報告的公開版本需對齊 `FormalToolMetadata`，內部 `TOOL_VERSION` 另列計算引擎；石材需對齊 `StonePublicMetadata`。公開狀態只讀 tracked `assets/status`；Pages deploy 由 `.github/workflows/pages-deploy.yml` 執行，並由 `pages-live-smoke.js`、`pages-live-browser-smoke.js` 與 `build-pages-clean-routes.js` 檢查全部公開入口、版本及直接列印邊界。 |
| `結構工具箱/tools/audit-dashboard.contract.test.js` | 納入 | 巡檢儀表板歷程合約；確認 latest preflight summary 與 preflight history 的耗時 / 最慢檢查、quick/full/failure 狀態、慢測重用、每筆 latest record 的 latest/history log 追蹤、通過檢查 log hygiene、歷史失敗與未完成的已收斂／仍待處理分流及數量對帳、成熟度升級缺口、多案例門檻、RC 正式附件完整性、具診斷證據的附件異常關閉紀錄，以及「優先建議報告閱讀狀態」只限頁面、不進計算書 / 列印 / PDF 的邊界提醒都能被 dashboard 讀取。 |
| `結構工具箱/tools/audit-dashboard-browser-smoke.test.js` | 納入 | 巡檢儀表板瀏覽器 smoke；用 fixture 驅動 dashboard，並拒絕任何未列入 fixture map 的 `output/` 請求，確認 latest record 表格、pass/fail 狀態、exitCode、latest/history log 連結、preflight history、成熟度矩陣、八類 RC 附件預期／實際／已驗證數，以及頁面專用報告閱讀狀態邊界提醒在真實 Edge/CDP 渲染下可用。歷史失敗或未完成紀錄只有在後續成功正式放行具完整來源證據時才顯示為 `C` 已收斂，且不計入目前待處理異常；原始失敗 key 與中斷原因仍留在明細。fixture 同時保留一筆尚未收斂的最新失敗，證明 KPI 不會把所有舊失敗一律洗白。匿名 SHA-256 僅在本機診斷證據存在時顯示，Pages 公開快照只能顯示分類計數與通過狀態。附件完整性另以 RC 柱缺件、未驗證及缺少 hash 的負向情境，在桌面與手機版確認總覽數量、紅色狀態與實際異常類別均正確顯示，其他七類不得被誤標；並固定驗證具證據的舊失敗可由後續成功 release 關閉、最新失敗維持未關閉、沒有本機診斷的公開狀態不載入關閉歷程，三者都不進列印。 |
| `結構工具箱/index.html` | 納入 | 正式工具箱首頁（弘一設計系統新版，依 `home.js` 單一資料源）；原公文版主選單保留為 `結構工具箱/index-classic.html` 可回退，clean route `/toolbox-home` 導向首頁。首頁的下一步建議須對齊最新成熟度矩陣，不得把已具多案例基準、版本化 JSON 與結果重播的工具重新描述成待補案例；當 upgrade gap 為零時，規劃文字應明確轉向適用範圍更深的新能力，並保留規範、案例與計算書邊界前置條件。 |
| `結構工具箱/assets/home/` | 納入 | 首頁 Web App 的資料驅動工具清單、分類樣式與 inline SVG 小圖示；不再使用分類圖片資產。 |
| `結構工具箱/assets/status/` | 納入 | GitHub Pages 可讀的首頁公開狀態快照；由 `tool-maturity-matrix.js --write` 從 git ignored `output/` 精簡產生，只保留狀態、runId、失敗數、筆數、耗時與 sourceHash 等公開欄位，不保留本機絕對路徑。報告閱讀狀態快照另保留最近一次正式放行的實際交付物渲染完成數、工具家族、release runId 與相對證據路徑；RC 附件完整性只保留各工具類別的 required／actual／verified／issue／pass，不得發布 scope、交付檔名、bytes、逐檔或集合 SHA-256、案件內容。首頁健康卡需直接用這組快照顯示 `完整檢查` / `快速檢查` / `正式放行` 與 `runId`，避免讀者把 quick run 誤判成正式交付證據；quick preflight 應保留這組 tracked 快照，正式放行或刻意刷新首頁狀態時才 staging。 |
| `結構工具箱/assets/hy/colors_and_type.css` | 納入 | 弘一首頁設計 token；只使用本機 / 系統字型，不載入遠端 Google Fonts，確保離線 preflight 與現場瀏覽器可用。 |
| `螺栓檢討/bolt-review-tool/` | 保持原工具碼與 deploy 輸出分流 | 原始 React 工具用 npm verify；`anchor/` 是部署鏡像。修改 `src/` 原始碼後先跑 `sync-anchor-deployment.ps1`，再跑 preflight；獨立 `tests/` 治理檔不屬於部署 fingerprint。新增規範路線時同步更新 `src/anchor-traceability.catalog.json` 與 `src/anchorTraceabilityCatalog.test.ts`；調整計算書、workbook、docx、正式產物留存或頁面專用附件閱讀狀態時，需一併更新 repo 層 `螺栓檢討/anchor-report.contract.test.js` 與 `tests/reportArtifacts.test.ts`。 |

目前 v3 正式附件包的事後驗證不只核對雜湊與清單；驗證器會重新解析包內正式附件及來源 JSON，重做正向工程內容、page-only 排除、正式附件身分、核可時間與來源／報告指紋配對，並核對清單 metadata 與附件實際文字。測試必須證明即使同步改寫附件、檔案雜湊、清單及附件包指紋，空殼計算書或工具／版本／時間／指紋不一致仍會 blocked。既有 v1／v2 維持原完整性規則及 review 分級。

開挖 ERH／RVR／SEV／SCV／可選 RTB 在附件組包中屬原生治理證據，不參與一般計算來源／報告配對。日常檢查、組包暫存區自我驗證及 v3 事後驗證均須核對 SCV 所列來源檔名、SHA-256、種類、版本、各層指紋、工程結果、身分採用狀態及「需保留來源檔重驗／不重算容量／不構成工程核可」邊界；缺檔、雜湊／指紋／狀態矛盾 blocked，孤立或被多份 SCV 重複引用 review。完整鏈只可進 `99_內部追溯_勿附入主報告/來源資料/`，不得進 `01_正式附件/`，摘要只顯示完成組數。此關係複驗不重算 Python 受控內容或 Ed25519 簽章，正式複驗仍須以包內來源檔重跑獨立驗證器。

案件附件工作台的唯讀 advisor 必須在外部程序執行，由 WinForms 計時器輪詢，不得在 UI thread 使用同步 `ReadToEnd()`／`WaitForExit()`。工作台須依啟動瞬間游標所在螢幕的可用工作區調整寬高並手動置中，不得固定使用主要螢幕；最低 780 × 640px 時以獨立雙向捲動容器保留全部主要內容，狀態列固定，底部正式核可提醒及最右側工具按鈕必須可捲動到完整可見。小視窗 smoke 須在顯示前設定 800 × 640px，證明視窗四邊均在所選螢幕工作區內，並以真實 `WM_VSCROLL / SB_BOTTOM` 與 `WM_HSCROLL / SB_RIGHT` 訊息及畫面座標證明內容邊界。工作台須提供明確 Tab 順序、輔助名稱與權限描述，並支援 `Ctrl+L` 聚焦路徑、`Enter` 執行目前的唯讀辨識操作及辨識中以 `Esc` 停止；鍵盤 smoke 必須證明焦點能使窄視窗外的最右側工具完整進入可視區。辨識期間按鈕必須改為「停止辨識」，讓使用者可立即取消背景程序並保留工作台開啟；路徑變更也需取消舊程序。完成結果只有在目前路徑仍相同時才能套用；60 秒逾時與關閉視窗都必須清理背景程序。worker 逾時或失敗時必須清除既有建議、將按鈕改為「重新辨識」，並在主提示及狀態列顯示可重試的狀態，不得停留在進行中文字。取消、逾時、失敗或過期結果不得開啟子工具、改變治理狀態或寫入案件。取消 smoke 必須在真實 WinForms message loop 中先以按鈕 `PerformClick()` 停止第一個 worker，再啟動第二個 worker 並以真實 `Esc` 停止，動態證明兩種路徑都會清除 advisor、保留視窗並復原按鈕與訊息；另須在同一視窗動態切換資料夾後正常關閉，證明被替換及關閉中的兩個 worker 都不殘留；逾時 smoke 必須由真實 WinForms 計時器終止第一個慢速 worker，再按同一按鈕重試並證明第二個 worker 正常完成、結果取代逾時狀態且兩個程序均不殘留；失敗 smoke 必須以真實 worker 錯誤完成相同的重試復原，兩者均不得被人工對話框阻塞。

`release-rendered-delivery-evidence` schema v3 另固定治理 canonical 完整性集合：風力／地震、局部快算、鋼構與 SRC 的 family summary 必須保存 PDF 與 evidence 各自的 bytes、SHA-256；release aggregate 以家族、角色及檔名去重後重驗 33 組、66 份實體檔，保存集合 SHA-256。PDF 或 evidence 任一同大小替換的負向 fixture 必須失敗關閉。Schema v14 納入群樁側向分配 RC 基礎案例後，成熟度矩陣僅接受 canonical 66/66、RC 視覺 66/66、混合格式 13/13 與 RC HTML 34/34 同時通過；詳細檔名與雜湊只留私人當輪證據，不進計算書或 Pages。Pages 僅能以「成品檔案完整性」公開前三組 `66/66`、`66/66`、`13/13` 及合計 `145/145` 的類別、數量、issue 與 pass，不得公開 aggregate 私有欄位名稱、scope、artifact、bytes 或 set hash。

`release-rendered-delivery-evidence` schema v4 再固定治理風力／地震 14 個正式工具的結果鏈：producer 在同一瀏覽器工作階段完成 manifest 全部 golden case 的 selector、metric 與文字結果斷言後，才以最後一個已驗證案例產生正式附件，並把案例 ID／SHA-256、驗證案例數、斷言數及報告計算指紋寫入 family summary；aggregate 必須核對 14/14 的 schema、策略、數量、指紋與集合 SHA-256。Pages 只可公開「數值結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、golden case 身分或計算指紋。

Schema v5 另固定治理 RC 梁、柱、板、牆、剪力牆、基礎與單樁的 30 組結果鏈：各 visual producer 必須保存實際專案快照的 SHA-256、案例 ID、已驗證結果斷言數及計算指紋；aggregate 必須逐筆核對報告 PDF、核可後可攜 HTML 與 reconciliation 指紋一致，要求 30/30、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「RC 結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、案例資料、快照雜湊或計算指紋。

Schema v6 再固定治理鋼構主工具連接板、主工具拉力構件、獨立連接板、鋼梁與鋼柱共 5 個計算來源：producer 必須在同一瀏覽器工作階段匯出來源 JSON、重新匯入重算、確認不相容版本遭拒且不改動既有狀態，再核對正式計算書與來源重播沿用同一計算指紋；aggregate 必須要求 5/5、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「鋼構結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、來源資料、來源 payload 雜湊或計算指紋。

Schema v7 再把 RC 梁、柱補強兩組表單重播案例納入 RC 結果鏈：producer 必須保存表單與結果快照，證明需求值改動會改變結果，再還原表單重算並核對報告及正式 HTML 指紋；aggregate 必須要求 RC 設計與補強合計 32/32、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「RC 結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、案例內容、來源快照雜湊或計算指紋。

Schema v13 再把土壓 JSON 銜接 RC 基礎案例納入結果鏈：來源必須由相同版本核心重新計算，並拒絕無效時間、遭竄改結果、不相容版本、重力式牆、靜止土壓及尚未定義 RC 強度組合的地震土壓；aggregate 必須要求 RC 設計與補強合計 33/33、RC PDF／PNG 64/64 與 RC HTML 33/33。
Schema v14 再把群樁 X／Y 向側向荷重分配納入結果鏈：aggregate 必須要求 RC 設計與補強合計 34/34、RC PDF／PNG 66/66 與 RC HTML 34/34；該分配只證明列別 p-multiplier 與水平力平衡。專業 p-y 結果必須使用固定 schema／單位，與目前樁數、樁距、樁徑、樁長、分析範圍及分析 Hx／Hy 相符，並經工程師明確勾選採用。表格可從 CSV／TSV／TXT 直接讀取或由 Excel 貼上，不直接解析 `.xlsx`；換算僅接受明示單位 profile 與固定欄名，取深度 0 位移和全深度最大絕對內力，保存各向原始檔名、列數與內容雜湊。代表單樁對應最大單樁 p-multiplier 荷重，群樁才對應整組荷重。已驗證候選可下載為與採用證據同位元組的 JSON；重新匯入、下載與採用前都須依目前模型重驗，匯入檔上限為 1 MiB。採用紀錄 v2 將原始 JSON 隨專案保存並在事後下載時重算雜湊；舊 v1 仍可重播計算，但沒有原文時不得產生替代檔。原文與採用結果被改動或不一致時必須失敗封閉。計算書只列採用結果、分析範圍、荷重與來源證據，候選警告與歸檔操作留在頁面。未採用相符結果時不得宣稱側向位移或樁身內力檢核完成。

Schema v15 再把 RC 真實來源 JSON／核可後正式 HTML 組包提升為 release aggregate 閘門：梁、柱、板、牆、剪力牆、基礎與單樁共 32 組專案重播案例，均須由 producer 保存附件檢查器 `ready`、唯一指紋連結及與重算結果相同的計算指紋；缺件、blocked、多重連結或指紋分離皆不得正式放行。補強兩案採表單重播而非專案 JSON，不得偽造來源組包證據。私人 aggregate 保存案例身分與集合 SHA-256；Pages 只可公開「RC 來源／正式 HTML 組包」required／complete／issue／pass，不得公開 scope、records、案例、檔名、工具版本、來源雜湊或計算指紋。

Schema v16 再把 RC 核可 HTML 的獨立可列印性提升為 release aggregate 閘門：設計與補強共 34 份下載後 HTML，必須在新的瀏覽器頁面、零外部網路請求下重新開啟；螢幕可恢復核可／下載控制項，但 print media 中控制項必須完全不可見。每份 HTML 都須重新列印成 PDF，通過正式附件狀態、計算指紋、正向工程內容、頁面專用文字排除及共同分頁檢查，並由 producer 與 aggregate 雙重核對 bytes／SHA-256；缺件、網路依賴、指紋分離、排版或內容失敗皆不得正式放行。私人 aggregate 保存案例、PDF 清冊與集合 SHA-256；Pages 只可公開「RC 核可 HTML 獨立列印」required／complete／issue／pass，不得公開 scope、records、案例、檔名、bytes、成品雜湊或計算指紋。

Schema v17 再把 RC 正式 HTML 計算內容封印提升為 release aggregate 閘門：34 份核可 HTML 必須對標題、追溯欄位、輸入、公式、結果、圖像資料及頁尾建立 SHA-256 內容封印，並由獨立重開的瀏覽器及附件檢查器各自重算一致。封印不符或封印範圍損壞一律 blocked；舊版正式 HTML 缺少封印時只可進入人工確認，不得自動 ready。內容封印是下載後內容一致性證據，不是核可人身分的數位簽章，也不能取代來源 JSON、計算指紋或核可時間檢查。Pages 只可公開「RC HTML 內容封印」required／complete／issue／pass，不得公開逐檔封印、scope、records、案例或檔名。

Schema v18 再把 RC 正式 HTML 核可封印提升為獨立 release aggregate 閘門：34 份核可 HTML 必須以另一組 SHA-256 綁定文件狀態、核可時間、計算指紋、報告／檔案標題與內容封印，並由瀏覽器及附件檢查器分別重算。現行 RC／共用正式 HTML 核可封印 v2 另綁定選填核可人與核可依據；空白值不構成缺件，填寫後的異動一律 blocked，既有 v1 封印仍依原欄位順序驗證相容。核可資料或標題異動一律 blocked；舊版正式 HTML 缺少核可封印只可進入人工複核。核可封印只證明下載後核可紀錄未變，不是核可人身分的數位簽章。Pages 只可公開「RC HTML 核可封印」required／complete／issue／pass，不得公開逐檔封印、scope、records、案例或檔名。

Schema v19 將雙封印邊界擴及 14 項風力／地震正式計算書：文件狀態控制與封印來源留在計算正文邊界之外，標題、追溯欄位、採用輸入、公式、結果、圖說與頁尾納入內容封印；核可封印另綁定文件狀態、核可時間、計算指紋、文件標題及內容封印。正式放行必須保存實際下載 HTML，重驗檔案 bytes／SHA-256、內容封印、核可封印，以及內容／核可兩種獨立竄改攔截。舊版缺封印轉人工複核，封印不符 blocked。Pages 只可公開兩組 `14/14` 完成數，不得公開下載檔名、scope、records 或逐檔雜湊。

Schema v20 將相同邊界擴及鋼構正式計算書：主工具連接板、主工具拉力構件、獨立連接板、鋼梁與鋼柱共 5 份核可 HTML 都必須保存實體檔並重驗雙封印。三類主工具報告從舊式直接列印改為預設內部審閱、核可後正式附件；空白計畫名稱、編號或設計人員直接省略，可由主文承接且不構成 NG，工程檢核 NG 仍可如實核可入附件。Pages 只公開內容與核可封印各 `5/5`，不得公開檔名、scope、records、bytes 或雜湊。

Schema v21 將相同邊界擴及錨栓正式 HTML：核可方塊與核可撤銷只存在工作頁，匯出後不得在附件內自行升格；正式附件保存工作頁核可時間與計算指紋，正文與核可資料各自具可重算 SHA-256 封印。瀏覽器及獨立附件檢查器都須驗證正常附件與兩種竄改反例。Pages 只公開內容與核可封印各 `1/1`，不得公開案件、檔名、scope、records、bytes、指紋或雜湊。

Schema v22 新增正式 Word 附件乾淨封裝：`結構工具箱/tools/docx-package-integrity.js` 依 OOXML 關聯從可見文件零件驗證石材、錨栓、覆工板與開挖擋土支撐當輪 DOCX，`結構工具箱/tools/docx-package-integrity.test.js` 固定驗證乾淨正例與污染反例，正式放行要求 `4/4`。未引用媒體或頁首頁尾、實際批註／批註錨點、未接受修訂、外掛範本、外部圖片、嵌入物件、ActiveX、巨集及非預期 custom XML 均須失敗關閉；合法超連結、空白 comments 零件與 python-docx 空白書目容器可接受。詳細零件清冊只留在私人 release 證據；Pages 只公開完成數，不得公開檔名、封裝清冊或逐檔細節。

Schema v23 新增正式 Excel 附件乾淨封裝：`結構工具箱/tools/xlsx-package-integrity.js` 依 OOXML 關聯、工作簿清冊與 worksheet 儲存格驗證錨栓當輪 XLSX，`結構工具箱/tools/xlsx-package-integrity.test.js` 固定驗證乾淨公式正例及隱藏／外部／主動內容污染反例，正式放行要求 `1/1`。外部關聯、外部公式或連線、公式錯誤／缺少快取結果、隱藏工作表／活頁簿視窗／名稱／列／欄、批註、嵌入物件、ActiveX、巨集、孤兒 worksheet／媒體／圖形及非預期 custom XML 均須失敗關閉；正常內部公式、凍結標題、篩選器、樣式及列印設定可接受。詳細工作表、公式與零件清冊只留在私人 release 證據；Pages 只公開完成數。

Schema v24 新增正式 Excel 列印成品：`結構工具箱/tools/xlsx-print-export.py` 使用獨立 Microsoft Excel 程序，在停用外部連結更新、巨集與事件的狀態下唯讀逐張輸出實際 XLSX；`結構工具箱/tools/xlsx-print-visual.js` 不信任產製器摘要，會重新核對來源與 PDF 雜湊、A4、橫直向、單頁寬、自動頁高、列印範圍、續頁表頭、文字層、頁面像素與裁切，`結構工具箱/tools/xlsx-print-visual.test.js` 固定驗證安全列印設定正例與 Letter／缺表頭／橫向溢出等反例。錨栓當輪活頁簿須 `1/1`、工作表 `9/9`，且橫向溢出頁為零。產製器必須先把設定寫入使用者收到的 XLSX，稽核器不得另存修改版冒充原交付品；Office 摘要、工作表名稱、PDF、逐頁指標與雜湊均不得發布至 Pages。

Schema v25 新增正式 Excel 雙封印：錨栓產製器以 `reportWorkbookSeal.ts` 在 `Summary` 寫入可見的內容／核可 SHA-256，獨立 `xlsx-seal-verifier.js` 從 OOXML 儲存格、公式與快取結果重算，不信任產製器摘要。內容封印與核可封印必須各 `1/1`，計算內容或公式竄改須讓內容封印失敗；文件狀態、核可時間、工具版本、輸出時間、計算指紋或已記錄內容 SHA 被改寫須讓核可封印失敗。日常正式附件檢查對舊版缺封印降 `review`，封印不符固定 `blocked`。這是內容完整性證據，不驗證核可人身分；驗證器、封印值、工作表內容及負向樣本均屬私人治理資料，不發布至 Pages，公開狀態只顯示完成數。

錨栓雙封印亦屬日常案件組包的必要語意檢查，不只存在 release 證據。`attachment-package-check.js` 必須以獨立檢查器重算正式 HTML 的內容與核可封印；舊版缺封印為 review，封印或 scope 不符為 blocked。v3 事後驗證會重新解析包內附件並套用相同判定，因此同步改寫附件、檔案雜湊、清單與附件包指紋仍不得掩蓋正文或核可資料竄改。

v3 事後驗證的正向證據必須保存於記憶體回傳結果：摘要分別顯示 HTML 雙封印與 XLSX 雙封印完成數／應驗數；HTML 逐份紀錄只顯示 `anchor`、`rc` 或 `formal` 家族，XLSX 逐份紀錄只顯示 `anchor` 家族，兩者均只保留內容／核可封印的 `verified`／異常狀態，不得輸出封印值、scope 或正規化正文。正式附件包管理器只能呈現這份既有自我驗證證據，不得另行改判；所有顯示仍屬內部交付確認，不得寫入附件包或計算書。XLSX 內容或核可資料被竄改時，即使重新計算附件 SHA、清單與附件包指紋，事後驗證仍須由雙封印失敗關閉。

XLSX 雙封印的獨立執行檔為 `結構工具箱/tools/xlsx-seal-verifier.js`，固定契約為 `結構工具箱/tools/xlsx-seal-verifier.test.js`；兩者屬本機／release 私人治理工具，不發布至 Pages。

Schema v8 新增石材 golden replay 至成品雜湊結果鏈：producer 必須使用目前瀏覽器核心重播 `case_01_standard_safe`，核對至少 6 項關鍵數值與控制結果，再以同一 payload 產生 PDF、DOCX 及 audit，並將 golden 檔、來源 payload、輸入／結果／計算來源與三份成品 SHA-256 綁入 summary。Aggregate 必須要求 1/1、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「石材結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、golden 案例內容、來源 payload、結果或成品雜湊。

Schema v9 新增錨栓工作區重播至成品雜湊結果鏈：producer 必須保存 v2 工作區備份實體檔，先驗證案例重現指紋並以目前核心重新計算至少 7 項控制結果，再用同一重現指紋產生正式 HTML、DOCX 及 XLSX。Aggregate 必須重新核對來源備份 schema／版本／案例／產品／重現指紋、三份成品中的計算指紋與成品 SHA-256，要求 1/1、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「錨栓結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、工作區資料、來源備份、重現／計算指紋或成品雜湊。

Schema v10 新增覆工板 JSON 重播至 DOCX 雜湊結果鏈：producer 必須保存來源 JSON 實體檔，從六組輸入執行目前頁面計算核心，至少核對 31 項數值、控制工況及判定，再用重算結果與同一計算指紋產生 DOCX。Aggregate 必須重新核對來源 JSON 案例、輸入／結果群組、計算指紋及 DOCX SHA-256，要求 1/1、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「覆工板結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、來源 JSON、輸入／結果資料、計算指紋或成品雜湊。

Schema v11 新增開挖擋土支撐 ProjectState 重播至 PDF／DOCX 雜湊結果鏈：producer 必須保存不含快取結果的 ProjectState 實體檔，回讀後執行目前 Python 後端核心，逐項核對 47 筆構件檢核與至少 618 項結果欄位，再用重算結果與同一計算指紋產生明確核可的正式 PDF、DOCX。兩種格式都須列出文件狀態、產出工具、版本、輸出時間、核可時間與計算指紋；工程名稱、計畫編號或設計者可留空並由主文承接，不得因此判為 NG。Aggregate 必須重新核對 ProjectState 案例、各輸入群組、結果／計算指紋及兩份成品 SHA-256，要求 1/1、案例身分唯一並形成私人集合 SHA-256。Pages 只可公開「開挖結果鏈」required／complete／issue／pass，不得公開 private aggregate、scope、records、ProjectState、輸入／結果資料、計算指紋或成品雜湊。

開挖計算書輸出預設為可列印的 `文件狀態：內部審閱`；只有報表匯出頁的明確核可勾選才可產生 `文件狀態：正式附件`，核可只決定該次文件身分，不得改寫工程結果或冒充數位簽章。核可 PDF 必須逐頁渲染，至少以 1.5 倍比例建立像素範圍、RapidOCR 文字、PDF 文字層、雙方 SHA-256 與 bigram Dice 一致率；任一頁低於 0.50、無墨量、缺少足量文字或生成期間 PDF 改變即失敗封閉。產出端可另提供 `.formal-source.zip` 減少分別下載，但必須驗證同名配對、artifact SHA-256、精確兩檔清單、回讀 bytes、來源穩定性及不覆寫；ZIP 只供搬運，可直接交給正式附件包管理器安全讀取並通過正式組包器，但不得把 ZIP 當成正式附件包。完整後端測試須使用實際計算核心、報表產生器、PDF canonical evidence、來源 ZIP、ERH／RVR／SEV／SCV／RTB、正式附件包管理器、組包器與事後驗證器完成匿名端到端演練，確認核可 PDF 只進正式附件區、canonical evidence 與治理 JSON 只進內部追溯區，並以 OCR 證據及來源證據替換反例證明失敗封閉。不得以純文字層測試取代可見性證據。

開挖分析階段對應屬採用資料治理：外部分析匯入列必須分開保存安裝階段、控制內力階段候選及可辨識的拆撐階段，並由使用者填寫實際施工步驟、對應依據及明確確認。若已辨識拆撐事件，使用者另須人工指定拆撐後荷重處置、承接構造或另案檢核邊界、正式依據及確認狀態；工具不得從事件名稱推定傳力路徑。後端須拒絕缺少安裝階段、未確認、未指定拆撐處置、缺少承接構造或依據、重複階段、非最大控制值、採用內力遭改動、控制早於安裝或拆撐未晚於控制的列；手動列維持相容。PDF／DOCX 只呈現已採用的安裝／控制／拆撐時序、施工步驟、依據、控制階段候選值及已確認的拆撐處置，操作警告與待確認提示不得進入正式計算書。拆撐處置欄位只建立可追溯紀錄，不代表工具已計算或驗證樓版、重撐、永久結構或其他承接構造容量，也不宣稱分析模型階段與現場步驟在工程上必然相符。

拆撐承接構造交接 JSON 屬跨工具／專案資料交接，不是正式計算書內容。後端僅可從最新重算結果建立，且只納入來源構件非 NG、拆撐生命週期與人工處置完整的列；檔案須保存來源計算指紋、逐列 ERT 指紋、整包 ERH 指紋，以及 `pending`、`autoApplied=false`、`autoVerified=false` 邊界。已發 ERH 版本須保存於專案。獨立接收端回簽助手只可在 ERH／ERT 指紋驗證成功後展開逐列表單，並由後端以受控欄位、自動彙整、固定責任邊界及 RVR 指紋產生回簽；它只能記錄接收端依正式模型完成的結果，不得自行推導容量、傳力方向、分配、偏心或載重組合。接收端回傳的 `receiver-capacity-verification-receipt` 只可在 RVR 指紋、來源計算指紋、ERH／ERT 關聯、逐列完整涵蓋、容量利用率與彙整結果均一致時匯入。ERH／ERT／RVR 指紋只證明內容完整性；既有未簽 RVR 必須維持人工核對身分。附加 Ed25519 簽章時，驗章須綁定 RVR、ERH、來源計算指紋、回簽單位及簽章時間，且 RVR 指紋不得因附加簽章改變。自帶公鑰只能證明簽章在密碼學上有效，不能自行取得信任；只有管理者已核對並登錄本機信任清冊、未撤銷且單位相符的公鑰，才可顯示「受信任簽章通過」。私人金鑰不得由網頁工具產生或保存；未知、撤銷、單位不符或未簽狀態均不得自動顯示為正式工程核可。

RVR 離線簽署介接必須以具獨立 `RSR-` 指紋的簽署請求固定實際 Ed25519 bytes；簽章回應須攜回完整請求及公開簽章資料，且在附加到 RVR 前重新核對 RSR、RVR、ERH、來源計算指紋與回簽單位。網頁及 API 不得接收私人金鑰或私鑰密碼。離線簽署程式只可讀取使用者明確選取的既有 Ed25519 私鑰，輸出不得包含私人金鑰或密碼，也不得把簽章有效直接提升為公鑰受信任或工程核可。

RVR 組織建鑰只可在接收單位控制的本機或金鑰設備執行；公開 RKE 的私鑰持有證明不等於組織身分證明。來源端登錄信任前須明確確認已透過獨立管道核對組織與 Key ID；`replacesKeyId` 只保存輪替關聯，不得自動撤銷舊金鑰。同一舊金鑰只得有一把仍受信任的待完成輪替新金鑰。所有信任清冊變更須由本機登入帳號執行：密碼只保存 scrypt 雜湊，工作階段採 HttpOnly／SameSite=Strict cookie 並以 CSRF token 保護，登入須限速，API 不得開放萬用 CORS；管理、申請與覆核角色須分開授權。帳號停用、重新啟用、角色異動、管理員密碼重設及本人密碼變更須以 SQLite 交易寫入禁止 UPDATE／DELETE 的串接指紋稽核事件；不得自行停用、改角色或由管理入口重設自己的密碼。停用或撤除申請角色須將該帳號尚未完成的輪替 claim 改為 blocked；停用與密碼重設須撤銷既有工作階段，管理員重設後須強制本人先變更臨時密碼，始得執行其他權限操作。輪替完成必須分成兩筆事件：具申請角色的帳號提出具 RVE 指紋、必填變更編號、切換摘要與 72 小時期限的申請；具覆核角色且 operator ID 不同的帳號另行確認後，專用 API 才可重驗新舊金鑰關聯、同單位與當前受信任狀態，並在同一次清冊寫入中撤銷正確舊金鑰。SQLite 須以 `BEGIN IMMEDIATE`、每把新金鑰僅一筆 pending 的部分唯一索引及申請／覆核 operator ID 不同的 CHECK 約束跨程序競爭。完成事件須保存兩個 operator ID，並以 `rotationApprovalRequestFingerprint`、`approvalRequestFingerprint`、`replacedByKeyId`、`rotationCompletionEventFingerprint` 及 `relatedKeyId` 建立雙向追溯；缺少 SQLite claim 的還原或舊版待審事件不得由角色權限核可。一般撤銷與舊版直接完成 API 不得以「輪替完成」原因繞過兩階段流程。本機帳號只驗證同一服務資料庫內的帳號、角色與工作階段，不等於外部組織目錄、自然人身分或公司授權已由第三方驗證。帳號稽核鏈僅涵蓋功能啟用後的治理操作，且不是外部可信時間戳；公開 RTB 不包含帳號、工作階段、輪替 claim 或帳號稽核鏈。JSON 事件清冊與 SQLite claim 分屬兩個檔案，不得宣稱跨檔單一原子交易；中斷或狀態矛盾時須失敗關閉並人工稽核。疑似外洩、確認外洩或私鑰遺失的緊急撤銷不得因雙人輪替門檻而延誤。

RVR 操作員治理災難復原必須使用獨立的 `receiver-operator-governance-encrypted-backup`，不得以只含公開資料的 RTB 代替。新版封包為 schema v3；帳號、角色、加鹽密碼驗證值、輪替 claim、到期備份處置 claim、ROE 稽核鏈與完整 GHR 治理健康歷程只可存在 scrypt 衍生金鑰的 AES-256-GCM 密文內。外層只可揭露時間、筆數、GHR 鏈首、加密參數與 ROB／ROG 指紋，且工作階段、CSRF token、登入失敗與限速紀錄不得備份；讀取端仍須接受不含 GHR 的 v1／v2 備份。匯出前須先追加匯出 ROE 與對應健康觀測，使 v3 密文內最新 GHR 綁定該次治理快照。匯出、清冊、演練、預覽與復原 API 均須要求已登入的啟用中 `receiver-key-admin`，所有變更或解密動作另須 CSRF；復原另須通過備份密碼、備份內啟用管理員帳密及完整置換確認。下載時另存受管制本機副本必須明確 opt-in，保存期間限 1 至 365 天；清冊須逐檔重驗封包、檔名、時間、ROB 指紋與 SHA-256，顯示是否涵蓋 GHR、筆數與鏈首，且無有效副本、檔案無法驗證、最新副本未演練或同一副本的演練超過 30 天均不得顯示健康。演練必須在隔離暫存 SQLite 使用實際復原函式，驗證備份管理員登入、復原事件、ROE 及 GHR 鏈；v3 歷程筆數與鏈首須完全一致，並以正式治理快照前後相同作為 `ROD-...` v2 通過收據前提。收據不得保存帳號、密碼、加密密碼或伺服器路徑。保存期限本身只提供治理提醒，不得觸發自動刪除。到期副本須先由啟用中 `receiver-key-requester` 建立含案件與依據、72 小時有效且同一 ROB 唯一未結案的 `RBR-...` claim，再由不同 operator ID 的 `receiver-key-approver` 覆核；覆核後須重新驗證受管制檔案及 SHA-256，才可做一般檔案項目移除並以不覆寫方式寫出 `RBD-...` 收據。申請、覆核與完成須分別追加 ROE 事件；中斷狀態須可由已保留完成資訊的同一覆核帳號續辦，且 claim 與事件須隨治理備份復原。RBD 必須明示 `automaticExpiryDeletion=false`、`ordinaryFilesystemEntryRemovalOnly=true`、`secureEraseGuaranteed=false`、`otherCopiesMayRemain=true`，不得宣稱 SSD、同步端、快照或其他備份媒體已安全抹除；收據不得含帳號名稱、密碼、加密密碼或伺服器路徑。既有有效資料庫只允許 ROE 與 GHR 都相同或向前延伸；全新單一啟動管理員且無輪替／備份處置 claim、GHR 只含啟動／登入／匯出觀測時才可作災難復原例外。v1／v2 復原須保留本機 GHR，不得宣稱恢復來源歷程。寫入前須建立同密碼加密、不覆寫且含現況 GHR 的 v3 pre-restore 保全檔，並在 `BEGIN IMMEDIATE` 交易內確認治理快照與 GHR 均仍等於預覽現況，再原子置換治理表及 v3 來源歷程、清除登入失敗、撤銷全部工作階段、追加復原 ROE 與復原後 GHR；競爭異動或任一步失敗均須整筆回滾並保持兩條鏈禁止 UPDATE／DELETE。備份密碼不得保存，密碼遺失即不可復原；因密文含加鹽密碼驗證值，檔案仍須以高度敏感資料受控並與密碼分開。受管制本機副本與 pre-restore 都不等於異地備份；應用層控制不能阻止具檔案或資料庫管理權限者直接替換或刪除資料，也不是外部可信時間戳、組織身分或工程核可。治理備份、清冊、演練、處置、差異與復原結果不得進入 PDF／DOCX 計算書或公開 Pages。

`receiver-key-requester` 與 `receiver-key-approver` 是既有帳號、備份及權限資料的相容性契約，不得因顯示文字調整而變更。HTML 顯示名稱固定為「治理申請人」與「治理覆核人」，兩者同時涵蓋金鑰輪替及到期備份處置，不得以只暗示單一流程的名稱縮限職責。HTML 必須提供唯讀治理權限矩陣，逐列列出 `receiver-key-admin`、`receiver-key-requester`、`receiver-key-approver` 單獨授權時可執行的管理、申請、覆核動作及異人限制，並明示角色可疊加但管理員角色不自動取得其他兩種權限。登入後另須只依後端工作階段回傳的穩定角色 ID 與待變更臨時密碼狀態產生目前帳號有效權限摘要，逐項區分有效、暫停及未授權；兼具申請與覆核角色仍須提示後端禁止自我覆核。管理員可見的治理分權健康摘要必須由後端單一唯讀 API 判定，前端不得自行拼接帳號、信任清冊與處置清冊形成第二套權威。後端只能把啟用且無待變更臨時密碼的帳號列為有效人力，須檢查不同 operator ID 的申請／覆核組合、專責角色分離、每筆待覆核輪替／備份處置 claim 的合格第二人，以及 SQLite claim 與 JSON 信任清冊事件的缺漏或矛盾；缺少不同帳號組合或任一 claim 不可覆核時必須顯示需要處理，角色重疊但可執行時不得宣稱分權完整。回應須提供 `RGH` 健康摘要、`ROG` 帳號治理快照、`RTR` 信任清冊及 ROE 鏈首追溯指紋，但不得含帳號名稱、顯示姓名、密碼或密碼驗證資料；並須明示 SQLite 單一讀取交易與 JSON 單一驗證快照不是跨儲存體原子交易，所有實際操作仍重新授權。每次治理異動後須以 append-only 收據鏈保存狀態轉換、來源指紋與前序 `GHR`；GET 不得因查看而寫入，重複 RGH 不得膨脹歷程，來源漂移未入鏈時須明示且只能由管理員 POST 觀測補記。完整歷程 JSON 須具 `GHE` 整包指紋並揭露其不是外部時間戳、數位簽章或高權限資料庫置換防護。矩陣、摘要、追溯與歷程資訊只供操作前核對，不得取代後端逐項授權，也不得進入 PDF／DOCX 計算書或公開 Pages。

RVR 信任金鑰撤銷是一次性且不可由畫面覆寫的治理事件。撤銷 API 必須要求受控原因分類、具體摘要、處理者、選用的事故／案件編號及不可復原確認；清冊同時保存金鑰撤銷欄位與串接前一事件指紋的 `receiver-verification-key-event`。重複撤銷、缺少確認、非法原因分類或事件指紋／順序不符必須失敗關閉。`RVE-...` 只提供本機完整性稽核線索，不得宣稱為外部可信時間戳或憑證機構背書。

RVR 信任清冊備份必須只含公開金鑰、登錄資料與生命週期事件，不得含私人金鑰、密碼或秘密欄位；匯出須以 `RTR-...` 固定清冊快照並以 `RTB-...` 固定完整備份包。匯入只可先驗證及產生差異預覽，不得立即改動本機清冊。復原 API 必須要求明確確認、寫入前建立 `pre-restore` 保護副本並在寫入後重驗清冊指紋。當目前清冊有效時，只允許既有事件鏈的向前延伸；移除既有 Key ID、改寫既有登錄／撤銷紀錄、回退事件鏈或逆轉撤銷均須失敗關閉。只有清冊遺失或無法讀取時，才可由通過完整驗證的快照進行災難復原。備份不等於外部可信時間戳、遠端保管或憑證機構背書，且不得出現在 PDF／DOCX 計算書正文。

RVR 定期備份演練只能在隔離暫存清冊執行真實復原；產生 `receiver-trust-registry-recovery-drill-receipt` 前，必須驗證備份雙指紋、事件鏈、選用的新鮮度門檻、復原後清冊指紋與筆數，並證明正式清冊前後 SHA-256 不變。`RDR-...` 收據必須固定備份檔 SHA-256、RTB、RTR、演練時間與結果；備份遭竄改、同名檔案已存在、隔離復原不一致或正式清冊發生異動時須失敗關閉且不得產生通過收據。工具不得自動覆寫或刪除歷史備份；RDR 不得宣稱為外部時間戳、異地副本證明或工程核可，也不得進入正式計算書正文。

RC 梁、柱、板、牆、剪力牆、基礎與單樁的專案 JSON 維持來源資料角色，附件檢查器直接辨識既有 `metadata`／`fields` 與單樁 `state` 結構。七個 RC 計算書的產出工具名稱與版本須和各自專案 JSON 完全一致，讓案件、工具、版本及 `CF-` 可形成自動來源／報告配對；包內同時存在完整追溯來源與正式計算書時至少須形成一組完整配對，零配對必須 `blocked`。實際瀏覽器測試必須把核可後可攜 HTML 與真實專案 JSON 交給同一組包檢查器，並證明正常配對 `ready`、竄改指紋、錯版本及完整身分改寫來源 `blocked`。不得要求使用者另造轉接 JSON，也不得把專案來源移入正式附件目錄。

### Windows 案件附件工作台捷徑

根目錄 `安裝案件附件工作台捷徑.bat`、根目錄 `檢查案件附件工作台捷徑.bat`、根目錄 `移除案件附件工作台捷徑.bat`、`結構工具箱/tools/install-attachment-governance-shortcuts.ps1` 與 `結構工具箱/tools/attachment-governance-shortcut-installer.test.js` 屬於納管的本機 Windows 入口。桌面、SendTo 與開始功能表三個 `.lnk` 只能指向 repo 根目錄受治理的 `啟動案件附件工作台.bat`，工作目錄必須是當前 repo，且 `Arguments` 必須為空，使 SendTo 只轉交當次 Windows 選取的單一案件資料夾或 `.formal-source.zip`；其他檔案、多重選取、連結與特殊項目仍須由工作台封閉拒絕。檢查模式必須全程唯讀：個別捷徑只可回報 `current`、`repairable`、`foreign` 或 `absent`，整體只可依序判為 `ready`、`review` 或 `blocked`；檢查前後檔案時間與內容不得改變。安裝器必須先預檢三個目的地，只可更新帶管理標記或精確指向目前 repo 受治理啟動器的捷徑；任一處存在同名但非本工具管理的捷徑時，必須保留原檔、在寫入前停止整批安裝並失敗封閉。指向其他資料夾同名批次檔的捷徑必須判為 `foreign`，不得由檢查、安裝或移除模式接管。建立後必須重讀 `.lnk` 驗證目標、工作目錄、空參數、描述與圖示；相同且正確的捷徑不得重寫。移除模式只能刪除受管理捷徑，不存在與同名使用者捷徑必須分別回報 `absent` 與 `preserved`。批次入口、PowerShell 安裝器與動態測試均屬私有本機治理資產，不發布至 Pages；PowerShell 檔須保留 UTF-8 BOM。

跨重啟多筆復原總覽只可對明確單選且仍有效、存在的精確輸出提供檔案總管預覽或 Windows 剪貼簿路徑複製；兩個動作都要在執行當下重查資格與資料夾存在性，不得觸發 verifier、修改、重建或核可。`Ctrl+C` 必須同時顯示於說明列與複製按鈕；清單須停用 DataGridView 原生整列複製，快捷鍵只能轉交同一受控路徑複製動作，無有效選取時攔截且零寫入。剪貼簿只接收該筆完整路徑，不接收收據內容、案件資料或驗證結果。

單筆合格復原收據須在主畫面待確認狀態卡顯示本機有效至與剩餘時間，並以 30 秒 UI timer 重讀精確收據及重查輸出資料夾；超過 2 小時採一般文字色，2 小時內以深橙色及明文提示，30 分鐘內以紅色及明文提示，無障礙說明亦須陳述期限狀態，不得僅依賴顏色。到期時移除收據並自動停用複製與唯讀驗證，提前失效或輸出不存在時也須失敗封閉。無收據的同次結果交接不得顯示虛構期限或啟動計時器，只能使用該次已鎖定且仍存在的預定輸出。狀態卡可顯示獨立的「複製精確路徑（Ctrl+C）」；按下時仍須重讀收據並封閉核對管理資格、期限、精確輸出一致性與資料夾存在性。快捷鍵只可在焦點位於該按鈕時攔截並轉交同一點擊路由，不得攔截其他欄位的原生 `Ctrl+C`。複製與唯讀驗證必須是兩條獨立路由，前者不得啟動 verifier、build、修改或核可，也不得複製收據內容或案件資料；到期刷新同樣不得觸發上述動作。

## 不進 repo 的類型

- `node_modules/`、`.vite/`、`.playwright-cli/`
- `tmp/`、`_tmp/`、`__pycache__/`、`.pytest_cache/`
- `output/`、測試 run log、release bundle
- 本機專案資料：`app_data/`
- 大型工程輸出與參考：PDF、Excel、Word、LibreOffice 暫存 profile
- 反查或抽取資料：`_extracted/`、dump 文字檔、OLE/EMF/WMF 圖檔
- 名稱剛好命中忽略規則的工具目錄，例如 `結構工具箱/tools/基礎/`；靜態工具應使用不被忽略的英文資料夾，如 `tools/foundation/`。

## 正式附件包進度觀測邊界

`attachment-package-build.js` 與 `attachment-package-manager-worker.js` 的建立階段 JSONL 只供本機 UI 旁路觀測。執行中寫入失敗不得中止或改寫 check / build / verify 結果；UI 可停留於最後已驗證階段，但不得把已原子發布成功的附件包誤報為失敗。輸入的事件檔路徑仍須在啟動核心動作前完成受管暫存區驗證，非法路徑維持失敗封閉。

管理器最終結果 JSON 是套用核心結論的必要 IPC，不等同可忽略的階段事件。worker 須在啟動任何核心動作前於系統暫存區排他建立受管結果檔，並透過同一持有中的檔案描述元寫入與 fsync；保留失敗必須在零核心動作時退出，既有檔不得覆寫。最終 I/O 若仍無法形成可解析結果，GUI 只能回報「建立結果待確認」，不得宣稱附件包建立失敗或直接重建；明確輸出若已存在應帶入驗證欄，先由既有唯讀 verifier 判定。

GUI 只可套用 action 與本次預期動作一致，且狀態和實際 worker 退出碼精確對應的結果；`ready / review / blocked / error` 固定對應 0 / 1 / 2 / 3。預存、錯配或無法解析的結果不得形成成功畫面、一次性組包權限或驗證結論。

任一來源檢查 ready 時，唯讀 worker 只可為空白欄位規劃一個含時間與隨機識別碼的唯一輸出路徑，不得預建資料夾或覆寫使用者選擇；正式 build 缺少確切路徑時拒絕啟動，既有位置仍由組包核心封閉阻擋。結果交接待確認時，只有該精確預定輸出目前確實存在才可形成一鍵復原入口；該入口只能委派既有背景唯讀 verifier，沿用取消、逾時與封閉結果檢查，不得呼叫 build、改寫附件包、核可文件或自行提升狀態，亦不得掃描或猜選其他候選包。

正式建立跨重啟復原只可使用目前 Windows 使用者系統暫存區內的短期私有收據：build 前排他新建、worker 啟動後原子更新，固定 request ID、24 小時期限、絕對來源／精確輸出、PID／啟動時間與受管 IPC。可信 build 結果立即刪除；交接異常且輸出存在才可保留。一般啟動只讀取受管檔名、正確 schema、未逾期、原程序已結束的收據；單筆只能導向既有唯讀 verifier，多筆只能開啟列出狀態、建立時間、有效至／剩餘期限與精確輸出路徑的本機唯讀單選總覽，依最早到期穩定排序。多筆與單筆採相同分級：超過 2 小時為一般文字色、2 小時內為深橙色明文提示、30 分鐘內為紅色明文提示；可見說明與無障礙描述均須陳述分級，不得只依賴顏色。初始不得預選且須定時刷新期限；已選項目若在刷新時到期、輸出消失或資格失效，必須立即清除選取及目前儲存格、停用開啟／複製／驗證，且任何失效列都不得被重新保留為選取。可見狀態列與 assertive 無障礙 live region 必須通知變化；未到期輸出暫時不存在時仍須持續重查並通知恢復。到期收據及其受管暫存檔須立即清除；全部候選均已到期時才可停止計時、將取消改為關閉並明示目前已無可驗證項目。明確選取仍有效且存在的項目後，可由 Windows 檔案總管開啟該精確資料夾供人工查看；此預覽不等同驗證，不得修改附件包、啟動 build 或形成核可。執行 verifier 前仍須重讀選定收據並重新確認管理資格、期限及路徑完全一致；取消或關閉清單、不可信封套或 `error` 結果須保留全部尚未到期且未完成的收據。只有明確選定項目的可信 `ready / review / blocked` 驗證結論套用後才清除該筆；收據不得送出本機、發布至 Pages、修改附件包、形成正式核可或保存超過期限。

## 首頁正式放行日期來源

首頁工具內容更新日與正式放行日是不同資料：逐工具日期留在 `HOME_TOOL_UPDATES`，正式放行日只由 tracked `preflight-summary.json` 在執行期提供。快照必須是 passing、非 quick、強制平台巡檢與慢測、來源 commit 可辨識且乾淨，否則卡片只引導查看上方交付前檢查狀態，不得顯示人工猜定日期。

Pages deployment manifest 現採 schema v3。前表所稱 v2 封閉逐檔清冊仍完整保留，並新增 `releaseEvidence`，直接綁定實際發布之 tracked `preflight-summary.json` 的正式 release runId、產生時間與受測來源 SHA；同時要求 `report-readiness-status.json` 通過且 runId 一致。不是 full run、未強制平台巡檢或慢測、來源不乾淨、checks / post-checks 未全數通過，或兩份公開快照身分不一致時，artifact 建置必須失敗封閉。巡檢儀表板的「一般巡檢新鮮度」與「正式放行證據」分開顯示；7 日與 30 日門檻只提供重驗提醒，不改寫既有 release 身分。公開 manifest 缺少時只顯示「未部署證據」；carrier、Actions run、release run 或 tested source 任一不一致時顯示紅色「未對齊」，不得用較新的單項巡檢掩蓋。

有效 schema v3 deployment manifest 同時是 dashboard 的公開資料範圍界線；資料範圍與部署信任不可混為一談，staging manifest 的 `sourceDirty` 只要是合法布林仍屬公開 artifact，dirty 狀態另由部署信任卡揭露及 gate 判定。公開模式只讀 manifest 與 `assets/status/` 三份 tracked 摘要，不請求 Git ignored `output/`、私人 log、本機附件診斷、RVR 或 GSM 狀態；三份摘要皆須符合 `publicEvidenceSchemaVersion: 2`，並由同一公開 Schema 模組逐欄、逐型別驗證四個證據面向及最多 8 次的封閉公開 release 歷史。必要欄位缺漏、未知版本、型別錯誤、完成數未滿、runId 不一致、歷史倒序／衝突／未對齊或 pass flag 不成立都不得宣稱完整，且不得把同一 platform pass 複製成四張模組卡；型別無效的數值顯示為不可用，不得以原字串偽裝成有效比值。沒有公開資料的成熟度、RVR／GSM、preflight 細項與私人巡檢歷程整區移出公開閱讀流程；本機模式仍完整保留。私人摘要與連結明示僅限本機，不得因私人檔 404 顯示為工程巡檢異常。只有 localhost 可用 `?audit_scope=local` 進入完整本機診斷；外部網域即使帶相同 query 仍維持公開模式。dashboard 專用 browser smoke 必須證明本機診斷、四個公開證據面向、結構化完成數、公開 release 歷史、失敗關閉、公開資訊密度與隱藏私人連結未退化；共用 Pages browser smoke 則在 staged artifact 與正式網址的桌面／手機驗證公開 scope 與零 private-output 請求，正式網址另故意附加 local query，證明外部覆寫無效。

本機 Windows Pages artifact 預演仍以 pinned Terser 產生與 Actions 相同的 browser program；為避免完整驗證成長後突破 Windows 命令列長度，PowerShell 先以 GZip 壓縮並 Base64 編碼，runner 再把壓縮內容交給受控頁面環境的 `DecompressionStream('gzip')` 解壓，將原程式碼回傳後執行。browser smoke source 仍不得進入 Pages artifact，bootstrap 仍受 7,500 字元硬上限約束，壓縮傳輸不得成為發布測試 source 的例外。

Pages browser smoke 的跨平台 CLI 回傳由 `.github/pages-smoke/normalize-playwright-result.js` 正規化；只接受具正整數 `routes`／`checks` 與非負 `issues` 的封閉結果，缺少量測值即失敗。此 helper 與 runner、CI 摘要同屬私有發布治理，不進 Pages、計算書或正式附件。

## 提交前檢查順序

1. `.\run-preflight-tools-quick.bat`
2. `.\run-preflight-tools-release.bat`
3. `.\run-pages-artifact-smoke.ps1`
4. `git status --short --untracked-files=normal`
5. 修改錨栓原始碼後執行 `.\sync-anchor-deployment.ps1`。
6. 確認 `anchor/assets` 舊 hash 刪除與新 hash 新增成對出現，且 `anchor/deployment-manifest.json` 已更新。

實際 staging 分包請參考 [STAGING_GROUPS.md](/C:/Users/USER/Desktop/AI/小工具製作/STAGING_GROUPS.md:1)。

## 接收端治理健康歷程邊界

接收端治理健康歷程 JSON 必須可由管理 HTML 送回後端重新驗證，並拒絕未知欄位、鏈語意不連續、筆數／鏈首／證據邊界或 `GHE` 整包指紋不一致。匯入只驗證，不得合併、覆寫正式歷程鏈，也不得使治理資訊進入 PDF／DOCX 計算書或公開 Pages。

GHR 歷程已由新版 v3 帳號治理加密備份涵蓋，可在通過解密、鏈驗證及回退／分叉防護後恢復正式鏈；v1／v2 舊版備份則只能保留復原目標的本機鏈。GHE 仍是獨立核驗檔，不是異地備援，重新匯入也不得恢復或合併正式鏈；v3 受管制本機副本同樣不等於異地保存或外部可信錨定。

管理 HTML 必須可從目前已驗證 GHR 建立封閉欄位的 `GCR` 簽署請求；完整 GHE、簽署時間、自陳邊界與獨立 context 須共同納入 Ed25519 payload，離線簽署器產生具 `GHC` 整包指紋的自足檢核點。匯入時必須重驗 Base64 canonical JSON、GHE、GCR、Ed25519、公鑰 Key ID、GHC、目前信任／撤銷狀態及 GHR 前綴關係；目前鏈短於檢核點須明示疑似回溯或資料遺失，非共同前綴須明示分叉，兩者均不得接受。未知或已撤銷公鑰即使密碼學簽章有效也不得視為目前狀態錨點。GHC 只是一份可攜式外部紀錄；應用程式不保存、不合併也不自動選取檢核點，不能證明其已離開本機，`signedAt` 也不是第三方可信時間戳。GCR、GHC、驗證結果與操作說明只屬 HTML 治理工作區，不得進入 PDF／DOCX 計算書、正式附件包或公開 Pages。

GHC 必須另有不啟動服務、不讀專案資料庫或私人金鑰的獨立驗證入口。它可在只提供 GHC 時驗證密碼學完整性；提供嚴格驗證的公開 RTB 時判斷該清冊在 `exportedAt` 快照中的受信任／未知／已撤銷狀態，但不得反推金鑰在可自陳的 `signedAt` 已受信任；再提供目前 GHE 時比較相同、延伸、落後或分叉。輸出的 `GCV` 收據須綁定各來源檔名與 SHA-256、GHC／GCR／GHE、RTB／RTR、簽章、歷程關係與固定責任邊界，並明示收據不能脫離來源檔作為單獨證明，不驗證外部保存或第三方時間戳，也不是工程核可或正式計算附件。

GCV 的選配可信時間流程必須先重新驗證 GCV 可由全部來源 bytes 完整重建，再以封閉 `GAM` 清單綁定檔名、大小、SHA-256 及治理摘要，產生要求 TSA 憑證且含 nonce 的 SHA-256 RFC 3161 `GTS` 查詢。工具不得內建、暗中選取或自動連線 TSA。取得外部回應後，必須以使用者明確選取的信任根同時驗證查詢 nonce、GAM 實際 bytes 與 TSA 憑證鏈，並把同一份已驗證快照封裝為 `GTV` 證據包；日後重驗須拒絕缺檔、加檔、子資料夾、檔名／指紋不一致、錯誤信任根、錯誤查詢、來源篡改或收據不一致。TSA `genTime` 只在採用者接受該 TSA、信任根與 policy identifier 時證明 GAM 綁定內容最遲於該時間已存在；本工具不查 OCSP／CRL、不證明異地保存、保留鎖、工程核可或正式附件資格。GAM、GTS、TSA 回應／憑證與 GTV 均不得進入 PDF／DOCX 計算書、正式附件包或公開 Pages。

GTV 的選配外部歸檔流程必須先把同一份已重驗 GTV bytes 封裝成未加密、未壓縮、無子資料夾、無重複可攜式檔名的封閉 `GAP` ZIP，再以 `GAD` 固定保存端、保存庫、GAP SHA-256／大小、不可變模式、最低期限與 legal hold 要求。工具不得自行上傳或把本機複製、一般版本控制、唯讀權限冒充 WORM。外部保存端只有在實際入庫後才能以獨立 Ed25519 金鑰簽發 `GAR`；案件端必須使用由組織另行核定的公開金鑰驗章、保存實際核定證據檔 bytes，且只有 provider／repository、物件／版本、stored、模式、期限與必要 legal hold 全部滿足時才可建立 `GAV`。日後重驗須重新驗證完整 GTV、GAD／GAP、GAR 簽章與六檔封閉 GAV。GAV 只證明指定公開金鑰可驗證的保存端簽章證言，不即時查詢外部物件是否仍存在，也不判讀核定文件、不驗證供應者作業控制、金鑰組織歸屬、工程核可或正式附件資格。GAD、GAP、GAR、公開金鑰、核定證據與 GAV 均不得進入 PDF／DOCX 計算書、正式附件包或公開 Pages。
